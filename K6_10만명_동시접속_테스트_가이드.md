# 📊 K6 대규모 채팅 부하 테스트 가이드 (10만명 동시 접속)

## 🎯 목표

하나의 채팅방에 **10만명이 동시에 접속**하여 메시지를 전송할 때:
1. **서버 응답 시간** 측정
2. **메시지 순서 보장** 확인
3. **10만개 메시지 조회 시간** 측정

## 📋 사전 준비

### 1. 필수 소프트웨어 설치

#### Windows
```bash
# K6 설치 (Chocolatey 사용)
choco install k6

# 또는 Scoop 사용
scoop install k6

# Docker 설치 (필수)
# https://www.docker.com/products/docker-desktop
```

#### macOS
```bash
# K6 설치
brew install k6

# Docker 설치
brew install --cask docker
```

#### Linux (Ubuntu/Debian)
```bash
# K6 설치
sudo gpg -k
sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
sudo apt-get update
sudo apt-get install k6

# Docker 설치
sudo apt-get install docker.io docker-compose
```

### 2. 테스트 사용자 생성

#### 옵션 A: SQL 스크립트로 직접 생성 (권장)

```bash
# MySQL 접속
mysql -u root -p coreconnect

# SQL 파일 실행
source 테스트_사용자_10만명_생성.sql

# 프로시저 실행 (원하는 규모 선택)
# 소규모 테스트 (1,000명)
CALL CreateTestUsers(1000);

# 중간 부하 (10,000명)
CALL CreateTestUsers(10000);

# 대규모 부하 (100,000명) - ⚠️ 10-30분 소요
CALL CreateTestUsers(100000);
```

#### 옵션 B: API를 통한 동적 생성

테스트 스크립트가 자동으로 사용자를 생성하도록 설정할 수도 있지만, 
사전에 DB에 생성하는 것이 더 안정적입니다.

### 3. 모니터링 환경 시작

```bash
# InfluxDB + Grafana 시작
docker-compose -f docker-compose.monitoring.yml up -d

# 상태 확인
docker ps | grep k6

# 로그 확인
docker-compose -f docker-compose.monitoring.yml logs -f
```

#### Grafana 접속

- URL: http://localhost:3000
- 로그인: `admin` / `admin123`
- 대시보드: "K6 - 10만명 동시 접속 채팅 부하 테스트"

### 4. 백엔드 서버 시작

```bash
# Docker Compose로 백엔드 시작
docker-compose up -d backend

# 상태 확인
curl http://localhost:8080/api/health
```

## 🚀 테스트 실행

### 방법 1: 자동 실행 스크립트 사용 (권장)

#### Windows
```bash
run-massive-chat-test.bat
```

#### Linux/Mac
```bash
chmod +x run-massive-chat-test.sh
./run-massive-chat-test.sh
```

스크립트 실행 시 다음 옵션을 선택할 수 있습니다:
1. **대규모 부하 테스트** (10만명, 5분 램프업)
2. **중간 부하 테스트** (1만명, 2분 램프업)
3. **소규모 테스트** (1,000명, 1분 램프업) - 로컬 테스트용
4. **사용자 정의**

### 방법 2: 수동 실행

```bash
k6 run \
  --out influxdb=http://localhost:8086/k6 \
  -e BASE_URL=http://localhost:8080 \
  -e WS_URL=ws://localhost:8080 \
  -e TEST_ROOM_ID=1 \
  -e TOTAL_USERS=100000 \
  -e RAMP_UP_TIME=5m \
  -e STEADY_TIME=10m \
  -e RAMP_DOWN_TIME=2m \
  performance-tests/massive-chat-load-test.js
```

### 방법 3: Docker로 실행

```bash
# K6 컨테이너 사용
docker run --rm -i \
  --network host \
  -e K6_OUT=influxdb=http://localhost:8086/k6 \
  -e BASE_URL=http://localhost:8080 \
  -e WS_URL=ws://localhost:8080 \
  -e TEST_ROOM_ID=1 \
  -e TOTAL_USERS=100000 \
  -e RAMP_UP_TIME=5m \
  -e STEADY_TIME=10m \
  -e RAMP_DOWN_TIME=2m \
  -v "$(pwd)/performance-tests:/scripts" \
  grafana/k6:latest run /scripts/massive-chat-load-test.js
```

## 📊 테스트 시나리오 상세

### 시나리오 1: 대규모 동시 접속 및 메시지 전송

```
[램프업 5분]
0명 → 10만명까지 점진적 증가
↓
[유지 10분]
10만명이 동시에 메시지 전송
- 각 사용자가 2-5초마다 메시지 전송
- 총 약 100만~300만 개 메시지 생성
↓
[램프다운 2분]
10만명 → 0명까지 점진적 감소
```

**측정 지표:**
- 메시지 전송 응답 시간 (평균, P50, P95, P99)
- 로그인 성공률
- 메시지 전달 성공률
- 처리량 (RPS)
- 에러율

### 시나리오 2: 메시지 조회 성능 테스트

```
100명의 사용자가 동시에
↓
채팅방의 메시지를 조회
(페이지당 1,000개씩)
↓
총 10페이지 조회
```

**측정 지표:**
- 메시지 조회 시간 (1,000개 단위)
- 순서 보장률 (메시지 ID/시간 기준)
- 순서 위반 건수

## 📈 결과 확인

### 1. Grafana 대시보드 (실시간)

http://localhost:3000 접속 후 대시보드 확인

**주요 패널:**
- 메시지 응답 시간 (평균)
- 현재 응답 시간 (게이지)
- 메시지 전달 성공률 (게이지)
- 활성 가상 사용자 (VUs)
- 초당 요청 수 (RPS)
- HTTP 요청 응답 시간 (P50, P95, P99)
- 메시지 전송/수신 카운트
- 메시지 순서 위반 건수
- 메시지 조회 시간

### 2. 콘솔 출력

테스트 완료 후 콘솔에 요약 정보가 출력됩니다:

```
========================================
📊 10만명 동시 접속 채팅 부하 테스트 결과 요약
========================================

✅ 로그인 성공률: 98.50%

💬 메시지 전송 통계:
  - 전송된 메시지: 1,500,000개
  - 수신된 메시지: 1,450,000개
  - 전달 성공률: 96.67%

⏱️ 메시지 응답 시간:
  - 평균: 850.25ms
  - 중간값 (P50): 650.00ms
  - P95: 2,100.00ms
  - P99: 3,500.00ms
  - 최대: 8,200.00ms

📖 메시지 조회 시간 (1000개 단위):
  - 평균: 1,250.50ms
  - 중간값 (P50): 1,100.00ms
  - P95: 2,500.00ms
  - P99: 4,000.00ms
  - 최대: 7,500.00ms

🔢 메시지 순서 보장:
  - 순서 위반 건수: 125건
  - 순서 위반률: 0.0083%
  - 순서 보장률: 99.9917%

🌐 HTTP 요청 통계:
  - 평균 응답시간: 750.00ms
  - P95 응답시간: 2,000.00ms
  - P99 응답시간: 3,200.00ms
  - 실패율: 3.50%

📈 처리량:
  - 총 요청 수: 2,500,000개
  - 초당 요청 수 (RPS): 2,500.00
```

### 3. HTML 리포트

테스트 완료 후 `summary.html` 파일이 생성됩니다.

```bash
# 브라우저에서 열기
# Windows
start summary.html

# macOS
open summary.html

# Linux
xdg-open summary.html
```

### 4. JSON 리포트

`summary.json` 파일에 상세한 메트릭이 저장됩니다.

```bash
# JSON 확인
cat summary.json | jq '.metrics.message_response_time'
```

## 🎯 성능 기준 및 판단

### ✅ 합격 기준

| 지표 | 목표 | 설명 |
|------|------|------|
| 로그인 성공률 | ≥ 95% | 10만명 중 95% 이상 로그인 성공 |
| 메시지 전달 성공률 | ≥ 90% | 전송된 메시지의 90% 이상 전달 |
| P95 응답 시간 | < 3초 | 95%의 메시지가 3초 이내 응답 |
| P99 응답 시간 | < 5초 | 99%의 메시지가 5초 이내 응답 |
| 메시지 순서 보장률 | ≥ 99% | 메시지 순서가 99% 이상 보장 |
| HTTP 에러율 | < 10% | 전체 요청의 10% 미만 에러 |

### ⚠️ 경고 (개선 필요)

| 지표 | 경고 기준 | 조치 사항 |
|------|-----------|----------|
| P95 응답 시간 | 3초 ~ 5초 | 캐싱, 비동기 처리 검토 |
| 메시지 전달 성공률 | 85% ~ 90% | WebSocket 안정성 개선 |
| 순서 보장률 | 95% ~ 99% | 메시지 큐 순서 처리 개선 |

### ❌ 불합격 (즉시 개선 필요)

| 지표 | 불합격 기준 | 조치 사항 |
|------|------------|----------|
| P95 응답 시간 | > 5초 | 시스템 아키텍처 재설계 필요 |
| 메시지 전달 성공률 | < 85% | 시스템 안정성 문제, 즉시 개선 |
| 순서 보장률 | < 95% | 메시지 처리 로직 재검토 |
| HTTP 에러율 | > 20% | 서버 리소스 부족 또는 버그 |

## 🔧 성능 개선 방안

### 현재 시스템 (개선 전)

```
사용자 → Spring Boot → MySQL → WebSocket
         (동기 처리)
```

**예상 문제점:**
- P95 응답 시간: **10초 이상**
- 메시지 전달 성공률: **50% 이하**
- 순서 보장률: **70% 이하**

### 개선안 1: Redis 캐싱 적용

```
사용자 → Spring Boot → Redis Cache → MySQL
                     ↓
                  WebSocket
```

**예상 효과:**
- P95 응답 시간: **3초 이내**
- 메시지 전달 성공률: **85% 이상**

### 개선안 2: Kafka + Redis Pub/Sub

```
사용자 → Spring Boot → Kafka → Consumer
                     ↓
                  Redis Pub/Sub → WebSocket
```

**예상 효과:**
- P95 응답 시간: **1초 이내**
- 메시지 전달 성공률: **95% 이상**
- 순서 보장률: **99.9% 이상**

### 개선안 3: 완전한 분산 아키텍처

```
사용자 → Load Balancer → Spring Boot 클러스터
                              ↓
                         Kafka Cluster
                              ↓
                    Redis Cluster (Pub/Sub)
                              ↓
                    WebSocket 서버 클러스터
```

**예상 효과:**
- P95 응답 시간: **500ms 이내**
- 메시지 전달 성공률: **99% 이상**
- 순서 보장률: **99.99% 이상**
- 동시 접속 지원: **100만명 이상**

## 🐛 문제 해결 (Troubleshooting)

### 문제 1: "connection refused" 에러

```bash
# 백엔드 서버 상태 확인
docker ps | grep backend
docker logs boot-container

# 서버 재시작
docker-compose restart backend
```

### 문제 2: InfluxDB 연결 실패

```bash
# InfluxDB 상태 확인
docker ps | grep influxdb
docker logs k6-influxdb

# InfluxDB 재시작
docker-compose -f docker-compose.monitoring.yml restart influxdb
```

### 문제 3: 메모리 부족

```bash
# Docker 리소스 증가
# Docker Desktop > Settings > Resources
# - CPU: 4 cores 이상
# - Memory: 8GB 이상
# - Swap: 2GB 이상
```

### 문제 4: K6 설치 실패

```bash
# Docker로 대체 실행
docker run --rm -i grafana/k6:latest run - <performance-tests/massive-chat-load-test.js
```

## 📝 테스트 체크리스트

### 테스트 전

- [ ] Docker 설치 확인
- [ ] K6 설치 확인
- [ ] MySQL 서버 실행 중
- [ ] 테스트 사용자 10만명 생성 완료
- [ ] 백엔드 서버 정상 실행 (http://localhost:8080/api/health)
- [ ] 모니터링 환경 실행 (InfluxDB + Grafana)
- [ ] Grafana 대시보드 접속 확인 (http://localhost:3000)

### 테스트 중

- [ ] Grafana 대시보드 실시간 모니터링
- [ ] 콘솔 로그 확인 (에러 발생 여부)
- [ ] 서버 리소스 모니터링 (CPU, 메모리)
- [ ] 네트워크 상태 확인

### 테스트 후

- [ ] HTML 리포트 저장
- [ ] JSON 결과 저장
- [ ] Grafana 대시보드 스크린샷
- [ ] 성능 지표 분석
- [ ] 개선사항 문서화

## 📚 참고 자료

- [K6 공식 문서](https://k6.io/docs/)
- [InfluxDB 문서](https://docs.influxdata.com/)
- [Grafana 문서](https://grafana.com/docs/)
- [WebSocket 성능 최적화](https://www.nginx.com/blog/websocket-nginx/)

## 🤝 지원

문제가 발생하면:
1. 콘솔 로그 확인
2. Docker 로그 확인 (`docker logs <container-name>`)
3. Grafana 대시보드에서 메트릭 확인
4. 이슈 트래커에 문의

---

**작성일:** 2025년 1월  
**버전:** 1.0.0  
**작성자:** CoreConnect 개발팀







