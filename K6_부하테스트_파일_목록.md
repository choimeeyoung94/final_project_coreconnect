# 📋 K6 대규모 채팅 부하 테스트 - 전체 파일 목록

## ✅ 생성된 파일 및 디렉토리

### 📊 메인 테스트 스크립트
```
performance-tests/
└── massive-chat-load-test.js           # K6 메인 테스트 스크립트
    ├── 시나리오 1: 10만명 동시 메시지 전송
    ├── 시나리오 2: 메시지 조회 성능 테스트
    ├── 커스텀 메트릭: message_response_time, message_query_time 등
    └── HTML/JSON 리포트 자동 생성
```

추가 테스트(멀티 Pod 팬아웃 확인용):
```
performance-tests/
└── k6-stomp-fanout-test.js             # STOMP 팬아웃(브로드캐스트) 전달률 확인용 (브로커 없음 한계 확인)
```

### 🐳 Docker 구성 파일
```
docker-compose.monitoring.yml            # 모니터링 환경 (InfluxDB + Grafana + K6)
├── InfluxDB (포트 8086)                  # 시계열 DB
├── Grafana (포트 3000)                   # 대시보드
└── K6 (선택적 실행)                      # 테스트 러너
```

### 📊 Grafana 설정
```
grafana/
├── provisioning/
│   ├── datasources/
│   │   └── influxdb.yml                 # InfluxDB 데이터소스 자동 설정
│   └── dashboards/
│       └── dashboard.yml                # 대시보드 프로비저닝
└── dashboards/
    └── k6-massive-chat-dashboard.json   # K6 전용 대시보드
        ├── 메시지 응답 시간 패널
        ├── 메시지 전달 성공률 게이지
        ├── 활성 사용자 수 그래프
        ├── RPS (초당 요청 수)
        ├── HTTP 응답 시간 (P50, P95, P99)
        ├── 메시지 전송/수신 카운터
        ├── 순서 위반 건수
        └── 메시지 조회 시간
```

### 🚀 실행 스크립트
```
run-massive-chat-test.bat               # Windows 실행 스크립트
├── 대화형 메뉴
├── 자동 환경 체크
├── 백엔드 서버 상태 확인
└── HTML 리포트 자동 열기

run-massive-chat-test.sh                # Linux/Mac 실행 스크립트
├── 색상 출력 지원
├── Docker K6 fallback
├── 자동 브라우저 열기
└── 상세 오류 처리
```

### 💾 데이터베이스 스크립트
```
테스트_사용자_10만명_생성.sql            # 테스트 사용자 생성 SQL
├── CreateTestUsers() 프로시저
├── 배치 처리 (1,000명 단위)
├── 진행률 표시
├── 옵션:
│   ├── 1,000명 (소규모)
│   ├── 10,000명 (중간)
│   └── 100,000명 (대규모)
└── 정리 쿼리 포함
```

### 📚 문서
```
K6_10만명_동시접속_테스트_가이드.md      # 완전한 가이드 (30+ 페이지)
├── 사전 준비
├── 설치 가이드
├── 테스트 실행 방법
├── 결과 해석
├── 성능 기준
├── 개선 방안
└── 문제 해결

QUICKSTART_MASSIVE_CHAT_TEST.md        # 빠른 시작 가이드 (5분)
├── 3단계 빠른 시작
├── 체크리스트
├── 결과 해석
└── 빠른 문제 해결

performance-tests/README_MASSIVE_CHAT_TEST.md  # 퍼포먼스 테스트 README
└── 파일 구조 및 빠른 참조

performance-tests/README_STOMP_FANOUT_TEST.md # STOMP 팬아웃 테스트 실행/해석 가이드
└── 멀티 Pod + 공유 브로커 없음 상황에서 전달률이 왜 떨어지는지 포함

K6_부하테스트_파일_목록.md              # 이 파일
└── 전체 파일 목록 및 사용법
```

## 🎯 사용 시나리오별 파일

### 시나리오 1: "빠르게 테스트만 실행하고 싶어요"

**사용할 파일:**
1. `QUICKSTART_MASSIVE_CHAT_TEST.md` 읽기
2. `run-massive-chat-test.bat` (또는 `.sh`) 실행

**소요 시간:** 5-10분

### 시나리오 2: "상세한 설정을 이해하고 싶어요"

**사용할 파일:**
1. `K6_10만명_동시접속_테스트_가이드.md` 정독
2. `performance-tests/massive-chat-load-test.js` 코드 분석
3. `grafana/dashboards/k6-massive-chat-dashboard.json` 대시보드 커스터마이징

**소요 시간:** 30-60분

### 시나리오 3: "커스텀 테스트를 만들고 싶어요"

**수정할 파일:**
1. `performance-tests/massive-chat-load-test.js`
   - `options` 섹션에서 시나리오 수정
   - 커스텀 메트릭 추가
2. `grafana/dashboards/k6-massive-chat-dashboard.json`
   - 새 패널 추가

**소요 시간:** 1-2시간

### 시나리오 4: "CI/CD에 통합하고 싶어요"

**사용할 파일:**
1. `docker-compose.monitoring.yml`
2. `performance-tests/massive-chat-load-test.js`

**예제:**
```yaml
# .github/workflows/performance-test.yml
name: Performance Test
on: [push]
jobs:
  load-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Start monitoring
        run: docker-compose -f docker-compose.monitoring.yml up -d
      - name: Run K6 test
        run: |
          docker run --rm -i \
            --network host \
            -e K6_OUT=influxdb=http://localhost:8086/k6 \
            -v "$(pwd)/performance-tests:/scripts" \
            grafana/k6:latest run /scripts/massive-chat-load-test.js
```

## 📊 파일 크기 및 특징

| 파일 | 크기 | 특징 |
|------|------|------|
| `massive-chat-load-test.js` | ~15KB | 완전한 K6 테스트 스크립트 |
| `k6-massive-chat-dashboard.json` | ~8KB | Grafana 대시보드 JSON |
| `docker-compose.monitoring.yml` | ~2KB | Docker Compose 설정 |
| `테스트_사용자_10만명_생성.sql` | ~5KB | SQL 프로시저 |
| `K6_10만명_동시접속_테스트_가이드.md` | ~20KB | 완전한 문서 |

## 🔧 커스터마이징 가이드

### 1. 테스트 시나리오 수정

**파일:** `performance-tests/massive-chat-load-test.js`

**수정 예시:**
```javascript
// 옵션 섹션에서 수정
export const options = {
  scenarios: {
    massive_concurrent_chat: {
      executor: 'ramping-vus',
      startVUs: 0,
      stages: [
        { duration: '2m', target: 50000 },  // 5만명으로 변경
        { duration: '5m', target: 50000 },
        { duration: '1m', target: 0 }
      ],
      // ...
    }
  }
};
```

### 2. 새 메트릭 추가

**파일:** `performance-tests/massive-chat-load-test.js`

**추가 예시:**
```javascript
// 새 메트릭 정의
const customMetric = new Trend('custom_metric', true);

// 시나리오에서 사용
export function massiveChatScenario() {
  const startTime = Date.now();
  // ... 작업 수행 ...
  const duration = Date.now() - startTime;
  customMetric.add(duration);
}
```

### 3. Grafana 대시보드 패널 추가

**파일:** `grafana/dashboards/k6-massive-chat-dashboard.json`

1. Grafana UI에서 패널 추가
2. JSON 모델 복사
3. `panels` 배열에 추가

### 4. 환경별 설정

**파일:** `run-massive-chat-test.sh` 또는 `.bat`

```bash
# 개발 환경
export BASE_URL=http://localhost:8080
export TOTAL_USERS=1000

# 스테이징 환경
export BASE_URL=https://staging.example.com
export TOTAL_USERS=10000

# 프로덕션 환경
export BASE_URL=https://api.example.com
export TOTAL_USERS=100000
```

## 🎓 학습 경로

### 초급: K6 기초 이해
1. `QUICKSTART_MASSIVE_CHAT_TEST.md` 읽기
2. 소규모 테스트 (1,000명) 실행
3. Grafana 대시보드 관찰

### 중급: 커스터마이징
1. `massive-chat-load-test.js` 코드 분석
2. 테스트 시나리오 수정
3. 새 메트릭 추가

### 고급: 프로덕션 적용
1. CI/CD 통합
2. 알림 설정 (Slack, Email)
3. 자동화된 성능 회귀 테스트

## 📈 메트릭 참조

### 기본 HTTP 메트릭 (K6 제공)
- `http_req_duration` - HTTP 요청 응답 시간
- `http_req_failed` - HTTP 요청 실패율
- `http_reqs` - 총 HTTP 요청 수
- `vus` - 활성 가상 사용자 수

### 커스텀 메트릭 (이 프로젝트)
- `message_response_time` - 메시지 전송 응답 시간
- `message_query_time` - 메시지 조회 시간
- `message_order_violations` - 순서 위반 건수
- `messages_sent` - 전송된 메시지 수
- `messages_received` - 수신된 메시지 수
- `login_success_rate` - 로그인 성공률
- `message_delivery_rate` - 메시지 전달 성공률
- `websocket_connections` - WebSocket 연결 수

## 🔗 관련 파일

### 기존 프로젝트 파일
```
backend/src/main/java/com/goodee/coreconnect/
├── chat/controller/ChatMessageController.java      # 채팅 API
├── chat/service/ChatRoomService.java               # 채팅 서비스
└── auth/controller/AuthController.java             # 인증 API

frontend/src/features/chat/
├── api/chatSocket.js                                # WebSocket 클라이언트
└── components/ChatMessageList.jsx                   # 채팅 UI
```

### 연관 문서
```
대규모_트래픽_아키텍처_개선안.md                      # 아키텍처 개선안
채팅_부하테스트_결과_분석.md                          # 이전 부하 테스트 결과
K6_부하테스트_결과_분석.md                            # K6 결과 분석
```

## 💡 유용한 명령어

### Docker 관련
```bash
# 모니터링 환경 시작
docker-compose -f docker-compose.monitoring.yml up -d

# 모니터링 환경 중지
docker-compose -f docker-compose.monitoring.yml down

# 로그 확인
docker logs k6-influxdb -f
docker logs k6-grafana -f

# 데이터 초기화
docker-compose -f docker-compose.monitoring.yml down -v
```

### K6 관련
```bash
# 로컬 실행
k6 run performance-tests/massive-chat-load-test.js

# InfluxDB 출력
k6 run --out influxdb=http://localhost:8086/k6 performance-tests/massive-chat-load-test.js

# 환경 변수 전달
k6 run -e TOTAL_USERS=1000 performance-tests/massive-chat-load-test.js

# Docker로 실행
docker run --rm -i grafana/k6:latest run - <performance-tests/massive-chat-load-test.js
```

### MySQL 관련
```bash
# 테스트 사용자 수 확인
mysql -u root -p -e "SELECT COUNT(*) FROM users WHERE email LIKE 'testuser%@test.com';" coreconnect

# 테스트 메시지 수 확인
mysql -u root -p -e "SELECT COUNT(*) FROM chat WHERE chat_room_id = 1;" coreconnect

# 테스트 데이터 정리
mysql -u root -p -e "DELETE FROM users WHERE email LIKE 'testuser%@test.com';" coreconnect
```

## 🌟 베스트 프랙티스

### 1. 점진적 부하 증가
```
1,000명 → 10,000명 → 100,000명
```

### 2. 충분한 준비 시간
- 데이터베이스 인덱스 확인
- 서버 리소스 모니터링 준비
- 백업 계획

### 3. 반복 테스트
- 최소 3회 반복
- 결과 평균 및 표준편차 계산
- 이상치(outlier) 분석

### 4. 문서화
- 각 테스트의 조건 기록
- 스크린샷 및 그래프 저장
- 개선 전후 비교

## ✅ 최종 체크리스트

테스트 실행 전:
- [ ] `docker-compose.monitoring.yml` 파일 존재 확인
- [ ] `performance-tests/massive-chat-load-test.js` 파일 존재 확인
- [ ] `grafana/` 디렉토리 구조 확인
- [ ] `테스트_사용자_10만명_생성.sql` 파일 존재 확인
- [ ] 실행 스크립트 (`.bat` 또는 `.sh`) 존재 확인
- [ ] 가이드 문서들 확인

테스트 실행 후:
- [ ] `summary.html` 생성 확인
- [ ] `summary.json` 생성 확인
- [ ] Grafana 스크린샷 저장
- [ ] 결과 분석 문서 작성

---

**파일 생성일:** 2025년 1월  
**버전:** 1.0.0  
**총 파일 수:** 11개  
**총 라인 수:** ~3,500 라인  
**예상 테스트 시간:** 5분 (소규모) ~ 20분 (대규모)

