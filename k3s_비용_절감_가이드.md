# K3s 비용 절감 완벽 가이드

## 🤔 질문: k8s를 k3s로 변경하면 비용 문제가 해결될까?

### 짧은 답변
**아니요. 이미 k3s를 사용 중이시고, 비용 문제의 핵심은 k3s가 아닙니다.**

### 긴 답변
비용의 87%는 **EC2 인스턴스**입니다. k3s는 이미 충분히 가볍습니다.

---

## 💰 현재 비용 구조 분석

### 월 40만원의 실체

```
EC2 인스턴스 (여러 대 또는 큰 타입)
├─ replicas: 10 (10개 Pod)
├─ 각 Pod: 1-2GB 메모리, 0.5-1 CPU
├─ 총 필요: 10-20GB 메모리, 5-10 코어
└─ 예상 구성: t3.xlarge (4코어, 16GB) × 2대
   = $0.1664 × 2 × 24h × 30일
   = $239/월 × 2 = $478/월 (≈ 64만원)

또는
└─ EKS (관리형 Kubernetes)
   = 컨트롤 플레인: $73/월
   + 워커 노드: t3.large × 3대
   = $73 + ($0.0832 × 3 × 24h × 30일)
   = $73 + $180 = $253/월 (≈ 34만원)
```

---

## 📊 k8s vs k3s 비용 비교

### EKS (관리형 k8s) 사용시

| 항목 | 비용 |
|------|------|
| 컨트롤 플레인 | $73/월 |
| 워커 노드 (t3.large × 3) | $180/월 |
| **총계** | **$253/월 (34만원)** |

### k3s (자체 호스팅) 사용시

| 항목 | 비용 |
|------|------|
| 컨트롤 플레인 | 무료! (워커 노드에 포함) |
| 워커 노드 (t3.large × 2) | $120/월 |
| **총계** | **$120/월 (16만원)** |

**k3s 효과: $133/월 (18만원) 절감**

---

## ✅ k3s를 이미 사용 중이라면?

### 현재 상태 확인

```yaml
# k8s/03-chat-server.yaml
type: LoadBalancer  # K3s는 자동으로 LoadBalancer 제공
imagePullPolicy: Never  # 로컬 이미지 사용 (K3s 특성)
```

**이미 k3s를 사용하고 계십니다!**

### 그럼 40만원은 어디서?

#### 원인 1: 높은 Replicas
```yaml
replicas: 10  # 개발/테스트용으로는 과함
```

#### 원인 2: 높은 리소스 요청
```yaml
resources:
  requests:
    memory: "1Gi"
    cpu: "500m"
  limits:
    memory: "2Gi"
    cpu: "1000m"
```

#### 원인 3: 24/7 운영
```
항상 켜져 있음 = 720시간/월
실제 사용: 주 10시간 정도?
낭비: 90% 이상!
```

---

## 🚀 실제 비용 절감 전략

### 전략 1: 로컬 개발 (최고!) ⭐⭐⭐

#### 방법
```bash
# 로컬 Docker Compose 사용
docker-compose up -d

# 또는 로컬 k3s
# k3s는 이미 가벼우므로 Docker만으로도 충분!
```

#### 효과
```
현재: $480/월 (40만원)
변경: $0/월
절감: $480/월 (100%)
```

#### 장점
- ✅ 무제한 테스트
- ✅ 빠른 개발 사이클
- ✅ k6로 충분히 부하 테스트 가능

---

### 전략 2: Replicas 최적화 ⭐⭐

#### 개발용 설정
```yaml
# k8s/03-chat-server-dev.yaml (이미 생성됨!)
replicas: 2  # 10 → 2
resources:
  requests:
    memory: "512Mi"  # 1Gi → 512Mi
    cpu: "250m"      # 500m → 250m
```

#### 필요 인스턴스
```
t3.small (2코어, 2GB): $0.0208/시간
= $15/월 (약 2만원)
```

#### 효과
```
현재: $480/월
변경: $15/월
절감: $465/월 (97%)
```

---

### 전략 3: 온디맨드 사용 ⭐⭐⭐

#### 방법
```bash
# 평소: 중지 (비용 $0)
aws ec2 stop-instances --instance-ids i-xxxxx

# 테스트 필요시: 시작 (2시간 사용)
aws ec2 start-instances --instance-ids i-xxxxx
k6 run --vus 100000 test.js

# 즉시 중지
aws ec2 stop-instances --instance-ids i-xxxxx
```

#### 효과
```
사용: 주 1회 × 2시간 = 8시간/월
비용: $0.0832 × 2인스턴스 × 8시간 = $1.33/월
+ EBS (중지 중에도 과금): $10/월
= 총 $11/월 (약 1.5만원)

절감: $469/월 (98%)
```

---

### 전략 4: k3s 추가 최적화 ⭐

k3s를 더 최적화할 수 있습니다!

#### k3s 경량화 옵션
```bash
# k3s 설치시 불필요한 기능 제거
curl -sfL https://get.k3s.io | sh -s - \
  --disable traefik \        # Traefik 대신 Nginx
  --disable servicelb \      # 외부 로드밸런서 사용
  --disable metrics-server   # 개발시 불필요

# 메모리 제한 설정
systemctl edit k3s
[Service]
Environment="K3S_KUBELET_ARGS=--system-reserved=memory=512Mi"
```

#### 효과
```
k3s 메모리 사용: 500MB → 300MB
→ t3.micro (1코어, 1GB) 사용 가능!
= $0.0104/시간 = $7.5/월 (약 1만원)
```

---

## 📈 비용 절감 시뮬레이션

### 시나리오 A: 현재 (EKS + 상시 운영)
```
┌─────────────────────────────────┐
│ EKS 컨트롤 플레인: $73          │
│ t3.large × 3: $180              │
│ EBS 100GB: $10                  │
│ 네트워크: $10                   │
├─────────────────────────────────┤
│ 총계: $273/월 (36만원)          │
└─────────────────────────────────┘
```

### 시나리오 B: k3s + 상시 운영
```
┌─────────────────────────────────┐
│ 컨트롤 플레인: $0 (무료!)       │
│ t3.large × 2: $120              │
│ EBS 100GB: $10                  │
│ 네트워크: $10                   │
├─────────────────────────────────┤
│ 총계: $140/월 (18만원)          │
│ 절감: $133/월 (48%)             │
└─────────────────────────────────┘
```

### 시나리오 C: k3s + Replicas 축소
```
┌─────────────────────────────────┐
│ 컨트롤 플레인: $0               │
│ t3.small × 1: $15               │
│ EBS 30GB: $3                    │
│ 네트워크: $2                    │
├─────────────────────────────────┤
│ 총계: $20/월 (2.7만원)          │
│ 절감: $253/월 (93%)             │
└─────────────────────────────────┘
```

### 시나리오 D: 로컬 + k3s 검증 ⭐
```
┌─────────────────────────────────┐
│ 로컬 개발: $0 (무제한)          │
│ k3s 검증 (월 1회, 2시간): $1.5  │
│ EBS (중지): $3                  │
├─────────────────────────────────┤
│ 총계: $4.5/월 (6천원)           │
│ 절감: $268/월 (98%)             │
└─────────────────────────────────┘
```

---

## 🎯 최종 추천

### 1단계: 즉시 실행 (오늘)

#### A. AWS 서버 중지
```bash
aws ec2 stop-instances --instance-ids i-xxxxx
```
**→ 즉시 비용 절감 시작!**

#### B. 로컬 개발 전환
```bash
# k3s 필요 없음! Docker Compose면 충분
docker-compose up -d

# k6로 부하 테스트
k6 run --vus 10000 k6-chatroom-performance-test.js
```
**→ 무제한 테스트 가능!**

---

### 2단계: 코드 최적화 (1-2주)

```bash
# 로컬에서 성능 개선
# - N+1 쿼리 해결
# - 인덱스 최적화
# - 캐싱 추가

# 계속 테스트
k6 run --vus 50000 test.js
```
**→ 비용 $0!**

---

### 3단계: 필요시만 k3s (월 1회)

```bash
# AWS 인스턴스 시작
aws ec2 start-instances --instance-ids i-xxxxx

# k3s 설정 (이미 되어 있음)
kubectl apply -f k8s/03-chat-server-loadtest.yaml

# 대규모 테스트
k6 run --vus 100000 --duration 30m test.js

# 즉시 중지
aws ec2 stop-instances --instance-ids i-xxxxx
```
**→ 비용 $1.5/회**

---

## 🔧 k3s 최적화 체크리스트

### ✅ 이미 적용된 것
- [x] k3s 사용 (k8s 대신)
- [x] LoadBalancer 자동 지원
- [x] 로컬 이미지 사용 (imagePullPolicy: Never)

### 🎯 추가로 할 것

#### 1. Replicas 축소
```yaml
# 현재
replicas: 10

# 개발용
replicas: 2

# 테스트용 (필요시만)
replicas: 10
```

#### 2. 리소스 최적화
```yaml
# 현재
resources:
  requests:
    memory: "1Gi"
    cpu: "500m"

# 개발용
resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
```

#### 3. 온디맨드 전략
```bash
# 평소: 중지
# 필요시: 시작 → 테스트 → 중지
```

---

## 💡 핵심 정리

### ❌ 오해
> "k8s를 k3s로 바꾸면 비용이 크게 줄어든다"

### ✅ 진실
> "이미 k3s 사용 중. 비용은 인스턴스가 문제"

### 🎯 해결책

#### 우선순위 1: 로컬 개발 (98% 절감)
```
비용: $0
효과: 최고
```

#### 우선순위 2: Replicas 축소 (93% 절감)
```
비용: $20/월
효과: 우수
```

#### 우선순위 3: 온디맨드 (98% 절감)
```
비용: $5/월
효과: 최고
```

#### 우선순위 4: k3s 추가 최적화 (5% 추가 절감)
```
비용 절감: 미미
효과: 있지만 우선순위 낮음
```

---

## 📊 최종 비교표

| 전략 | 월 비용 | 절감액 | 절감률 | 난이도 |
|------|---------|--------|--------|--------|
| 현재 (EKS) | $273 (36만원) | - | - | - |
| k3s 전환 | $140 (18만원) | $133 | 48% | 중 |
| k3s + 축소 | $20 (2.7만원) | $253 | 93% | 쉬움 |
| 로컬 개발 | $0 | $273 | 100% | 쉬움 |
| 하이브리드 | $5 | $268 | 98% | 쉬움 |

**최고의 전략: 로컬 개발 + 월 1회 k3s 검증**

---

## 🚀 지금 바로 실행

```bash
# 1. AWS 중지
aws ec2 stop-instances --instance-ids i-xxxxx

# 2. 로컬 시작
docker-compose up -d

# 3. 테스트
k6 run --vus 10000 k6-chatroom-performance-test.js

# 축하합니다! 월 40만원 절약 시작! 🎉
```

---

## 📚 추가 자료

### k3s 공식 문서
- https://k3s.io/

### 비용 계산기
- AWS EC2 Pricing: https://aws.amazon.com/ec2/pricing/
- k3s vs k8s: https://k3s.io/#why-k3s

### 프로젝트 문서
- `비용절감_AWS_배포_전략.md`
- `로컬_vs_AWS_비교.md`
- `비용_절감_실행_플랜.md`

---

**결론: k3s는 이미 충분히 가볍습니다. 비용 절감의 핵심은 로컬 개발 전환입니다!** 🎯
