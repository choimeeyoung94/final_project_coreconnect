# k6 부하 테스트 가이드

## 📋 테스트 개요

이 테스트는 채팅방 목록/최신 메시지 조회 API의 성능을 측정합니다.

### 측정 항목
- **P95 Latency**: 95번째 백분위수 응답 시간 (목표: < 500ms)
- **P99 Latency**: 99번째 백분위수 응답 시간 (목표: < 1000ms)
- **RPS**: 초당 요청 처리량 (목표: 10~15+)
- **에러율**: 실패한 요청 비율 (목표: < 1%)

### 테스트 시나리오
1. **Warmup** (2분): 0→5 VU, 시스템 준비
2. **Main** (10분): 점진적 부하 증가 (5→10→15→20 RPS)
3. **Spike** (1분 20초): 급격한 부하 (30 VU)

---

## 🚀 실행 방법

### 1. 환경 변수 설정

```bash
# k6 Cloud 설정 (필수)
export K6_CLOUD_PROJECT_ID=6156169
export K6_CLOUD_TOKEN=YOUR_TOKEN

# 서버 설정 (기본값 있음)
export BASE_URL=http://54.116.26.182:8080
export USER_EMAIL=admin@coreconnect.io.kr
export USER_PASSWORD=1
```

### 2. k6 Cloud 로그인 (토큰이 없는 경우)

```bash
k6 login cloud
# 브라우저에서 로그인 후 토큰이 자동 설정됩니다
```

### 3. 테스트 실행

#### 방법 A: 쉘 스크립트 사용 (권장)
```bash
chmod +x run-k6-cloud-test.sh
./run-k6-cloud-test.sh
```

#### 방법 B: 직접 실행
```bash
k6 run --out cloud k6-chatroom-performance-test.js
```

---

## 📊 결과 확인

### k6 Cloud 대시보드
테스트 실행 후 출력되는 URL로 접속:
```
https://app.k6.io/projects/6156169
```

### Grafana에서 확인할 지표

#### 1. 응답 시간 (Response Time)
- `chat_latency_ms` (P95, P99, Avg)
- `http_req_duration` (P95, P99)
- `http_req_waiting` (P95)

#### 2. 처리량 (Throughput)
- `http_reqs` (초당 요청 수)
- `iterations` (VU 반복 횟수)
- `data_received` (수신 데이터량)

#### 3. 에러율 (Error Rate)
- `http_req_failed` (HTTP 실패율)
- `chat_fail` (채팅 API 실패율)
- `login_fail` (로그인 실패율)
- `error_rate` (전체 에러율)

#### 4. 동시 사용자 (Virtual Users)
- `vus` (현재 VU 수)
- `vus_max` (최대 VU 수)

---

## 🎯 성능 목표 (Thresholds)

테스트 스크립트에 정의된 성능 목표:

| 메트릭 | 목표 | 현재 (개선 전) | 상태 |
|--------|------|---------------|------|
| `chat_latency_ms` P95 | < 500ms | ~6.3s | ❌ |
| `chat_latency_ms` P99 | < 1000ms | ~7.4s | ❌ |
| `chat_latency_ms` Avg | < 300ms | ~5s | ❌ |
| `http_req_failed` | < 1% | 0% | ✅ |
| `chat_fail` | < 1% | 0% | ✅ |
| `error_rate` | < 1% | 0% | ✅ |

---

## 📈 개선 전/후 비교

### Grafana "Compare with" 기능 사용

1. k6 Cloud 대시보드에서 테스트 결과 선택
2. 우측 상단 "Compare with" 버튼 클릭
3. 개선 전 테스트 결과 선택
4. 지표 비교:
   - P95/P99 Latency 감소율
   - RPS 증가율
   - 에러율 변화

### 예상 개선 효과

| 지표 | 개선 전 | 개선 후 (목표) | 개선율 |
|------|---------|---------------|--------|
| P95 Latency | 6.3s | < 0.5s | **92% 감소** |
| P99 Latency | 7.4s | < 1s | **86% 감소** |
| RPS | 5.3 | 10~15 | **2~3배 증가** |
| 에러율 | 0% | < 1% | **유지** |

---

## 🔍 문제 해결

### 1. "connection refused" 에러

```bash
# 서버 상태 확인
curl http://54.116.26.182:8080/actuator/health

# Docker 컨테이너 상태 확인
docker-compose ps

# 로그 확인
docker-compose logs -f backend
```

### 2. "login failed" 에러

```bash
# 로그인 수동 테스트
curl -X POST http://54.116.26.182:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@coreconnect.io.kr","password":"1"}' \
  -v
```

### 3. k6 Cloud 토큰 에러

```bash
# 다시 로그인
k6 login cloud

# 토큰 수동 설정
export K6_CLOUD_TOKEN=YOUR_TOKEN
```

### 4. Threshold 실패

- ❌ Threshold 실패는 **성능 목표 미달**을 의미 (테스트 자체는 성공)
- 📊 Grafana에서 병목 구간 분석 필요
- 🔧 추가 최적화 검토 (Redis 캐시, Read Replica 등)

---

## 📝 커스텀 메트릭

스크립트에서 수집하는 커스텀 메트릭:

| 메트릭 | 타입 | 설명 |
|--------|------|------|
| `chat_latency_ms` | Trend | 채팅방 조회 응답 시간 |
| `login_latency_ms` | Trend | 로그인 응답 시간 |
| `login_fail` | Rate | 로그인 실패율 |
| `chat_fail` | Rate | 채팅 조회 실패율 |
| `error_rate` | Rate | 전체 에러율 |
| `total_requests` | Counter | 총 요청 수 |
| `successful_requests` | Counter | 성공 요청 수 |

---

## 🎨 Grafana 대시보드 추천 패널

### 1. 응답 시간 추이
- `chat_latency_ms` (P95, P99, Avg) 시계열 그래프
- Threshold 라인 추가 (500ms, 1000ms)

### 2. 에러율 추이
- `error_rate`, `chat_fail`, `login_fail` 스택 그래프

### 3. RPS & VUs
- `http_reqs` (RPS)
- `vus` (동시 사용자)

### 4. HTTP 상태 코드 분포
- `http_req_failed` by status code

---

## 🔗 관련 문서

- [성능 개선 요약 (AS-IS/TO-BE)](./성능_개선_요약_AS-IS_TO-BE.md)
- [모니터링 가이드](./성능_모니터링_및_측정_가이드.md)
- [DB 인덱스 스크립트](./채팅_테이블_컬럼_추가_스크립트.sql)

---

## 📞 문의

- 테스트 결과 이상 시: Slow Query Log 및 EXPLAIN 결과 공유
- Threshold 미달 시: Grafana 스크린샷 및 HikariCP/Tomcat 지표 공유
- 추가 최적화 검토 시: Redis 캐시 또는 Read Replica 분리 검토

**마지막 업데이트**: 2025-12-20

