# 🎯 기존 coreconnect 서버 사용 가이드

## ✅ 사용 가능한 서버

### 서버 1: coreconnect-k6-server
```
IP: 15.165.50.43
타입: t3.xlarge (4 vCPU, 16GB RAM)
상태: ✅ 실행 중
용도: 개발/테스트
```

### 서버 2: coreconnect-k6-server2 ⭐ 추천!
```
IP: 3.38.28.172
타입: t3.2xlarge (8 vCPU, 32GB RAM)
상태: ✅ 실행 중
용도: 프로덕션/대규모 테스트
```

---

## 🚀 5분 빠른 배포

### 방법 1: 자동 스크립트 (추천!)

```batch
기존_서버_배포.bat
```

**입력 사항:**
1. 서버 선택 (1 또는 2)
2. SSH 키 파일 경로

**→ 자동으로 배포됩니다!**

---

### 방법 2: 수동 배포

#### Step 1: SSH 접속

**서버 1 사용:**
```bash
ssh -i your-key.pem ec2-user@15.165.50.43
```

**서버 2 사용:** (추천!)
```bash
ssh -i your-key.pem ec2-user@3.38.28.172
```

#### Step 2: k3s 확인

```bash
# k3s 상태 확인
sudo systemctl status k3s

# 없으면 설치 (1분)
curl -sfL https://get.k3s.io | sh -
sleep 30

# 확인
sudo kubectl get nodes
```

#### Step 3: 저장소 업데이트

```bash
# 저장소가 있으면
cd final_project_coreconnect
git pull

# 없으면 클론
git clone https://github.com/your-username/final_project_coreconnect.git
cd final_project_coreconnect
```

#### Step 4: 이미지 빌드

```bash
cd backend
sudo docker build -t chat-server:latest .
cd ..
```

#### Step 5: 배포

```bash
# Namespace
sudo kubectl apply -f k8s/00-namespace.yaml

# MySQL
sudo kubectl apply -f k8s/01-mysql.yaml
sleep 60

# Redis
sudo kubectl apply -f k8s/02-redis.yaml
sleep 30

# Chat Server
sudo kubectl apply -f k8s/03-chat-server-dev.yaml
sleep 30

# 확인
sudo kubectl get pods -n chat-system
```

---

## 📊 서버 비교

| 항목 | Server 1 | Server 2 | 추천 |
|------|----------|----------|------|
| IP | 15.165.50.43 | 3.38.28.172 | - |
| 타입 | t3.xlarge | t3.2xlarge | ⭐ Server 2 |
| vCPU | 4 | 8 | ⭐ Server 2 |
| RAM | 16GB | 32GB | ⭐ Server 2 |
| 용도 | 개발/테스트 | 프로덕션 | ⭐ Server 2 |
| 10만 VU | ⚠️ 가능하나 부담 | ✅ 여유 | ⭐ Server 2 |

**추천: Server 2 (3.38.28.172)** - 더 강력하고 안정적!

---

## 💰 비용 (이미 실행 중)

### 현재 비용 (추정)

**coreconnect-k6-server (t3.xlarge):**
```
시간당: $0.1664
월 (24/7): $120
```

**coreconnect-k6-server2 (t3.2xlarge):**
```
시간당: $0.3328
월 (24/7): $240
```

**총 월 비용: ~$360 (약 48만원)**

### 💡 비용 절감 방법

#### 방법 1: 하나만 사용
```
Server 2만 사용 → $240/월 (32만원)
절감: $120/월 (16만원)
```

#### 방법 2: 필요시에만 시작
```
개발: 로컬
배포 검증: Server 2 (월 10시간)
= $3.3/월

절감: $357/월 (47.7만원)
```

---

## 🎯 추천 전략

### 1. 즉시 배포: Server 2 사용

```
- 이미 실행 중
- 가장 고사양
- 10만 VU 여유 있게 처리
- 바로 배포 가능!
```

### 2. Server 1은 중지

```
# AWS Console에서 Server 1 중지
# 또는
aws ec2 stop-instances --instance-ids i-0f7f1bd3e93c4cd9d

# 월 $120 절감!
```

### 3. 개발은 로컬

```
- 일상적인 개발/테스트: 로컬 Docker
- 최종 배포/검증: Server 2
- 비용 추가 절감 가능
```

---

## ✅ 배포 확인

### Health Check

```bash
# 서버에서
curl http://localhost/actuator/health

# 로컬에서
curl http://3.38.28.172/actuator/health

# 브라우저
http://3.38.28.172
```

### Pod 상태

```bash
sudo kubectl get pods -n chat-system

# 모든 Pod가 Running이면 OK!
```

### 로그 확인

```bash
sudo kubectl logs -f <pod-name> -n chat-system
```

---

## 🔧 유용한 명령어

### 배포 관련

```bash
# 전체 재배포
sudo kubectl delete -f k8s/
sudo kubectl apply -f k8s/

# Pod 재시작
sudo kubectl rollout restart deployment/chat-server -n chat-system

# Scale 조정
sudo kubectl scale deployment/chat-server --replicas=5 -n chat-system
```

### 모니터링

```bash
# 리소스 사용량
sudo kubectl top nodes
sudo kubectl top pods -n chat-system

# 이벤트 확인
sudo kubectl get events -n chat-system --sort-by='.lastTimestamp'
```

### 디버깅

```bash
# Pod 상세 정보
sudo kubectl describe pod <pod-name> -n chat-system

# Shell 접속
sudo kubectl exec -it <pod-name> -n chat-system -- /bin/sh

# 로그 (에러만)
sudo kubectl logs <pod-name> -n chat-system | grep ERROR
```

---

## 🎉 요약

### ✅ 좋은 소식

1. **이미 서버가 있습니다!**
   - 새로 만들 필요 없음
   - 고사양 서버 (t3.2xlarge)
   - 즉시 사용 가능

2. **배포가 쉽습니다!**
   - 자동 스크립트 제공
   - 5분 안에 배포 가능
   - SSH 키만 있으면 됨

3. **비용 절감 가능!**
   - Server 1 중지 → $120/월 절감
   - 로컬 개발 병행 → 추가 절감

### 🚀 지금 바로 실행

```batch
기존_서버_배포.bat
```

**선택:**
- 서버 2 (3.38.28.172) 추천!
- SSH 키 파일 입력
- 5분 후 배포 완료!

---

## 📞 접속 정보

### Server 2 (추천)

```
IP: 3.38.28.172
SSH: ssh -i your-key.pem ec2-user@3.38.28.172
HTTP: http://3.38.28.172
Health: http://3.38.28.172/actuator/health
```

### Server 1 (예비)

```
IP: 15.165.50.43
SSH: ssh -i your-key.pem ec2-user@15.165.50.43
HTTP: http://15.165.50.43
```

---

**이미 완벽한 서버가 있으니 바로 배포하세요!** 🎉
