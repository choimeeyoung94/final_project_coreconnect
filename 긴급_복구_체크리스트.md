# ⚡ 긴급 복구 체크리스트

## 🎯 현재 상황
- k8s 서버 삭제됨
- 개발 테스트 및 배포용
- 빠른 복구 필요

---

## ✅ 5분 빠른 복구 (로컬)

### 1단계: 즉시 실행 (2분)

```batch
# 자동 스크립트 실행
빠른_복구_시작.bat

# 또는 수동 실행
docker-compose down
docker-compose up -d
```

### 2단계: 확인 (1분)

```batch
# 상태 확인
docker-compose ps

# 로그 확인
docker-compose logs -f chat-app-1
```

### 3단계: 테스트 (2분)

```batch
# Health Check
curl http://localhost/actuator/health

# 부하 테스트
k6 run --vus 1000 --duration 30s k6-login-test.js
```

**✅ 완료! 개발 계속 가능!**

---

## ☁️ AWS 복구 (필요한 경우)

### 옵션 A: k3s-server2 사용 (가장 빠름!)

#### 정보
```
IP: 52.78.195.123
상태: 실행 중 ✅
```

#### 연결
```bash
# SSH 접속
ssh -i your-key.pem ec2-user@52.78.195.123

# k3s 확인
sudo systemctl status k3s
sudo kubectl get nodes
sudo kubectl get pods -A
```

#### 배포
```bash
# Git 클론
git clone your-repo-url
cd final_project_coreconnect

# 배포
sudo kubectl apply -f k8s/00-namespace.yaml
sudo kubectl apply -f k8s/01-mysql.yaml
sudo kubectl apply -f k8s/02-redis.yaml
sudo kubectl apply -f k8s/03-chat-server-dev.yaml

# 확인
sudo kubectl get pods -n chat-system -w
```

---

### 옵션 B: 새 서버 생성 (10분)

#### 1. AWS Console에서 생성

```
1. AWS Console → EC2 → 인스턴스 시작

2. 설정:
   - 이름: k3s-server-new
   - AMI: Amazon Linux 2
   - 타입: t3.small (개발용)
   - 키 페어: 기존 키 선택
   - 보안 그룹: 
     * SSH (22)
     * HTTP (80)
     * HTTPS (443)
     * k3s API (6443)

3. 시작 클릭
```

#### 2. SSH 접속

```bash
# Public IP 확인 후
ssh -i your-key.pem ec2-user@<NEW_IP>
```

#### 3. k3s 설치 (1분)

```bash
# 기본 설치
curl -sfL https://get.k3s.io | sh -

# 확인
sudo systemctl status k3s
sudo kubectl get nodes
```

#### 4. 앱 배포 (2분)

```bash
# Git 클론
git clone https://github.com/your-username/final_project_coreconnect.git
cd final_project_coreconnect

# Namespace 생성
sudo kubectl apply -f k8s/00-namespace.yaml

# DB 배포
sudo kubectl apply -f k8s/01-mysql.yaml

# Redis 배포
sudo kubectl apply -f k8s/02-redis.yaml

# 앱 배포 (개발용)
sudo kubectl apply -f k8s/03-chat-server-dev.yaml

# 상태 확인
sudo kubectl get pods -n chat-system -w
```

#### 5. 접속 확인

```bash
# LoadBalancer IP 확인
sudo kubectl get svc -n chat-system

# Health Check
curl http://<EXTERNAL-IP>/actuator/health
```

---

## 📊 복구 옵션 비교

| 옵션 | 시간 | 비용 | 난이도 | 추천도 |
|------|------|------|--------|--------|
| **로컬 Docker** | **5분** | **$0** | **쉬움** | **⭐⭐⭐** |
| k3s-server2 사용 | 3분 | 기존 | 쉬움 | ⭐⭐ |
| 새 k3s 생성 | 10분 | $15/월 | 중간 | ⭐ |

---

## 🎯 추천 전략

### 지금 당장: 로컬 복구 (5분)

```batch
빠른_복구_시작.bat
# 옵션 1 선택
```

**이유:**
- ✅ 가장 빠름 (5분)
- ✅ 비용 $0
- ✅ 무제한 테스트
- ✅ 개발에 충분

---

### 나중에 필요하면: AWS 복구

#### 언제?
- 배포 직전 최종 검증
- 실제 클라우드 환경 테스트 필요
- 다른 팀원과 공유 필요

#### 방법
```
1. k3s-server2 먼저 확인
2. 안 되면 새로 생성
3. 10분 안에 복구 가능
```

---

## 🔧 트러블슈팅

### 문제 1: Docker 시작 안 됨

```batch
# Docker Desktop 재시작
# 작업 관리자에서 Docker Desktop 종료 후 재시작

# 또는 WSL 재시작
wsl --shutdown
```

### 문제 2: 포트 충돌

```batch
# 사용 중인 포트 확인
netstat -ano | findstr :80
netstat -ano | findstr :3306

# 프로세스 종료 (관리자 권한)
taskkill /PID <PID> /F
```

### 문제 3: k3s 연결 안 됨

```bash
# SSH 키 권한 확인
chmod 400 your-key.pem

# 보안 그룹 확인
# AWS Console → EC2 → 보안 그룹
# 인바운드 규칙: 22번 포트 열림?
```

### 문제 4: 메모리 부족

```batch
# Docker 메모리 증가
# Docker Desktop → Settings → Resources
# Memory: 8GB 이상
```

---

## 📞 빠른 명령어 모음

### 로컬 Docker

```batch
# 시작
docker-compose up -d

# 중지
docker-compose down

# 재시작
docker-compose restart

# 상태
docker-compose ps

# 로그
docker-compose logs -f

# 특정 서비스만
docker-compose up -d mysql-master redis-pubsub
```

### k3s (AWS)

```bash
# 노드 확인
sudo kubectl get nodes

# Pod 확인
sudo kubectl get pods -A

# 서비스 확인
sudo kubectl get svc -A

# 로그 확인
sudo kubectl logs -f <pod-name> -n chat-system

# 재배포
sudo kubectl delete -f k8s/03-chat-server-dev.yaml
sudo kubectl apply -f k8s/03-chat-server-dev.yaml
```

### 부하 테스트

```bash
# 소규모
k6 run --vus 1000 --duration 30s k6-login-test.js

# 중규모
k6 run --vus 10000 --duration 1m k6-chatroom-performance-test.js

# 대규모
k6 run --vus 50000 --duration 30s k6-chatroom-performance-test.js
```

---

## ✅ 완료 체크리스트

### 로컬 복구 완료

- [ ] Docker 시작됨
- [ ] 모든 컨테이너 실행 중 (docker-compose ps)
- [ ] Health Check 성공 (curl http://localhost/actuator/health)
- [ ] 로그인 테스트 성공
- [ ] 채팅 테스트 성공

### AWS 복구 완료 (선택)

- [ ] k3s 노드 정상 (kubectl get nodes)
- [ ] 모든 Pod Running (kubectl get pods -A)
- [ ] LoadBalancer IP 할당됨
- [ ] 외부 접속 가능
- [ ] Health Check 성공

---

## 💡 중요한 점

### ✅ 데이터는 안전합니다!

- MySQL 데이터: 별도 볼륨 (docker volume) 또는 별도 서버
- Redis 데이터: 캐시라 재생성 가능
- 코드: Git 저장소
- 설정: k8s/ 폴더

### ✅ 빠르게 복구 가능합니다!

- 로컬: 5분
- k3s-server2: 3분
- 새 서버: 10분

### ✅ 실수가 오히려 기회입니다!

- 로컬 개발로 전환 → 비용 $0
- 더 빠른 개발 사이클
- 무제한 테스트

---

## 🚀 지금 바로 실행

```batch
# 이 명령어 하나로 끝!
빠른_복구_시작.bat

# 5분 후 개발 재개 가능! 🎉
```

---

**핵심: 당황하지 마세요! 5분 안에 복구 가능합니다!** ✅
