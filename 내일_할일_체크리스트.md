# 📋 내일 할 일 체크리스트

> **핵심**: EC2 1대만 만들면 됩니다! (3대 아님)

---

## 🎯 선택 1: Docker Compose (간단) - $150/월

### ⏱️ 예상 시간: 30분

### ✅ 단계별 작업

#### 1️⃣ EC2 인스턴스 생성 (5분)

**AWS Console 접속**
```
1. EC2 → Launch Instance 클릭

2. 설정:
   Name: chat-server-prod
   AMI: Ubuntu 22.04 LTS
   Instance Type: t3.xlarge (4 vCPU, 16GB RAM)
   Key pair: 새로 만들거나 기존 것 선택
   Storage: 50GB gp3

3. Security Group (중요!):
   - Type: SSH, Port: 22, Source: My IP
   - Type: HTTP, Port: 80, Source: 0.0.0.0/0
   - Type: Custom TCP, Port: 8081-8083, Source: My IP (개발용)

4. Launch Instance 클릭

5. Public IP 메모
   예: 54.180.123.45
```

**비용**: 약 $0.2/시간 = $150/월

---

#### 2️⃣ SSH 접속 (1분)

```bash
# Windows PowerShell에서
ssh -i "your-key.pem" ubuntu@54.180.123.45

# 접속 성공!
ubuntu@ip-172-31-xx-xx:~$
```

---

#### 3️⃣ Docker & Docker Compose 설치 (3분)

```bash
# Docker 설치 (한 줄!)
curl -fsSL https://get.docker.com | sh

# Docker Compose 설치
sudo apt update
sudo apt install -y docker-compose

# ubuntu 유저를 docker 그룹에 추가
sudo usermod -aG docker ubuntu

# 재접속 (권한 적용)
exit
ssh -i "your-key.pem" ubuntu@54.180.123.45

# 확인
docker --version
docker-compose --version
```

---

#### 4️⃣ 프로젝트 배포 (5분)

```bash
# Git 클론
git clone https://github.com/choimeeyoung94/final_project_coreconnect.git
cd final_project_coreconnect

# feature 브랜치로 체크아웃
git checkout feature_scale-out-10-servers

# 환경 변수 설정 (선택)
export MYSQL_ROOT_PASSWORD="YourSecurePassword123!"

# 3대 서버 실행!
docker-compose -f docker-compose.3servers.yml up -d

# 기다리기 (2-3분)
# MySQL, Redis 초기화 시간 필요
```

---

#### 5️⃣ 배포 확인 (5분)

```bash
# 1. 컨테이너 상태 확인
docker ps

# 출력:
# CONTAINER ID   IMAGE            STATUS    PORTS                    NAMES
# xxx            chat-server      Up 2m     0.0.0.0:8081->8080/tcp   chat-app-1
# xxx            chat-server      Up 2m     0.0.0.0:8082->8080/tcp   chat-app-2
# xxx            chat-server      Up 2m     0.0.0.0:8083->8080/tcp   chat-app-3
# xxx            nginx:alpine     Up 2m     0.0.0.0:80->80/tcp       chat-nginx
# xxx            redis:7.2        Up 2m     0.0.0.0:6379->6379/tcp   redis
# xxx            mysql:8.0        Up 2m     0.0.0.0:3306->3306/tcp   mysql

# ✅ 6개 컨테이너 모두 Running이면 성공!

# 2. 헬스체크
curl http://localhost/health

# 출력: healthy
# ✅ Nginx 작동!

# 3. 각 서버 확인
curl http://localhost:8081/actuator/health
curl http://localhost:8082/actuator/health
curl http://localhost:8083/actuator/health

# 출력: {"status":"UP"}
# ✅ 3대 서버 모두 정상!

# 4. 로드 밸런싱 확인
for i in {1..10}; do
  curl -s http://localhost/api/chatrooms | head -n 1
done

# ✅ Server 1, 2, 3이 골고루 나오면 성공!
```

---

#### 6️⃣ 외부 접속 확인 (1분)

**로컬 PC에서 테스트**
```bash
# Windows PowerShell에서
curl http://54.180.123.45/health

# 브라우저에서 접속
http://54.180.123.45
```

**✅ 성공하면 배포 완료!**

---

#### 7️⃣ 모니터링 설정 (선택 - 5분)

```bash
# 실시간 로그 확인
docker-compose -f docker-compose.3servers.yml logs -f

# 리소스 사용량
docker stats

# 특정 서버만
docker logs -f chat-app-1
```

---

### 🎉 완료!

**총 소요 시간**: 30분
**비용**: $150/월
**결과**: 3대 서버 (30,000명 동시 접속)

---

## 🚀 선택 2: Kubernetes (추천) - $250/월

### ⏱️ 예상 시간: 1시간

### ✅ 단계별 작업

#### 1️⃣ EC2 인스턴스 생성 (5분)

**AWS Console 접속**
```
1. EC2 → Launch Instance 클릭

2. 설정:
   Name: chat-server-k8s
   AMI: Ubuntu 22.04 LTS
   Instance Type: c5.2xlarge (8 vCPU, 16GB RAM) ← 더 큰 인스턴스
   Key pair: 새로 만들거나 기존 것 선택
   Storage: 100GB gp3

3. Security Group:
   - Type: SSH, Port: 22, Source: My IP
   - Type: HTTP, Port: 80, Source: 0.0.0.0/0
   - Type: Custom TCP, Port: 6443, Source: My IP (K8s API)
   - Type: Custom TCP, Port: 30000-32767, Source: 0.0.0.0/0 (NodePort)

4. Launch Instance 클릭

5. Public IP 메모
   예: 13.124.45.67
```

**비용**: 약 $0.34/시간 = $250/월

---

#### 2️⃣ SSH 접속 (1분)

```bash
ssh -i "your-key.pem" ubuntu@13.124.45.67
```

---

#### 3️⃣ K3s 설치 (2분)

```bash
# K3s 설치 (한 줄! 정말 쉬움)
curl -sfL https://get.k3s.io | sh -

# 30초 대기
sleep 30

# 확인
sudo k3s kubectl get nodes

# 출력:
# NAME     STATUS   ROLES                  AGE   VERSION
# ubuntu   Ready    control-plane,master   1m    v1.28.4+k3s1
# ✅ Kubernetes 설치 완료!

# kubectl 단축키 설정
echo "alias k='sudo k3s kubectl'" >> ~/.bashrc
source ~/.bashrc

# 이제 'k' 명령어로 사용 가능
k get nodes
```

---

#### 4️⃣ 프로젝트 클론 & 이미지 준비 (10분)

```bash
# Docker 설치 (이미지 빌드용)
curl -fsSL https://get.docker.com | sh

# Git 클론
git clone https://github.com/choimeeyoung94/final_project_coreconnect.git
cd final_project_coreconnect

# feature 브랜치로 체크아웃
git checkout feature_scale-out-10-servers

# 이미지 빌드 (3-5분 소요)
cd backend
docker build -t chat-server:latest .

# K3s에 이미지 import
docker save chat-server:latest | sudo k3s ctr images import -

# 확인
sudo k3s crictl images | grep chat-server

# 출력:
# docker.io/library/chat-server    latest    xxx    xxx MB
# ✅ 이미지 준비 완료!
```

---

#### 5️⃣ Kubernetes 배포 (2분)

```bash
# 프로젝트 루트로 이동
cd ~/final_project_coreconnect

# 전체 배포 (한 줄!)
k apply -f k8s/

# 출력:
# namespace/chat-system created
# statefulset.apps/mysql created
# service/mysql created
# deployment.apps/redis-pubsub created
# service/redis-pubsub created
# deployment.apps/chat-server created
# service/chat-service created
# horizontalpodautoscaler.autoscaling/chat-server-hpa created
# ✅ 배포 시작!
```

---

#### 6️⃣ 배포 상태 확인 (5분)

```bash
# Pod 상태 확인 (실시간)
watch -n 2 'k get pods -n chat-system'

# 모든 Pod가 Running 될 때까지 대기 (3-5분)
# 출력:
# NAME                           READY   STATUS    RESTARTS   AGE
# mysql-0                        1/1     Running   0          3m
# redis-pubsub-xxx               1/1     Running   0          3m
# chat-server-xxx-1              1/1     Running   0          2m
# chat-server-xxx-2              1/1     Running   0          2m
# ...
# chat-server-xxx-10             1/1     Running   0          2m
# ✅ 10개 Pod 모두 Running!

# Ctrl+C로 종료

# Service 확인
k get svc -n chat-system

# 출력:
# NAME           TYPE           CLUSTER-IP      EXTERNAL-IP      PORT(S)
# chat-service   LoadBalancer   10.43.0.12      13.124.45.67     80:30080/TCP
# ✅ LoadBalancer 생성됨!

# HPA 확인
k get hpa -n chat-system

# 출력:
# NAME               REFERENCE                TARGETS         MINPODS   MAXPODS   REPLICAS
# chat-server-hpa    Deployment/chat-server   10%/70%, 15%/80%   3        20        10
# ✅ 자동 스케일링 준비 완료!
```

---

#### 7️⃣ 접속 테스트 (5분)

```bash
# EC2에서 테스트
curl http://localhost/actuator/health

# 출력: {"status":"UP"}
# ✅ 정상 작동!

# 로드 밸런싱 확인 (10개 Pod로 분산)
for i in {1..20}; do
  curl -s http://localhost/api/chatrooms 2>/dev/null | head -n 1
done

# ✅ 10개 Pod가 골고루 나오면 성공!
```

**로컬 PC에서 테스트**
```bash
# Windows PowerShell에서
curl http://13.124.45.67/actuator/health

# 브라우저에서 접속
http://13.124.45.67
```

---

#### 8️⃣ 자동 스케일링 확인 (선택 - 10분)

```bash
# K6 설치 (로컬 PC에서)
# Windows: choco install k6
# Mac: brew install k6

# 부하 테스트 실행
k6 run --vus 30000 --duration 3m k6-chatroom-performance-test.js

# EC2에서 실시간 모니터링
watch -n 1 'k get hpa -n chat-system'

# 출력 (실시간 변화):
# REPLICAS: 10 → 12 → 15 → 18 (자동 증가!)
# CPU: 45% → 75% → 85% → 70% (부하 분산)
# ✅ 자동 스케일링 작동 확인!

# 부하 중단 후 (5분 후)
# REPLICAS: 18 → 15 → 12 → 10 → 3 (자동 감소)
# ✅ 비용 최적화 작동!
```

---

### 🎉 완료!

**총 소요 시간**: 1시간
**비용**: $250/월
**결과**: 
- 10개 Pod (100,000명 처리 가능)
- 자동 스케일링 (3~20개)
- 자가 치유 (< 10초)
- **포트폴리오 가치 10배!** ✅

---

## 🤔 어떤 옵션을 선택할까?

### 💰 비용이 중요하다면: **옵션 A (Docker Compose)**
```
✅ 저렴함 ($150/월)
✅ 간단함 (30분)
✅ 포트폴리오와 일치 (3대 검증)

❌ 수동 관리
❌ 자동 스케일링 없음
```

### 🚀 포트폴리오 가치가 중요하다면: **옵션 B (Kubernetes)** ⭐ 추천!
```
✅ 자동 스케일링 (HPA)
✅ 자가 치유 (Self-healing)
✅ Kubernetes 경험 (면접 플러스!)
✅ 최신 기술

❌ 약간 비쌈 (+$100/월)
❌ 약간 복잡 (1시간)
```

### 💡 제 추천: **옵션 B (Kubernetes)**
**이유:**
- $100 차이는 적지만, 포트폴리오 가치는 10배
- 면접에서 "Kubernetes 경험 있어요" 말할 수 있음
- 실제 실무에서 쓰는 기술
- 자동 스케일링으로 비용 최적화 가능 (3개 ~ 20개)

---

## 📅 타임라인

### 옵션 A (Docker Compose) - 30분
```
09:00 - 09:05  EC2 생성
09:05 - 09:08  Docker 설치
09:08 - 09:15  프로젝트 배포
09:15 - 09:20  확인 및 테스트
09:20 - 09:30  최종 점검

→ 09:30 완료! ✅
```

### 옵션 B (Kubernetes) - 1시간
```
09:00 - 09:05  EC2 생성
09:05 - 09:07  K3s 설치
09:07 - 09:20  이미지 빌드
09:20 - 09:22  배포
09:22 - 09:30  배포 확인
09:30 - 09:40  테스트
09:40 - 10:00  자동 스케일링 확인

→ 10:00 완료! ✅
```

---

## 🎯 최종 체크리스트

### 배포 전 준비
- [ ] AWS 계정 준비
- [ ] 신용카드 등록 (EC2 비용)
- [ ] SSH Key 준비 (.pem 파일)
- [ ] Git 계정 (프로젝트 클론용)

### 배포 후 확인
- [ ] 모든 컨테이너/Pod Running
- [ ] 헬스체크 정상
- [ ] 외부 접속 가능
- [ ] 로드 밸런싱 동작 확인
- [ ] (Kubernetes) 자동 스케일링 확인

### 포트폴리오 업데이트
- [ ] EC2 Public IP 기록
- [ ] 스크린샷 촬영
  - docker ps / kubectl get pods
  - 헬스체크 결과
  - 로드 밸런싱 결과
- [ ] 포트폴리오에 배포 URL 추가

---

## 💡 문제 해결

### Docker Compose가 실행 안 될 때
```bash
# 로그 확인
docker-compose -f docker-compose.3servers.yml logs

# 재시작
docker-compose -f docker-compose.3servers.yml restart

# 완전 재배포
docker-compose -f docker-compose.3servers.yml down
docker-compose -f docker-compose.3servers.yml up -d
```

### Kubernetes Pod가 시작 안 될 때
```bash
# Pod 상세 정보
k describe pod -n chat-system chat-server-xxx-1

# 로그 확인
k logs -n chat-system chat-server-xxx-1

# Pod 재시작
k delete pod -n chat-system chat-server-xxx-1
# → 자동으로 새 Pod 생성됨
```

---

## 📞 도움이 필요하면

1. **로그 확인**
   ```bash
   # Docker
   docker-compose logs -f
   
   # Kubernetes
   k logs -f -n chat-system deployment/chat-server
   ```

2. **상태 확인**
   ```bash
   # Docker
   docker ps -a
   
   # Kubernetes
   k get all -n chat-system
   ```

3. **재시작**
   ```bash
   # Docker
   docker-compose restart
   
   # Kubernetes
   k rollout restart deployment/chat-server -n chat-system
   ```

---

## 🎉 성공하면

1. **스크린샷 저장**
   - EC2 대시보드
   - docker ps / kubectl get pods 결과
   - curl 테스트 결과

2. **포트폴리오 업데이트**
   ```markdown
   ## 배포 환경
   - AWS EC2: c5.2xlarge
   - Kubernetes: K3s
   - Pods: 10개 (자동 스케일링 3~20개)
   - URL: http://13.124.45.67
   ```

3. **Git 커밋**
   ```bash
   git add 포트폴리오_인프라_확장성_개선.md
   git commit -m "docs: Add production deployment info"
   ```

---

**🚀 준비되셨나요? 내일 화이팅!**

추천: **Kubernetes (옵션 B)** - 포트폴리오 가치 10배! ✨

