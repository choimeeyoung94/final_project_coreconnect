# 🏠 로컬 vs ☁️ AWS 비교

## 질문: "서버를 내리면 k6 테스트를 할 수 없나요?"

### ❌ 오해
> "AWS 서버가 없으면 10만 명 부하 테스트를 못한다"

### ✅ 진실
> "k6는 **부하 생성 도구**이므로 로컬 PC에서도 10만 명 시뮬레이션 가능"

---

## 🎯 핵심 개념

### k6의 동작 원리

```
┌─────────────┐
│  로컬 PC    │
│  (k6 실행)  │ ─────> 가상 사용자 10만 명 생성
└─────────────┘
      │
      │ HTTP 요청 전송
      ▼
┌─────────────┐
│ 테스트 서버 │ ◄──── 로컬 Docker 또는 AWS
│ (Backend)   │
└─────────────┘
```

**k6의 역할:**
- 가상 사용자(Virtual Users) 생성
- HTTP 요청 전송
- 응답 시간 측정
- 결과 리포트 생성

**k6는 테스트 서버가 어디 있든 상관없음!**
- ✅ 로컬 Docker → 가능
- ✅ 로컬 호스트 → 가능
- ✅ AWS → 가능
- ✅ 다른 클라우드 → 가능

---

## 📊 상세 비교

### 1. 기능 테스트

| 항목 | 로컬 | AWS | 승자 |
|------|------|-----|------|
| 기본 API 테스트 | ✅ | ✅ | 동점 |
| 채팅 기능 테스트 | ✅ | ✅ | 동점 |
| 알림 기능 테스트 | ✅ | ✅ | 동점 |
| 이메일 기능 테스트 | ✅ | ✅ | 동점 |
| **비용** | **$0** | **$40만/월** | **🏆 로컬** |

### 2. 부하 테스트 (10만 VU)

| 항목 | 로컬 | AWS | 승자 |
|------|------|-----|------|
| 10만 VU 생성 | ✅ | ✅ | 동점 |
| 실제 네트워크 | ❌ | ✅ | AWS |
| 여러 리전 테스트 | ❌ | ✅ | AWS |
| 반복 테스트 | ✅ 무제한 | ⚠️ 비용 증가 | 🏆 로컬 |
| **비용** | **$0** | **$40만/월** | **🏆 로컬** |

### 3. 성능 최적화

| 작업 | 로컬 | AWS | 승자 |
|------|------|-----|------|
| N+1 쿼리 발견 | ✅ | ✅ | 동점 |
| 인덱스 효과 측정 | ✅ | ✅ | 동점 |
| 코드 개선 검증 | ✅ | ✅ | 동점 |
| 빠른 반복 개발 | ✅ 즉시 | ⚠️ 배포 필요 | 🏆 로컬 |
| **비용** | **$0** | **$40만/월** | **🏆 로컬** |

---

## 💰 비용 상세 비교

### 시나리오 1: 개발 단계 (2-4주)

#### AWS 상시 운영
```
EC2 t3.large × 10대
= $0.0832/시간 × 10 × 24시간 × 30일
= $599/월
≈ 80만원/월
```

#### 로컬 Docker
```
전기세: 약 1만원/월
총 비용: 1만원/월
```

**절감액: 79만원/월** 🎉

---

### 시나리오 2: 부하 테스트만 (주 1회)

#### AWS 상시 운영
```
월 비용: $599 (80만원)
```

#### AWS 온디맨드 (필요할 때만)
```
주 1회 × 2시간 × 4주
= 8시간/월
= $0.0832 × 10 × 8
= $6.66/월
≈ 9,000원/월
```

#### 로컬 + AWS 하이브리드
```
로컬 개발: $0
월 1회 AWS 검증: $1.66 (2시간)
총 비용: 약 2,000원/월
```

**절감액: 79만원/월** 🎉

---

### 시나리오 3: 최종 검증 (1회)

#### 배포 전 최종 테스트
```
스팟 인스턴스 × 10대 × 3시간
= $0.025 × 10 × 3
= $0.75
≈ 1,000원
```

---

## 🏆 결론: 단계별 전략

### Phase 1: 개발 및 최적화 (2-4주)
**환경**: 로컬 Docker  
**테스트**: k6 (무제한)  
**비용**: $0  

```bash
# 로컬에서 충분히 테스트
docker-compose up -d
k6 run --vus 10000 --duration 10m k6-chatroom-performance-test.js

# 코드 개선 반복
# - N+1 쿼리 해결
# - 인덱스 최적화
# - 캐싱 추가
```

### Phase 2: 중간 검증 (월 1회)
**환경**: AWS 최소 구성 (t3.small 2대)  
**테스트**: k6 (중규모)  
**비용**: ~$2/회  

```bash
# AWS 서버 시작
./aws-cost-optimizer.sh  # 옵션 2: 인스턴스 시작

# 중규모 테스트
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js

# 서버 중지
./aws-cost-optimizer.sh  # 옵션 3: 인스턴스 중지
```

### Phase 3: 최종 검증 (배포 전)
**환경**: AWS 스팟 인스턴스 (t3.large 10대)  
**테스트**: k6 (대규모)  
**비용**: ~$1/회  

```bash
# 스팟 인스턴스로 대규모 클러스터 구성
# 2-3시간 집중 테스트
k6 run --vus 100000 --duration 30m k6-chatroom-performance-test.js

# 즉시 종료
```

---

## 🎓 실전 예제

### 예제 1: N+1 쿼리 최적화 (로컬)

```bash
# 1. 문제 발견
docker-compose up -d
k6 run --vus 1000 --duration 1m k6-chatroom-performance-test.js
# 결과: 평균 응답 시간 2초

# 2. 로그 확인
docker-compose logs chat-server | grep "Hibernate"
# N+1 쿼리 발견!

# 3. 코드 수정 (Fetch Join 추가)

# 4. 재테스트
k6 run --vus 1000 --duration 1m k6-chatroom-performance-test.js
# 결과: 평균 응답 시간 200ms

# ✅ 성공! 10배 개선
# 💰 비용: $0
```

### 예제 2: 인덱스 효과 측정 (로컬)

```bash
# 1. 인덱스 없이 테스트
docker-compose up -d
k6 run --vus 5000 --duration 2m k6-chatroom-performance-test.js
# 결과: P95 = 3초

# 2. 인덱스 생성
docker exec -i mysql mysql -uroot -p'Chat@2024!Secure' db_coreconnect < 인덱스_생성_간단버전.sql

# 3. 인덱스 있는 상태로 테스트
k6 run --vus 5000 --duration 2m k6-chatroom-performance-test.js
# 결과: P95 = 500ms

# ✅ 성공! 6배 개선
# 💰 비용: $0
```

### 예제 3: 최종 검증 (AWS)

```bash
# 로컬에서 충분히 최적화한 후

# 1. AWS 서버 시작 (온디맨드)
export INSTANCE_ID=i-your-instance-id
./aws-cost-optimizer.sh
# 선택: 4 (자동 테스트)

# 2. 대규모 테스트 자동 실행
# - 인스턴스 시작
# - k6 테스트 (10만 VU)
# - 인스턴스 중지

# ✅ 성공! 실제 환경 검증 완료
# 💰 비용: ~$0.17 (2시간)
```

---

## 📈 성능 지표 비교

### 로컬에서 발견 가능한 문제들

#### ✅ 쉽게 발견 (로컬로 충분)
- N+1 쿼리 문제
- 비효율적인 인덱스
- 메모리 누수
- 느린 알고리즘
- 동기 처리 병목
- DB 커넥션 풀 부족
- 캐싱 누락

#### ⚠️ 추가 확인 필요 (AWS 권장)
- 실제 네트워크 레이턴시
- 여러 리전 간 통신
- CDN 효과
- 실제 사용자 분포

---

## 🚀 추천 워크플로우

```
1주차: 로컬 개발
├─ Docker Compose 실행
├─ k6 소규모 테스트 (1,000 VU)
├─ 병목 찾기
└─ 코드 개선 반복

2주차: 로컬 최적화
├─ N+1 쿼리 해결
├─ 인덱스 추가
├─ 캐싱 추가
└─ k6 중규모 테스트 (10,000 VU)

3주차: 로컬 검증
├─ k6 대규모 테스트 (50,000 VU)
├─ 시스템 한계 확인
└─ 추가 최적화

4주차: AWS 최종 검증 (1회)
├─ 스팟 인스턴스 구성
├─ k6 10만 VU 테스트
├─ 실제 환경 검증
└─ 즉시 종료

총 비용: ~$1-2
```

---

## 💡 핵심 인사이트

### 1. k6는 로컬에서도 강력합니다
```bash
# 로컬 PC에서 10만 VU 생성 가능
k6 run --vus 100000 --duration 30s test.js
```

### 2. 대부분의 문제는 로컬에서 발견됩니다
- 성능 문제의 80%는 코드/DB 이슈
- 로컬에서 충분히 최적화 가능
- AWS는 최종 확인용

### 3. 반복 테스트는 로컬이 유리합니다
- 무제한 테스트
- 빠른 피드백
- 비용 $0

### 4. AWS는 선택적으로 사용합니다
- 최종 검증용 (월 1-2회)
- 실제 환경 테스트
- 총 비용 < $10/월

---

## 🎯 액션 아이템

### 지금 당장 하세요 ✅

1. **AWS 서버 중지**
```bash
export INSTANCE_ID=i-your-instance-id
export AWS_REGION=ap-northeast-2
./aws-cost-optimizer.sh
# 선택: 3 (인스턴스 중지)
```

2. **로컬 환경 구축**
```bash
docker-compose up -d
docker-compose ps  # 상태 확인
```

3. **로컬 테스트 시작**
```bash
k6 run --vus 1000 --duration 1m k6-login-test.js
k6 run --vus 1000 --duration 1m k6-chatroom-performance-test.js
```

4. **비용 절감 확인**
```
이전: 40만원/월
현재: 0원/월
절감: 40만원/월 🎉
```

---

## ✨ 최종 답변

### Q: 서버를 내리면 k6 테스트를 못하나요?

### A: 아니요! 오히려 더 좋습니다!

**이유:**
1. ✅ k6는 로컬에서도 10만 VU 생성 가능
2. ✅ 무제한 테스트 가능 (비용 $0)
3. ✅ 빠른 개발 사이클
4. ✅ 대부분의 성능 문제는 로컬에서 발견
5. ✅ AWS는 최종 검증용으로만 사용 (월 $1-2)

**결론:**
- 지금 당장 AWS 서버를 중지하세요
- 로컬에서 충분히 개발하고 최적화하세요
- 월 40만원 → 0원으로 절감하세요
- 포트폴리오는 로컬 테스트 결과로도 충분합니다!

**🎉 축하합니다! 연 480만원을 절약하셨습니다!**


