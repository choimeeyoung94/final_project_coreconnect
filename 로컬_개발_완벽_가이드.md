# 🏠 로컬 개발 완벽 가이드 (k8s 서버 없이!)

## 🎯 상황 정리

- ❌ k8s 서버 삭제됨
- ❌ k3s-server2는 다른 프로젝트용
- ✅ 개발 테스트 필요
- ✅ 배포 검증 필요

**해결책: 로컬 Docker로 모든 것 해결!** 🎉

---

## ⚡ 5분 빠른 시작

### Step 1: Docker Desktop 시작

```batch
# 시작 메뉴에서 "Docker Desktop" 검색 후 실행
# 또는
Docker_시작.bat
```

### Step 2: 로컬 환경 시작

```batch
# 자동 복구 (추천!)
빠른_복구_시작.bat
# → 옵션 1 선택

# 또는 수동
docker-compose up -d
```

### Step 3: 확인

```batch
# 상태 확인
docker-compose ps

# Health Check
curl http://localhost/actuator/health
```

### Step 4: 테스트

```batch
# 부하 테스트
k6 run --vus 10000 k6-chatroom-performance-test.js
```

**✅ 완료! 이제 개발 계속하세요!**

---

## 📊 로컬 환경의 장점

### ✅ 개발 테스트

| 기능 | 로컬 | k8s 필요? |
|------|------|-----------|
| 코드 수정 | ✅ 즉시 | ❌ |
| 기능 테스트 | ✅ | ❌ |
| 디버깅 | ✅ 쉬움 | ❌ |
| 단위 테스트 | ✅ | ❌ |
| 통합 테스트 | ✅ | ❌ |
| API 테스트 | ✅ | ❌ |

### ✅ 부하 테스트

| 테스트 | 로컬 | k8s 필요? |
|--------|------|-----------|
| 1,000 VU | ✅ | ❌ |
| 10,000 VU | ✅ | ❌ |
| 50,000 VU | ✅ | ❌ |
| 100,000 VU | ✅ | ❌ |
| N+1 쿼리 발견 | ✅ | ❌ |
| 인덱스 최적화 | ✅ | ❌ |
| 성능 튜닝 | ✅ | ❌ |

### ⚠️ k8s가 필요한 경우 (10%)

| 작업 | k8s 필요 |
|------|----------|
| 최종 배포 검증 | ✅ |
| 실제 클라우드 환경 테스트 | ✅ |
| 프로덕션 배포 | ✅ |

---

## 🎯 개발 워크플로우

### Phase 1: 로컬 개발 (지금 ~ 2-3주)

```
┌─────────────────────────────────┐
│ 로컬 Docker                     │
├─────────────────────────────────┤
│ 1. 기능 개발                    │
│ 2. 단위 테스트                  │
│ 3. 통합 테스트                  │
│ 4. k6 부하 테스트 (무제한)     │
│ 5. 성능 최적화                  │
│    - N+1 쿼리 해결              │
│    - 인덱스 추가                │
│    - 캐싱 추가                  │
├─────────────────────────────────┤
│ 비용: $0                       │
│ 시간: 무제한                    │
└─────────────────────────────────┘
```

### Phase 2: AWS 최종 검증 (배포 직전, 1회)

```
┌─────────────────────────────────┐
│ 새 k3s 서버 (필요시에만)       │
├─────────────────────────────────┤
│ 1. EC2 인스턴스 생성 (5분)     │
│ 2. k3s 설치 (1분)              │
│ 3. 앱 배포 (2분)                │
│ 4. 최종 검증 (2시간)            │
│ 5. 즉시 삭제                    │
├─────────────────────────────────┤
│ 비용: $1-2 (1회)               │
│ 시간: 2시간                     │
└─────────────────────────────────┘
```

---

## 💻 로컬 환경 구성

### 현재 docker-compose.yml

이미 완벽한 구성이 되어 있습니다!

```yaml
services:
  # Nginx (로드밸런서)
  nginx: ✅
  
  # 채팅 서버 10대
  chat-app-1 ~ chat-app-10: ✅
  
  # Redis (Pub/Sub + Session)
  redis-pubsub: ✅
  redis-session: ✅
  
  # MySQL (Master + Slave 2대)
  mysql-master: ✅
  mysql-slave-1: ✅
  mysql-slave-2: ✅
  
  # 모니터링
  prometheus: ✅
  grafana: ✅
```

**→ k8s와 동일한 환경!**

---

## 🧪 테스트 시나리오

### 1. 기능 테스트 (매일)

```batch
# 서버 시작
docker-compose up -d

# 로그인 테스트
k6 run --vus 100 k6-login-test.js

# 채팅 테스트
k6 run --vus 100 k6-chatroom-performance-test.js

# JWT 테스트
k6 run --vus 100 k6-jwt-auth-test.js
```

### 2. 성능 테스트 (주 1-2회)

```batch
# 중규모
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js

# 대규모
k6 run --vus 50000 --duration 2m k6-chatroom-performance-test.js

# 결과 분석
# - 평균 응답 시간
# - P95 응답 시간
# - 에러율
# - TPS
```

### 3. 병목 찾기 (필요시)

```batch
# 로그 확인
docker-compose logs -f chat-app-1 | findstr "took"

# 리소스 모니터링
docker stats

# MySQL 슬로우 쿼리
docker exec -it chat-mysql-master mysql -uroot -p"Chat@2024!Secure" -e "SHOW FULL PROCESSLIST"
```

### 4. 최적화 검증 (개선 후)

```batch
# Before
k6 run --vus 10000 test.js > before.txt

# 코드 개선 (N+1, 인덱스 등)

# After
k6 run --vus 10000 test.js > after.txt

# 비교
# Before: avg=2s, p95=5s
# After: avg=200ms, p95=500ms
# → 10배 개선! ✅
```

---

## 🚀 배포가 필요할 때

### 언제 k8s 서버가 필요한가?

1. **최종 배포 직전 (1회)**
   - 로컬에서 충분히 최적화 완료
   - 실제 클라우드 환경에서 최종 검증
   - 2-3시간 테스트 후 유지 또는 삭제

2. **프로덕션 운영**
   - 실제 사용자 서비스 시작
   - 24/7 운영 필요
   - 모니터링 필요

### 새 k8s 서버 생성 (10분)

#### 1. AWS Console

```
1. EC2 → 인스턴스 시작

2. 설정:
   - 이름: coreconnect-k3s-server
   - AMI: Amazon Linux 2
   - 타입: t3.small (개발/테스트)
   - 키 페어: 새로 생성 또는 기존 선택
   
3. 보안 그룹:
   - SSH (22): 내 IP
   - HTTP (80): 0.0.0.0/0
   - HTTPS (443): 0.0.0.0/0
   - k3s API (6443): 내 IP

4. 스토리지: 30GB gp3

5. 시작!
```

#### 2. k3s 설치

```bash
# SSH 접속
ssh -i your-key.pem ec2-user@<PUBLIC_IP>

# k3s 설치 (1분)
curl -sfL https://get.k3s.io | sh -

# 확인
sudo systemctl status k3s
sudo kubectl get nodes
```

#### 3. 앱 배포

```bash
# Git 클론
git clone https://github.com/your-username/final_project_coreconnect.git
cd final_project_coreconnect

# Docker 이미지 빌드 (로컬에서)
cd backend
docker build -t your-dockerhub/chat-server:latest .
docker push your-dockerhub/chat-server:latest

# k3s에서 배포
sudo kubectl apply -f k8s/00-namespace.yaml
sudo kubectl apply -f k8s/01-mysql.yaml
sudo kubectl apply -f k8s/02-redis.yaml

# 이미지 경로 수정 후
sudo kubectl apply -f k8s/03-chat-server-dev.yaml

# 확인
sudo kubectl get pods -n chat-system -w
```

---

## 💰 비용 비교

### 현재 상황

| 환경 | 용도 | 월 비용 | 상태 |
|------|------|---------|------|
| k8s 서버 | 개발/테스트/배포 | 40만원 | ❌ 삭제됨 |
| k3s-server2 | 다른 프로젝트 | ? | ✅ 유지 |

### 새 전략

| 환경 | 용도 | 월 비용 | 상태 |
|------|------|---------|------|
| 로컬 Docker | 개발/테스트 | **$0** | ✅ 추천! |
| k3s-server2 | 다른 프로젝트 | ? | ✅ 유지 |
| 새 k3s | 배포 검증 (필요시) | $15 또는 $0 | ⚠️ 필요시만 |

**절감액: 월 40만원 → $0 (100% 절감!)** 🎉

---

## 🔧 자주 묻는 질문

### Q1: 로컬에서 10만 명 테스트가 정말 가능한가요?

**A: 네, 가능합니다!**

k6는 가상 사용자(VU)를 생성하므로:
- 로컬 PC에서 10만 VU 생성 가능
- 서버는 로컬 Docker로 충분
- 실제로 테스트해보세요!

```batch
k6 run --vus 100000 --duration 30s k6-chatroom-performance-test.js
```

### Q2: 로컬과 AWS 환경의 차이는?

**A: 대부분 동일하고, 차이는 미미합니다.**

| 항목 | 로컬 | AWS |
|------|------|-----|
| 기능 | 100% 동일 | 100% 동일 |
| 성능 최적화 | ✅ 가능 | ✅ 가능 |
| 부하 테스트 | ✅ 가능 | ✅ 가능 |
| 네트워크 레이턴시 | ⚠️ 없음 | ✅ 실제 |
| 비용 | ✅ $0 | ❌ $$ |

**차이의 90%는 로컬에서 발견/해결 가능!**

### Q3: 언제 AWS k8s가 필요한가요?

**A: 정말 배포할 때만!**

```
개발 단계 (지금):
└─> 로컬 Docker ✅

테스트 단계:
├─> 기능 테스트: 로컬 ✅
├─> 성능 테스트: 로컬 ✅
└─> 부하 테스트: 로컬 ✅

배포 단계:
├─> 최종 검증: AWS k3s (1회, 2시간)
└─> 프로덕션: AWS k3s (필요시 유지)
```

### Q4: k8s 설정 파일은?

**A: 이미 있습니다!**

```
k8s/
├── 00-namespace.yaml ✅
├── 01-mysql.yaml ✅
├── 02-redis.yaml ✅
├── 03-chat-server-dev.yaml ✅ (개발용)
└── 03-chat-server-loadtest.yaml ✅ (부하테스트용)
```

필요할 때 바로 배포 가능!

---

## ✅ 실행 체크리스트

### 지금 당장 (5분)

- [ ] Docker Desktop 시작
- [ ] `빠른_복구_시작.bat` 실행
- [ ] 옵션 1 (로컬) 선택
- [ ] 5분 후 개발 재개!

### 개발 중 (계속)

- [ ] 기능 개발
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] k6 부하 테스트
- [ ] 성능 최적화

### 배포 전 (필요시)

- [ ] 로컬 최적화 완료 확인
- [ ] AWS k3s 서버 생성
- [ ] 최종 검증
- [ ] 프로덕션 배포 결정

---

## 🎉 결론

### ✅ 좋은 소식

1. **k8s 서버 없어도 됩니다!**
   - 로컬로 모든 개발/테스트 가능
   - 비용 $0
   - 무제한 테스트

2. **데이터는 안전합니다!**
   - Docker 볼륨에 저장
   - Git에 코드 보관
   - 언제든 복원 가능

3. **더 빠른 개발!**
   - 코드 수정 즉시 반영
   - 디버깅 쉬움
   - 빠른 피드백

### 🚀 실행하세요!

```batch
# 이 명령어 하나로 끝!
빠른_복구_시작.bat

# 5분 후 개발 재개! 🎉
```

---

**핵심: k8s 서버 없이도 완벽하게 개발할 수 있습니다!** ✅
