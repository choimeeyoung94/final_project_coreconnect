# 🏠 로컬 부하 테스트 완벽 가이드

## 💡 핵심 개념

**AWS 없이도 k6로 10만 명 동시 접속 테스트가 가능합니다!**

- k6는 **가상 사용자(VU)**를 생성하는 도구
- 로컬 PC에서도 수십만 VU 생성 가능
- 실제 서버는 로컬 Docker로 충분

---

## 🚀 빠른 시작 (5분)

### 1단계: 로컬 서버 시작
```bash
# Docker Compose로 전체 시스템 실행
docker-compose up -d

# 서버 상태 확인
docker-compose ps
```

### 2단계: 소규모 테스트 (1,000명)
```bash
# 로그인 테스트
k6 run --vus 1000 --duration 30s k6-login-test.js

# 채팅 테스트
k6 run --vus 1000 --duration 30s k6-chatroom-performance-test.js
```

### 3단계: 중규모 테스트 (10,000명)
```bash
k6 run --vus 10000 --duration 1m k6-chatroom-performance-test.js
```

### 4단계: 대규모 테스트 (100,000명)
```bash
# 주의: PC 리소스가 충분한지 확인
k6 run --vus 100000 --duration 30s k6-chatroom-performance-test.js
```

---

## 📊 단계별 테스트 전략

### Phase 1: 기능 검증 (100 VU)
**목적**: 기본 기능 동작 확인

```bash
k6 run --vus 100 --duration 1m k6-login-test.js
k6 run --vus 100 --duration 1m k6-chatroom-performance-test.js
```

**체크 포인트:**
- ✅ 모든 API 응답 200 OK
- ✅ 에러율 0%
- ✅ 평균 응답 시간 < 500ms

---

### Phase 2: 병목 찾기 (1,000 VU)
**목적**: 성능 이슈 발견

```bash
k6 run --vus 1000 --duration 5m k6-chatroom-performance-test.js
```

**체크 포인트:**
- ❓ 어느 API가 느린가?
- ❓ N+1 쿼리가 있는가?
- ❓ 메모리 사용량이 증가하는가?

**병목 확인 방법:**
```bash
# Docker 컨테이너 리소스 모니터링
docker stats

# 로그 확인
docker-compose logs -f chat-server
```

---

### Phase 3: 성능 개선 검증 (10,000 VU)
**목적**: 코드 개선 효과 측정

```bash
# 개선 전 테스트
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js > before.txt

# 코드 개선 (N+1 해결, 인덱스 추가 등)

# 개선 후 테스트
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js > after.txt

# 비교
diff before.txt after.txt
```

**측정 지표:**
- 평균 응답 시간 (Target: 50% 감소)
- P95 응답 시간 (Target: 1초 미만)
- 에러율 (Target: 1% 미만)
- TPS (Target: 2배 증가)

---

### Phase 4: 한계 테스트 (50,000+ VU)
**목적**: 시스템 한계 파악

```bash
# 점진적으로 부하 증가
k6 run --vus 10000 --duration 2m k6-chatroom-performance-test.js
k6 run --vus 30000 --duration 2m k6-chatroom-performance-test.js
k6 run --vus 50000 --duration 2m k6-chatroom-performance-test.js
k6 run --vus 100000 --duration 30s k6-chatroom-performance-test.js
```

**한계 지점 찾기:**
- 언제부터 에러율이 증가하는가?
- CPU/메모리가 언제 포화 상태가 되는가?
- DB 커넥션 풀이 부족한가?

---

## 🔧 로컬 환경 최적화

### Docker 리소스 설정
```bash
# Docker Desktop > Settings > Resources
CPU: 최소 4 cores (권장 8 cores)
Memory: 최소 8GB (권장 16GB)
Swap: 2GB
```

### MySQL 최적화
```sql
-- my.cnf 또는 Docker Compose 설정
max_connections = 500
innodb_buffer_pool_size = 2G
innodb_log_file_size = 512M
```

### Redis 최적화
```bash
# redis.conf
maxmemory 1gb
maxmemory-policy allkeys-lru
```

---

## 📈 테스트 결과 분석

### k6 HTML 리포트 생성
```bash
# 예쁜 HTML 리포트 생성
k6 run --out json=results.json k6-chatroom-performance-test.js

# JSON → HTML 변환 (k6-reporter 사용)
npm install -g k6-reporter
k6-reporter results.json
```

### 주요 지표 해석

#### 1. HTTP Request Duration
```
http_req_duration..............: avg=250ms min=50ms med=200ms max=2s p(95)=800ms
```
- **avg**: 평균 응답 시간 → Target: < 500ms
- **p(95)**: 95% 사용자 체감 → Target: < 1s
- **max**: 최악의 경우 → Target: < 3s

#### 2. HTTP Request Failed
```
http_req_failed................: 0.50%
```
- Target: < 1%
- 1% 이상이면 문제 있음

#### 3. Virtual Users
```
vus............................: 10000
vus_max........................: 10000
```
- 목표한 VU 수가 달성되었는지 확인

#### 4. Throughput (TPS)
```
http_reqs......................: 50000 (1666.67/s)
```
- 초당 처리 요청 수
- 많을수록 좋음

---

## 🎯 목표 설정 예시

### 채팅 메시지 전송
```javascript
// k6-chatroom-performance-test.js 수정
export let options = {
  stages: [
    { duration: '2m', target: 10000 },   // 10,000 동시 사용자로 증가
    { duration: '5m', target: 10000 },   // 5분간 유지
    { duration: '2m', target: 50000 },   // 50,000으로 증가
    { duration: '5m', target: 50000 },   // 5분간 유지
    { duration: '2m', target: 0 },       // 종료
  ],
  thresholds: {
    http_req_duration: ['p(95)<1000'],   // 95%가 1초 미만
    http_req_failed: ['rate<0.01'],      // 에러율 1% 미만
  },
};
```

### 로그인
```javascript
export let options = {
  vus: 1000,
  duration: '30s',
  thresholds: {
    http_req_duration: ['p(95)<500'],    // 95%가 500ms 미만
    http_req_failed: ['rate<0.001'],     // 에러율 0.1% 미만
  },
};
```

---

## 🐛 트러블슈팅

### 문제 1: "dial tcp: lookup localhost: no such host"
**원인**: Docker 네트워크 이슈

**해결:**
```bash
# k6 테스트에서 localhost 대신 host.docker.internal 사용
# 또는 Docker 컨테이너 IP 직접 사용
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' chat-server
```

### 문제 2: "too many open files"
**원인**: 시스템 파일 디스크립터 제한

**해결 (Mac/Linux):**
```bash
ulimit -n 65536
```

**해결 (Windows):**
- Docker Desktop 재시작
- WSL2 메모리 제한 해제

### 문제 3: "out of memory"
**원인**: Docker 메모리 부족

**해결:**
```bash
# Docker Compose 메모리 제한 조정
services:
  chat-server:
    mem_limit: 2g
    memswap_limit: 2g
```

### 문제 4: MySQL 연결 에러
**원인**: 커넥션 풀 부족

**해결:**
```yaml
# application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 100  # 기본 10 → 100으로 증가
      minimum-idle: 10
```

---

## 💰 비용 비교

| 환경 | VU 수 | 테스트 시간 | 비용 |
|------|-------|-------------|------|
| 로컬 Docker | 10,000 | 무제한 | $0 |
| 로컬 Docker | 100,000 | 무제한 | $0 |
| AWS t3.small | 10,000 | 1시간 | ~$0.02 |
| AWS t3.large | 100,000 | 1시간 | ~$0.08 |
| AWS 상시 운영 | 100,000 | 24/7 | ~$60/일 |

**결론: 로컬에서 충분합니다!**

---

## 📋 체크리스트

### 테스트 전
- [ ] Docker Compose 실행 확인
- [ ] 테스트 계정 생성 완료
- [ ] DB 인덱스 생성 완료
- [ ] 시스템 리소스 확인 (CPU, RAM)

### 테스트 중
- [ ] Docker stats로 리소스 모니터링
- [ ] 로그 실시간 확인
- [ ] k6 결과 실시간 관찰

### 테스트 후
- [ ] 결과 파일 저장
- [ ] 병목 구간 기록
- [ ] 개선 사항 정리
- [ ] 다음 테스트 계획 수립

---

## 🎓 학습 리소스

### k6 공식 문서
- https://k6.io/docs/

### 부하 테스트 모범 사례
- https://k6.io/docs/testing-guides/

### 프로젝트 내 참고 문서
- `K6_10만명_동시접속_테스트_가이드.md`
- `K6_부하테스트_결과_분석.md`
- `채팅_부하테스트_결과_분석.md`

---

## 🚀 다음 단계

1. **로컬에서 충분히 테스트**
   - 모든 기능 검증
   - 병목 찾기 및 개선
   - 목표 성능 달성

2. **AWS는 최종 검증용으로만**
   - 배포 직전 1-2회
   - 실제 네트워크 환경 테스트
   - 총 비용: $5-10

3. **포트폴리오 작성**
   - 로컬 테스트 결과 캡처
   - 개선 전후 비교
   - 문제 해결 과정 기록

**핵심: AWS 없이도 충분히 훌륭한 성능 테스트와 최적화가 가능합니다!** 🎉


