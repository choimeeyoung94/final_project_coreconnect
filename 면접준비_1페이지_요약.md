# 🎯 CoreConnect 면접 1페이지 핵심 요약

## 📌 개발 기능 (3가지)
1. **채팅**: WebSocket 실시간 채팅 (SockJS + STOMP)
2. **알림**: WebSocket 실시간 알림 시스템
3. **이메일**: SendGrid API 연동, 첨부파일, 예약 발송

---

## 🏗️ 기술 스택

### Frontend
- React 18, Material-UI
- SockJS-client, @stomp/stompjs
- Axios, Context API

### Backend
- Spring Boot 3.x, Java 17
- Spring WebSocket, Spring Security
- JPA/Hibernate, MySQL 8.0

### Infra
- Docker, Docker Compose
- Nginx (Reverse Proxy)
- AWS S3 (파일 저장)
- GitHub Actions (CI/CD)

### External API
- SendGrid (이메일 발송)

---

## 🔄 채팅 흐름 (10단계)

```
[Frontend]
1. 채팅방 입장 → 2. WebSocket 연결(/ws/chat) → 5. 토픽 구독(/topic/chat/{roomId}) → 6. 메시지 전송 → 10. 메시지 수신 & 화면 표시

[Nginx]
3. WebSocket 프록시 → boot-container:8080

[Backend]
4. JWT 인증(쿠키) → 7. @MessageMapping 처리 → 8. DB 저장(MySQL) → 9. 브로드캐스트(/topic/chat/{roomId})
```

---

## 🔔 알림 흐름 (7단계)

```
[Frontend]
1. 로그인 → 2. WebSocket 연결(/ws/notification) + 토픽 구독(/topic/notification/{userId}) → 6. 알림 수신 → 7. UI 표시(배지, 토스트)

[Backend]
3. 이벤트 발생(새 이메일 등) → 4. NotificationService.sendNotification() + DB 저장 → 5. WebSocket 전송(/topic/notification/{userId})
```

---

## 📧 이메일 흐름 (7단계)

```
[Frontend]
1. 이메일 작성 → 2. POST /api/v1/email/send (multipart)

[Backend]
3. EmailController → 4. 첨부파일 S3 업로드 + Email 엔티티 DB 저장 → 5. SendGrid API 호출 → 6. 수신자에게 알림 발송(WebSocket)

[Frontend]
7. "새 이메일 도착" 알림 수신
```

---

## 💡 핵심 기술 개념 (암기!)

### WebSocket
- **정의**: 양방향 실시간 통신 프로토콜
- **특징**: 지속적 연결, 낮은 지연시간, 낮은 오버헤드
- **Handshake**: HTTP로 시작 → Upgrade → WebSocket

### STOMP
- **정의**: Simple Text Oriented Messaging Protocol
- **역할**: WebSocket 위에서 메시징 패턴 제공 (Pub/Sub)
- **Prefix**: `/app` (client→server), `/topic` (broadcast), `/queue` (P2P)

### JWT 인증 흐름
1. 로그인 → 2. 서버가 JWT 발급 → 3. HttpOnly Cookie 저장 → 4. 이후 요청 시 자동 전송 → 5. 서버 검증

### SendGrid
- **역할**: 이메일 발송 전문 SaaS
- **장점**: 높은 전달률, SMTP 관리 불필요, API 간편
- **첨부파일**: Base64 인코딩하여 전송

---

## 🎤 필수 암기 질문 5개

### Q1: "왜 WebSocket 사용?" 
**A**: "실시간 양방향 통신 필요. HTTP Polling은 비효율적 (불필요한 트래픽, 지연). WebSocket은 한 번 연결 후 지속적 통신, 서버 푸시 가능"

### Q2: "STOMP 사용 이유?"
**A**: "순수 WebSocket은 저수준. STOMP는 메시지 라우팅, 구독 관리 자동화. Spring Boot와 완벽 통합 (@MessageMapping)"

### Q3: "WebSocket 인증 방법?"
**A**: "HandshakeInterceptor에서 쿠키의 JWT 토큰 검증. 인증 실패 시 연결 거부. HttpOnly Cookie로 XSS 방지"

### Q4: "SendGrid 사용 이유?"
**A**: "높은 전달률, 스팸 필터 통과, SMTP 서버 관리 불필요, 모니터링 제공. 직접 SMTP는 IP 평판 관리 어려움"

### Q5: "가장 어려웠던 점?"
**A**: "WebSocket 인증 처리. HttpOnly Cookie를 JS에서 못 읽음 → SockJS가 자동 전송하도록 구현. Nginx Upgrade 헤더 설정 필수"

---

## 🎬 데모 시나리오 (2분)

```
1. "로그인하겠습니다" → JWT 토큰 발급(개발자도구 쿠키 보여주기)

2. "채팅 기능입니다" → 채팅방 입장 → 메시지 전송 → 실시간 표시
   (개발자도구 WS 연결, STOMP 로그 보여주기)

3. "이메일 기능입니다" → 이메일 작성 → 첨부파일 추가 → 발송
   → "알림이 즉시 왔습니다" (알림 배지 강조)

4. "예약 발송도 가능합니다" → 예약 시간 설정 화면 보여주기

5. "백엔드 로그도 보여드리겠습니다" → SSH 접속 → docker logs
```

---

## 🚀 자신감 포인트

**말할 것**:
- ✅ "실제 배포된 서비스입니다" (http://54.116.26.182)
- ✅ "Docker로 컨테이너화하여 운영 중입니다"
- ✅ "GitHub Actions로 CI/CD 자동화했습니다"
- ✅ "WebSocket 양방향 통신을 구현한 경험이 있습니다"
- ✅ "트러블슈팅 경험이 풍부합니다" (예시 2-3개 준비)

**말하지 말 것**:
- ❌ "잘 모르겠습니다"
- ❌ "그냥 복붙했습니다"
- ❌ "구글링해서 했습니다" (→ "공식 문서를 참고했습니다"로 표현)

---

## 📋 면접 당일 체크리스트

**전날 밤**:
- [ ] 서버 정상 작동 확인 (http://54.116.26.182)
- [ ] 테스트 계정으로 로그인 테스트
- [ ] 채팅, 이메일, 알림 기능 모두 작동 확인
- [ ] Draw.io 파일 열어보기 (설명 가능한지 확인)

**당일 아침**:
- [ ] 서버 재확인 (혹시 모를 장애 대비)
- [ ] 노트북 충전
- [ ] 핵심 코드 복습 (WebSocketConfig, ChatSocket.js)
- [ ] 예상 질문 답변 소리내어 연습

**면접장**:
- [ ] 노트북 Wi-Fi 연결 테스트
- [ ] Draw.io 파일 미리 열어두기
- [ ] 브라우저 탭 준비: 
  - 사이트 (http://54.116.26.182)
  - GitHub 코드
  - 개발자도구 F12

---

## 🎯 마지막 조언

**기술 깊이 > 넓이**:
- 3가지 기능을 깊게 설명 (10분)
- 다른 기능은 간단히 (1분)

**자신감**:
- "구현했습니다" (과거형 ✅)
- "구현할 수 있을 것 같습니다" (추측형 ❌)

**솔직함**:
- 모르는 것은 "잘 모르지만, 이렇게 학습하겠습니다"
- 실수 인정: "처음에는 ~했다가, ~로 개선했습니다"

**성장 가능성**:
- "현재는 ~이지만, Redis/Kafka로 확장 가능합니다"
- "마이크로서비스로 분리할 계획입니다"

---

## ⏰ 시간 배분 (30분 면접 기준)

- **자기소개**: 3분
- **프로젝트 개요**: 2분
- **채팅 기능 설명**: 5분
- **알림 기능 설명**: 3분
- **이메일 기능 설명**: 3분
- **데모**: 5분
- **질의응답**: 9분

---

**화이팅! 내일 면접 성공하시길 바랍니다!** 🚀💪

궁금한 점 있으면 언제든 물어보세요!

