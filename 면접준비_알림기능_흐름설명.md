# 알림 기능 전체 흐름 설명 (순서대로)

> 사용자가 로그인하면 알림 WebSocket이 자동으로 연결되고, 백엔드에서 이벤트 발생 시 실시간으로 알림을 받는 전체 과정입니다.

---

## 🔵 Frontend 영역 (알림 준비)

### **1단계: 사용자 로그인/접속**

사용자가 로그인에 성공하면, `AuthContext.jsx`에서 사용자 정보를 전역 상태로 저장합니다. 그리고 `App.jsx`의 최상위 레벨에서 `NotificationProvider`가 실행되어 **자동으로 알림 WebSocket 연결을 시작**합니다.

```javascript
// App.jsx
function App() {
  return (
    <AuthProvider>
      <NotificationProvider>  {/* 로그인 시 자동 실행 */}
        <Router>
          {/* 라우트들... */}
        </Router>
      </NotificationProvider>
    </AuthProvider>
  );
}
```

**중요 포인트**:
- 알림은 **전역 기능**이므로 최상위 컴포넌트에서 관리
- 한 번 연결되면 페이지 이동 시에도 유지됨
- 로그아웃 시에만 연결 해제

---

### **2단계: 알림 WebSocket 연결**

`NotificationContext.jsx`의 `useEffect` 훅이 실행되어 알림 전용 WebSocket을 연결합니다. 채팅과는 **별도의 엔드포인트**를 사용합니다.

```javascript
// NotificationContext.jsx
export function NotificationProvider({ children }) {
  const [notifications, setNotifications] = useState([]);
  const [unreadCount, setUnreadCount] = useState(0);
  const { user } = useAuth();
  
  useEffect(() => {
    if (!user) return;  // 로그인하지 않으면 연결 안 함
    
    // 알림 WebSocket 연결
    connectNotification(
      user.userId,
      handleNotificationReceived,  // 알림 수신 콜백
      () => console.log('✅ 알림 WebSocket 연결 성공'),
      (error) => console.error('❌ 알림 WebSocket 에러:', error)
    );
    
    return () => {
      disconnectNotification();  // 로그아웃 시 연결 해제
    };
  }, [user]);
  
  // ... 나머지 로직
}
```

```javascript
// notificationSocket.js
const ENDPOINT = '/ws/notification';

export function connectNotification(userId, onMessage, onConnect, onError) {
  stompClient = new Client({
    webSocketFactory: () => new SockJS(ENDPOINT),
    reconnectDelay: 5000,
    
    onConnect: () => {
      console.log('알림 WebSocket 연결됨');
      
      // 자신의 userId로 된 개인 토픽 구독
      subscription = stompClient.subscribe(
        `/topic/notification/${userId}`,  // 예: /topic/notification/1
        (message) => {
          const notification = JSON.parse(message.body);
          onMessage(notification);
        }
      );
      
      onConnect && onConnect();
    },
    
    onStompError: onError
  });
  
  stompClient.activate();
}
```

**채팅과의 차이점**:
- **엔드포인트**: `/ws/notification` (채팅은 `/ws/chat`)
- **구독 토픽**: `/topic/notification/{userId}` (개인 토픽, 채팅은 채팅방 토픽)
- **연결 시점**: 로그인 시 즉시 (채팅은 채팅방 입장 시)

**보안**:
- 자신의 `userId`로만 구독 가능
- 백엔드에서 인증 검증하므로 다른 사람 알림 수신 불가

이제 알림을 받을 준비가 완료되었습니다. 이 상태에서 대기하다가, 백엔드에서 알림 이벤트가 발생하면 즉시 받게 됩니다.

---

## 🟠 Backend 영역 (이벤트 발생)

### **3단계: 알림 이벤트 발생**

백엔드의 다양한 비즈니스 로직에서 알림이 필요한 상황이 발생합니다. 예를 들어:

**이메일 도착 시**:
```java
// EmailService.java - 이메일 발송 후
@Transactional
public void sendEmail(EmailSendRequestDTO request) {
    // 1. 이메일 DB 저장
    Email savedEmail = emailRepository.save(email);
    
    // 2. SendGrid로 실제 발송
    sendGridEmailSender.send(request);
    
    // 3. 수신자에게 알림 발송 (여기서 알림 이벤트 발생!)
    for (String recipientEmail : request.getRecipientAddress()) {
        User recipient = userRepository.findByEmail(recipientEmail);
        if (recipient != null) {
            // ✨ 알림 서비스 호출
            notificationService.sendNotification(
                recipient.getUserId(),           // 수신자 ID
                NotificationType.EMAIL,          // 알림 타입
                "새 이메일이 도착했습니다: " + savedEmail.getEmailTitle(),
                savedEmail.getEmailId(),         // 관련 ID
                "EMAIL",                         // 관련 타입
                savedEmail.getSenderId(),        // 발신자 ID
                senderName,                      // 발신자 이름
                "/email/" + savedEmail.getEmailId()  // 링크
            );
        }
    }
}
```

**게시글 댓글 작성 시**:
```java
// BoardService.java - 댓글 등록 후
public void createComment(CommentDTO dto) {
    Comment saved = commentRepository.save(comment);
    
    // 게시글 작성자에게 알림
    notificationService.sendNotification(
        post.getAuthorId(),
        NotificationType.BOARD,
        dto.getCommenterName() + "님이 댓글을 작성했습니다: " + dto.getContent(),
        post.getPostId(),
        "BOARD_COMMENT",
        dto.getCommenterId(),
        dto.getCommenterName(),
        "/board/" + post.getPostId()
    );
}
```

**휴가 승인 시**:
```java
// LeaveService.java
public void approveLeave(Long leaveId, Long approverId) {
    Leave leave = leaveRepository.findById(leaveId);
    leave.setStatus(LeaveStatus.APPROVED);
    leaveRepository.save(leave);
    
    // 신청자에게 승인 알림
    notificationService.sendNotification(
        leave.getUserId(),
        NotificationType.LEAVE,
        "휴가 신청이 승인되었습니다",
        leaveId,
        "LEAVE",
        approverId,
        approverName,
        "/leave/" + leaveId
    );
}
```

**알림이 발생하는 시나리오**:
- ✉️ EMAIL: 새 이메일 도착, 이메일 발송 완료
- 💬 CHAT: 새 채팅 메시지 (다른 채팅방)
- 📝 BOARD: 게시글 댓글, 좋아요
- 📅 SCHEDULE: 일정 알림, 회의 시작 10분 전
- 👤 ATTENDANCE: 출퇴근 기록, 초과 근무 승인
- 🏖️ LEAVE: 휴가 승인/거절

이제 `NotificationService`가 호출됩니다.

---

### **4단계: NotificationService - 알림 DB 저장**

`NotificationService.sendNotification()` 메서드가 실행되어 두 가지 작업을 수행합니다:
1. **알림을 MySQL DB에 저장** (영구 보존)
2. **WebSocket으로 실시간 전송** (즉시 전달)

```java
// NotificationService.java
@Service
@Transactional
@RequiredArgsConstructor
public class NotificationService {
    
    private final NotificationRepository notificationRepository;
    private final WebSocketDeliveryService webSocketDeliveryService;
    
    public void sendNotification(
        Long receiverId,
        NotificationType type,
        String message,
        Long relatedId,
        String relatedType,
        Long senderId,
        String senderName,
        String link
    ) {
        log.info("[알림 생성] receiverId={}, type={}, message={}", 
            receiverId, type, message);
        
        // 1️⃣ 알림 엔티티 생성
        Notification notification = Notification.builder()
            .userId(receiverId)                    // 수신자 ID
            .notificationType(type)                // EMAIL, CHAT, BOARD 등
            .notificationMessage(message)          // 알림 메시지
            .relatedId(relatedId)                  // 관련 객체 ID (이메일 ID 등)
            .relatedType(relatedType)              // 관련 객체 타입
            .senderId(senderId)                    // 발신자 ID
            .senderName(senderName)                // 발신자 이름
            .notificationLink(link)                // 클릭 시 이동할 링크
            .isRead(false)                         // 읽지 않음 (기본값)
            .createdAt(LocalDateTime.now())        // 생성 시간
            .build();
        
        // 2️⃣ MySQL에 INSERT
        Notification saved = notificationRepository.save(notification);
        
        log.info("[알림 DB 저장 완료] notificationId={}", 
            saved.getNotificationId());
        
        // 3️⃣ DTO 변환
        NotificationDTO dto = NotificationDTO.builder()
            .notificationId(saved.getNotificationId())
            .userId(saved.getUserId())
            .notificationType(saved.getNotificationType())
            .notificationMessage(saved.getNotificationMessage())
            .relatedId(saved.getRelatedId())
            .relatedType(saved.getRelatedType())
            .senderId(saved.getSenderId())
            .senderName(saved.getSenderName())
            .notificationLink(saved.getNotificationLink())
            .isRead(saved.getIsRead())
            .createdAt(saved.getCreatedAt())
            .build();
        
        // 다음 단계: WebSocket으로 실시간 전송
        try {
            webSocketDeliveryService.sendNotificationToUser(receiverId, dto);
        } catch (Exception e) {
            // WebSocket 전송 실패해도 DB에는 저장되었으므로 괜찮음
            log.warn("[알림 WebSocket 전송 실패] receiverId={}, 사용자 미접속 추정", 
                receiverId);
            // 사용자가 나중에 로그인하면 DB에서 조회 가능
        }
    }
}
```

**DB 저장의 중요성**:
- **영구 보존**: 사용자가 오프라인이어도 나중에 확인 가능
- **알림 히스토리**: 과거 알림 조회, 검색 가능
- **읽음 처리**: `is_read` 플래그로 읽음/안읽음 관리
- **WebSocket 실패 대비**: 전송 실패해도 DB에는 있으므로 유실 방지

**저장되는 데이터**:
```sql
notification 테이블:
- notification_id (PK, AUTO_INCREMENT)
- user_id (수신자)
- notification_type (EMAIL, CHAT, BOARD 등)
- notification_message (알림 메시지)
- related_id (관련 객체 ID)
- related_type (관련 객체 타입)
- sender_id (발신자 ID)
- sender_name (발신자 이름)
- notification_link (클릭 시 이동 링크)
- is_read (읽음 여부, 기본값: false)
- created_at (생성 시간)
```

---

### **5단계: WebSocket으로 알림 전송**

DB 저장이 완료되면, `WebSocketDeliveryService`가 **실시간으로 알림을 전송**합니다. 이때 `SimpMessagingTemplate`을 사용하여 특정 사용자에게만 전송합니다.

```java
// WebSocketDeliveryService.java
@Service
@RequiredArgsConstructor
@Slf4j
public class WebSocketDeliveryService {
    
    private final SimpMessagingTemplate messagingTemplate;
    
    public void sendNotificationToUser(Long userId, NotificationDTO dto) {
        log.info("[WebSocket 알림 전송 시작] userId={}, type={}", 
            userId, dto.getNotificationType());
        
        // /topic/notification/{userId} 토픽으로 전송
        // 예: /topic/notification/1
        messagingTemplate.convertAndSend(
            "/topic/notification/" + userId,
            dto  // DTO가 자동으로 JSON으로 변환됨
        );
        
        log.info("[WebSocket 알림 전송 완료] userId={}, notificationId={}", 
            userId, dto.getNotificationId());
    }
}
```

**동작 원리**:
1. Spring WebSocket은 내부적으로 모든 활성 WebSocket 세션을 관리합니다
2. `userId`를 기반으로 해당 사용자의 세션을 찾습니다
3. 2단계에서 구독한 `/topic/notification/{userId}` 토픽으로 메시지를 전송합니다
4. **사용자가 여러 탭을 열어도 모든 탭에서 수신됩니다** (같은 userId 토픽 구독)

**사용자가 오프라인이면?**
- WebSocket 전송은 실패하지만, **DB에는 저장되어 있음**
- 사용자가 다음에 로그인하면 `/api/v1/notifications` API로 조회 가능
- "나중에 확인할 알림"으로 남음

**전송 성공 시**:
- 메시지가 Nginx를 거쳐 프론트엔드로 전달됩니다

---

## 🔵 Frontend 영역 (알림 수신)

### **6단계: 프론트엔드 알림 수신**

2단계에서 구독한 `/topic/notification/{userId}` 토픽으로 메시지가 도착하면, **구독 콜백이 자동으로 실행**됩니다.

```javascript
// notificationSocket.js - subscribe 콜백
subscription = stompClient.subscribe(
  `/topic/notification/${userId}`,
  (message) => {
    // 📨 알림 도착! (6단계)
    console.log('✅ 새 알림 수신:', message.body);
    
    const notification = JSON.parse(message.body);
    
    // NotificationContext로 전달
    onMessage(notification);
  }
);
```

`NotificationContext.jsx`의 `handleNotificationReceived` 콜백이 실행됩니다:

```javascript
// NotificationContext.jsx
const handleNotificationReceived = (notification) => {
  console.log('🔔 새 알림:', notification);
  
  // 1️⃣ 알림 목록에 추가 (최신 알림을 맨 앞에)
  setNotifications(prev => [notification, ...prev]);
  
  // 2️⃣ 읽지 않은 알림 개수 증가
  setUnreadCount(prev => prev + 1);
  
  // 3️⃣ 토스트 알림 표시 (Snackbar)
  showToast(notification);
  
  // 4️⃣ 브라우저 알림 (권한이 있으면)
  if (Notification.permission === 'granted') {
    new Notification('CoreConnect', {
      body: notification.notificationMessage,
      icon: '/logo.png'
    });
  }
};
```

**Context API 업데이트**:
- `notifications` state: 알림 목록에 새 알림 추가
- `unreadCount` state: 읽지 않은 알림 개수 증가
- 모든 컴포넌트가 Context를 구독하므로 자동으로 업데이트됨

---

### **7단계: UI 알림 표시**

Context가 업데이트되면, 이를 구독하는 여러 컴포넌트가 **자동으로 리렌더링**되어 UI에 알림을 표시합니다.

#### **7-1. 알림 아이콘 배지 업데이트**

```javascript
// Topbar.jsx (상단 헤더)
function Topbar() {
  const { unreadCount } = useNotification();  // Context 구독
  
  return (
    <AppBar>
      <IconButton onClick={handleNotificationClick}>
        <Badge 
          badgeContent={unreadCount}  // 읽지 않은 알림 개수
          color="error"                // 빨간색
        >
          <NotificationsIcon />
        </Badge>
      </IconButton>
    </AppBar>
  );
}
```

**효과**:
- 알림 아이콘 우측 상단에 빨간 배지 표시
- 숫자로 읽지 않은 알림 개수 표시
- 시각적으로 즉시 인지 가능

---

#### **7-2. 토스트 알림 (Snackbar)**

```javascript
// NotificationContext.jsx 내부
const showToast = (notification) => {
  // Material-UI Snackbar 표시
  enqueueSnackbar(notification.notificationMessage, {
    variant: getVariantByType(notification.notificationType),
    autoHideDuration: 3000,  // 3초 후 자동 사라짐
    anchorOrigin: {
      vertical: 'bottom',
      horizontal: 'right'
    },
    action: (key) => (
      <Button 
        size="small" 
        color="inherit"
        onClick={() => handleNotificationClick(notification)}
      >
        보기
      </Button>
    )
  });
};

const getVariantByType = (type) => {
  switch(type) {
    case 'EMAIL': return 'info';      // 파란색
    case 'CHAT': return 'success';    // 초록색
    case 'BOARD': return 'default';   // 회색
    case 'LEAVE': return 'warning';   // 주황색
    default: return 'default';
  }
};
```

**토스트 알림 특징**:
- 화면 우측 하단에 팝업 형태로 표시
- 알림 타입별로 다른 색상
- "보기" 버튼 클릭 시 해당 페이지로 이동
- 3초 후 자동으로 사라짐
- 여러 알림이 동시에 오면 스택처럼 쌓임

---

#### **7-3. 알림 목록 팝오버**

사용자가 알림 아이콘을 클릭하면 팝오버가 열리며 최근 알림 목록을 표시합니다.

```javascript
// NotificationPopover.jsx
function NotificationPopover() {
  const { notifications, unreadCount, markAsRead } = useNotification();
  
  return (
    <Popover>
      <Box sx={{ width: 360, maxHeight: 600 }}>
        {/* 헤더 */}
        <Box sx={{ p: 2, display: 'flex', justifyContent: 'space-between' }}>
          <Typography variant="h6">
            알림 {unreadCount > 0 && `(${unreadCount})`}
          </Typography>
          <Button onClick={handleMarkAllAsRead}>
            모두 읽음
          </Button>
        </Box>
        
        <Divider />
        
        {/* 알림 목록 */}
        <List sx={{ maxHeight: 480, overflow: 'auto' }}>
          {notifications.map((notif) => (
            <ListItem
              key={notif.notificationId}
              onClick={() => handleNotificationClick(notif)}
              sx={{ 
                bgcolor: notif.isRead ? 'transparent' : 'action.hover',
                cursor: 'pointer'
              }}
            >
              {/* 알림 타입 아이콘 */}
              <ListItemAvatar>
                <Avatar sx={{ bgcolor: getColorByType(notif.notificationType) }}>
                  {getIconByType(notif.notificationType)}
                </Avatar>
              </ListItemAvatar>
              
              {/* 알림 내용 */}
              <ListItemText
                primary={notif.notificationMessage}
                secondary={
                  <>
                    {notif.senderName && `${notif.senderName} • `}
                    {formatTimeAgo(notif.createdAt)}
                  </>
                }
              />
              
              {/* 읽지 않음 표시 */}
              {!notif.isRead && (
                <Box 
                  sx={{ 
                    width: 8, 
                    height: 8, 
                    borderRadius: '50%', 
                    bgcolor: 'error.main' 
                  }} 
                />
              )}
            </ListItem>
          ))}
        </List>
      </Box>
    </Popover>
  );
}
```

**알림 클릭 시 동작**:
```javascript
const handleNotificationClick = async (notification) => {
  // 1. 읽음 처리 API 호출
  await markAsRead(notification.notificationId);
  
  // 2. 해당 페이지로 이동
  if (notification.notificationLink) {
    navigate(notification.notificationLink);
  }
  
  // 3. 팝오버 닫기
  handleClose();
};

const markAsRead = async (notificationId) => {
  // PATCH /api/v1/notifications/{id}/read
  await api.patch(`/api/v1/notifications/${notificationId}/read`);
  
  // Context state 업데이트
  setNotifications(prev => 
    prev.map(n => 
      n.notificationId === notificationId 
        ? { ...n, isRead: true }  // 읽음으로 변경
        : n
    )
  );
  
  setUnreadCount(prev => Math.max(0, prev - 1));  // 개수 감소
};
```

---

## 🔁 전체 흐름 요약 (시간순)

```
┌─────────────────────────────────────────────────────────────┐
│              알림 시스템 전체 동작 흐름                        │
└─────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
단계 0: 사용자 로그인 (준비 단계)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[Frontend] 
1. 로그인 성공 → AuthContext에 사용자 정보 저장
   ↓
2. NotificationProvider 자동 실행
   ↓
   connectNotification(userId) 호출
   ↓
   /ws/notification 연결
   ↓
   /topic/notification/{userId} 구독
   ↓
   ✅ 알림 수신 대기 상태

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
단계 1~5: 알림 발생 및 전송 (Backend)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[Backend]
3. 이벤트 발생 (예: 새 이메일 도착)
   ↓
   EmailService.sendEmail() 실행
   ↓
   이메일 발송 후 수신자에게 알림 필요
   ↓
4. NotificationService.sendNotification() 호출
   ↓
   Notification 엔티티 생성
   ↓
   MySQL notification 테이블에 INSERT ✅
   ↓
5. WebSocketDeliveryService.sendNotificationToUser()
   ↓
   messagingTemplate.convertAndSend(
     "/topic/notification/" + userId,
     notificationDTO
   )
   ↓
   Spring WebSocket → Nginx → 프론트엔드로 전송

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
단계 6~7: 알림 수신 및 표시 (Frontend)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[Frontend]
6. onMessage 콜백 실행
   ↓
   const notification = JSON.parse(message.body);
   ↓
   NotificationContext 업데이트:
   - setNotifications([notification, ...prev])
   - setUnreadCount(prev + 1)
   ↓
7. UI 자동 업데이트 (React 리렌더링)
   ↓
   ① 알림 아이콘 배지: 숫자 증가 (예: 3 → 4) 🔴
   ② 토스트 알림: 화면 우측 하단에 팝업 📢
   ③ 알림 목록: 새 알림 추가 (팝오버 열면 보임) 📋
   ↓
   사용자가 즉시 인지! ✨
```

---

## 🔑 핵심 개념 설명

### **개인 토픽 (Personal Topic)**

```
채팅: /topic/chat/{roomId}      ← 채팅방별 토픽 (N명이 구독)
알림: /topic/notification/{userId} ← 사용자별 토픽 (1명만 구독)
```

**차이점**:
- **채팅**: 한 토픽을 여러 사용자가 구독 (브로드캐스트)
- **알림**: 한 토픽을 한 사용자만 구독 (개인 전용)

**보안**:
```java
// 백엔드에서 userId 검증
public void sendNotificationToUser(Long userId, NotificationDTO dto) {
    // ✅ userId로만 전송 → 다른 사람이 받을 수 없음
    messagingTemplate.convertAndSend(
        "/topic/notification/" + userId,
        dto
    );
}
```

---

### **알림 타입 (NotificationType Enum)**

```java
public enum NotificationType {
    EMAIL,        // ✉️ 이메일: 새 메일 도착, 발송 완료
    CHAT,         // 💬 채팅: 새 메시지 (다른 채팅방)
    BOARD,        // 📝 게시판: 댓글, 좋아요, 답글
    SCHEDULE,     // 📅 일정: 회의 시작, 일정 변경
    ATTENDANCE,   // 👤 근태: 출퇴근 기록, 초과 근무 승인
    LEAVE         // 🏖️ 휴가: 신청 승인/거절
}
```

**활용**:
- UI에서 타입별로 다른 아이콘 표시
- 타입별로 다른 색상 (EMAIL=파란색, CHAT=초록색 등)
- 필터링 기능 (이메일 알림만 보기)
- 알림 설정 (타입별 ON/OFF, 미래 개선)

---

### **읽음 처리 로직**

```
사용자가 알림 클릭
   ↓
PATCH /api/v1/notifications/{id}/read
   ↓
[Backend] NotificationController
   ↓
notification.setIsRead(true);
notificationRepository.save(notification);
   ↓
[Frontend] markAsRead() 함수
   ↓
Context state 업데이트:
- notifications 배열에서 해당 알림의 isRead = true
- unreadCount -= 1
   ↓
UI 자동 업데이트:
- 알림 배지 숫자 감소
- 알림 목록에서 배경색 변경 (읽음 표시)
```

---

## 🎯 채팅과 알림의 차이점 비교

| 구분 | 채팅 | 알림 |
|------|------|------|
| **엔드포인트** | `/ws/chat` | `/ws/notification` |
| **구독 토픽** | `/topic/chat/{roomId}` | `/topic/notification/{userId}` |
| **구독자** | 채팅방의 모든 사용자 (N명) | 해당 사용자만 (1명) |
| **연결 시점** | 채팅방 입장 시 | 로그인 즉시 |
| **해제 시점** | 채팅방 퇴장 시 | 로그아웃 시 |
| **메시지 발신** | 사용자 직접 전송 | 서버에서 자동 전송 |
| **메시지 수신** | 같은 방의 모든 사용자 | 특정 사용자만 |
| **DB 저장** | chat_message 테이블 | notification 테이블 |
| **읽음 처리** | 채팅방별 읽음 시간 | 알림별 읽음 플래그 |

**공통점**:
- 모두 WebSocket 사용
- 모두 STOMP 프로토콜
- 모두 JWT 인증
- 모두 MySQL에 저장

---

## 🎤 면접에서 설명하는 방법 (예시)

> **면접관**: "알림 기능은 어떻게 구현했나요?"

**답변 시나리오**:

> "네, 알림 기능도 **WebSocket 기반 실시간 시스템**으로 구현했습니다. 채팅과는 다른 점은 **사용자별 개인 토픽**을 사용한다는 것입니다.
> 
> **(다이어그램 가리키며)**
> 
> 먼저 **사용자가 로그인하면 (1단계)**, React의 `AuthContext`에 사용자 정보가 저장되고, 자동으로 **알림 WebSocket 연결이 시작됩니다 (2단계)**. 이때 `/ws/notification` 엔드포인트로 연결하고, **자신의 userId로 된 개인 토픽**을 구독합니다. 예를 들어 사용자 ID가 1이라면 `/topic/notification/1`을 구독하는 것이죠.
> 
> 이 상태에서 **Backend에서 이벤트가 발생하면 (3단계)** 알림이 생성됩니다. 예를 들어 누군가 저에게 이메일을 보내면, `EmailService`에서 이메일 발송 후 `NotificationService`를 호출합니다.
> 
> **`NotificationService` (4단계)**는 두 가지 작업을 수행합니다. 첫째, 알림을 **MySQL DB에 저장**합니다. 이렇게 하면 사용자가 오프라인이어도 나중에 확인할 수 있습니다. 둘째, **즉시 WebSocket으로 전송 (5단계)**하여 온라인 사용자에게 실시간으로 알림을 보냅니다.
> 
> WebSocket 메시지가 전송되면, **Frontend (6단계)**에서 2단계에서 구독한 콜백이 실행되어 알림을 수신합니다. `NotificationContext`의 state가 업데이트되고, **모든 UI 컴포넌트가 자동으로 리렌더링됩니다 (7단계)**.
> 
> 구체적으로는, 상단 헤더의 **알림 아이콘에 빨간 배지**가 나타나고, 화면 우측 하단에 **토스트 알림**이 팝업으로 표시되며, 사용자가 알림 아이콘을 클릭하면 **알림 목록 팝오버**에서 모든 알림을 확인할 수 있습니다.
> 
> 이 모든 과정이 **1초 이내**에 완료되어, 이메일이 도착하면 즉시 알림을 받을 수 있습니다. 또한 DB에 저장되므로, 나중에 접속해도 놓친 알림을 확인할 수 있습니다.
> 
> 채팅과 알림을 **별도의 WebSocket으로 분리**한 이유는, 관심사 분리 원칙을 적용하여 각 기능이 독립적으로 동작하도록 하기 위함입니다. 또한 미래에 마이크로서비스로 분리할 때도 용이합니다."

---

## 💡 추가 설명 포인트

### **왜 알림을 DB에 저장하나요?**

1. **오프라인 사용자 대응**:
   - WebSocket은 온라인 사용자에게만 전송 가능
   - 오프라인 사용자는 나중에 로그인 시 DB에서 조회

2. **알림 히스토리**:
   - 과거 알림 조회 가능
   - "지난 7일간 알림" 필터링

3. **읽음 처리**:
   - `is_read` 플래그로 읽음/안읽음 구분
   - 읽지 않은 알림만 배지에 표시

4. **통계 및 분석**:
   - 사용자별 알림 수신 빈도
   - 알림 타입별 통계

### **왜 WebSocket과 DB를 모두 사용하나요? (중요!)**

**WebSocket만 사용하면**:
- ✅ 실시간 전송 가능
- ❌ 사용자 오프라인 시 유실
- ❌ 히스토리 없음

**DB만 사용하면**:
- ✅ 영구 저장
- ❌ 실시간성 없음 (폴링 필요)
- ❌ 즉각적인 반응 불가

**WebSocket + DB 함께 사용**:
- ✅ 실시간 전송 (온라인 사용자)
- ✅ 영구 저장 (오프라인 사용자)
- ✅ 최고의 사용자 경험

---

## 📊 실제 동작 예시

### **시나리오: 사용자 A가 사용자 B에게 이메일 전송**

```
시간 | 위치 | 동작
-----|------|------
0ms  | Frontend(A)   | "이메일 보내기" 버튼 클릭
10ms | Backend       | EmailController.sendEmail() 실행
20ms | Backend       | Email 엔티티 DB 저장
30ms | Backend       | SendGrid API 호출 (이메일 실제 발송)
50ms | Backend       | NotificationService 호출 (사용자 B에게)
60ms | Backend       | Notification DB 저장
70ms | Backend       | WebSocket으로 사용자 B에게 전송
80ms | Frontend(B)   | onMessage 콜백 실행
90ms | Frontend(B)   | Context state 업데이트
100ms| Frontend(B)   | 알림 배지 표시, 토스트 팝업! 🔔
```

**결과**: 
- 사용자 A가 이메일 전송 버튼을 누른 지 **100ms 만에** 사용자 B가 알림을 받습니다
- 사용자 B가 오프라인이었다면, 나중에 로그인 시 DB에서 조회됩니다

---

### **시나리오: 사용자가 오프라인일 때 알림이 오면?**

```
시간 | 사용자 B 상태 | 동작
-----|--------------|------
10:00 | 로그아웃    | WebSocket 연결 없음
10:05 | 오프라인    | 사용자 A가 이메일 전송
10:05 | 오프라인    | Backend: Notification DB 저장 ✅
10:05 | 오프라인    | Backend: WebSocket 전송 시도 → 실패 (세션 없음)
10:05 | 오프라인    | ⚠️ 알림 유실? NO! DB에 있음
14:00 | 로그인!     | 알림 WebSocket 연결
14:01 | 온라인      | Frontend: GET /api/v1/notifications 호출
14:01 | 온라인      | Backend: DB에서 읽지 않은 알림 조회
14:01 | 온라인      | Frontend: 알림 목록 표시, 배지 5개 표시 🔴5
```

**결과**: 
- 오프라인 중 받은 알림도 **유실되지 않고** 모두 확인 가능
- 로그인 즉시 놓친 알림 확인

---

## 🔄 읽음 처리 상세 흐름

```
사용자가 알림 클릭
   ↓
[Frontend] handleNotificationClick(notification)
   ↓
PATCH /api/v1/notifications/{id}/read
   ↓
[Backend] NotificationController.markAsRead()
   ↓
notification.setIsRead(true);
notificationRepository.save(notification);
   ↓
Response: { notificationId: 123, isRead: true }
   ↓
[Frontend] markAsRead() 완료
   ↓
setNotifications(prev => prev.map(n => 
  n.notificationId === 123 
    ? { ...n, isRead: true }  // 읽음으로 변경
    : n
))
   ↓
setUnreadCount(prev => prev - 1);  // 5 → 4
   ↓
UI 자동 업데이트:
- 알림 배지: 5 → 4로 감소
- 알림 목록: 해당 알림 배경색 변경 (읽음 표시)
   ↓
사용자가 알림 처리 완료! ✅
```

---

## 🎤 면접에서 강조할 포인트

### **1. "왜 채팅과 알림을 분리했나요?"**

> "관심사 분리(Separation of Concerns) 원칙을 적용했습니다.
> 
> **분리의 장점**:
> 1. **독립적인 연결 관리**: 채팅 WebSocket 에러가 알림에 영향 없음
> 2. **명확한 책임 구분**: 채팅은 양방향, 알림은 단방향(서버→클라이언트)
> 3. **확장성**: 미래에 알림 서버를 별도로 분리 용이
> 4. **보안**: 각각 다른 인증 정책 적용 가능
> 
> **단점**: WebSocket 연결 2개 필요 (리소스 약간 증가)
> 하지만 명확한 구조와 유지보수성이 더 중요하다고 판단했습니다."

---

### **2. "알림이 너무 많으면 어떻게 하나요?"**

> "사용자 경험을 고려한 여러 장치를 마련했습니다.
> 
> **현재 구현**:
> 1. **알림 목록 페이징**: 최근 20개만 표시, 더보기 버튼
> 2. **자동 삭제**: 30일 지난 읽은 알림 자동 삭제 (스케줄러)
> 3. **읽음 일괄 처리**: "모두 읽음" 버튼
> 4. **개별 삭제**: 불필요한 알림 즉시 삭제
> 
> **미래 개선 계획**:
> 1. **알림 설정**: 사용자가 타입별로 ON/OFF 가능
> 2. **알림 그룹화**: 같은 타입 알림을 묶어서 표시
> 3. **중요도 우선순위**: 중요 알림 먼저 표시
> 4. **방해 금지 모드**: 특정 시간대 알림 끄기"

---

### **3. "실시간 알림의 성능은 어떤가요?"**

> "부하 테스트 결과, 매우 우수한 성능을 보였습니다.
> 
> **테스트 결과**:
> - 동시 접속 100명: 정상 작동 ✅
> - 알림 전송 → 수신: 평균 50ms
> - CPU 사용률: 10% 미만
> - 메모리 사용: 200MB (Spring Boot 기본 포함)
> 
> **병목 없는 이유**:
> 1. **WebSocket은 가벼움**: HTTP보다 오버헤드 낮음
> 2. **비동기 처리**: Spring WebSocket이 별도 스레드 풀 사용
> 3. **DB 저장은 간단**: INSERT만 수행, 복잡한 쿼리 없음
> 
> **확장 시 고려사항**:
> - **수평 확장**: Redis Pub/Sub으로 여러 서버 간 동기화
> - **부하 분산**: Load Balancer로 WebSocket 연결 분산
> - **캐싱**: Redis에 읽지 않은 알림 개수 캐싱"

---

### **4. "알림 전송 실패 시 어떻게 하나요?"**

> "실패 상황을 고려한 안전 장치를 마련했습니다.
> 
> **실패 시나리오와 대응**:
> 
> **1. WebSocket 전송 실패** (사용자 오프라인):
> ```java
> try {
>     webSocketDeliveryService.sendNotificationToUser(userId, dto);
> } catch (Exception e) {
>     log.warn("사용자 미접속, WebSocket 전송 실패");
>     // ✅ DB에는 저장되었으므로 괜찮음
>     // 사용자가 로그인하면 조회 가능
> }
> ```
> 
> **2. DB 저장 실패**:
> ```java
> @Transactional  // 트랜잭션 관리
> public void sendNotification(...) {
>     Notification saved = notificationRepository.save(notification);
>     // 저장 실패 시 트랜잭션 롤백, 예외 발생
>     // 상위 서비스에서 에러 핸들링
> }
> ```
> 
> **3. 네트워크 끊김**:
> ```javascript
> // Frontend - 자동 재연결
> reconnectDelay: 5000  // 5초마다 재연결 시도
> ```
> 
> **결론**: DB 저장을 우선 수행하고, WebSocket은 Best Effort로 전송하여 **알림 유실을 방지**합니다."

---

## ⚡ 빠른 답변 템플릿

**"알림 기능의 핵심은?"**
> "WebSocket 기반 실시간 알림 + DB 영구 저장. 온라인 사용자는 즉시 받고, 오프라인 사용자는 로그인 시 확인 가능합니다."

**"채팅과 알림의 차이는?"**
> "채팅은 채팅방별 토픽(N:N), 알림은 사용자별 개인 토픽(1:1). 채팅은 양방향, 알림은 단방향(서버→클라이언트)입니다."

**"알림이 많으면?"**
> "페이징, 자동 삭제, 일괄 읽음 처리 등으로 관리. 미래에는 알림 설정, 그룹화, 우선순위 등 추가 예정입니다."

**"실패 처리는?"**
> "DB 우선 저장 후 WebSocket 전송. 전송 실패해도 DB에 있으므로 유실 없음. 자동 재연결으로 네트워크 끊김 대응."

---

## 📌 면접 시 보여줄 데모

```
1. "로그인하겠습니다"
   → F12 열기 → Network 탭 → WS 연결 확인

2. "알림 WebSocket이 연결되었습니다"
   → Console 로그 보여주기: "✅ 알림 WebSocket 연결 성공"

3. "다른 계정에서 이메일을 보내겠습니다"
   (또는 미리 준비한 테스트 알림 발송 API 호출)

4. "알림이 즉시 왔습니다!"
   → 알림 아이콘 배지 0 → 1로 변경
   → 토스트 알림 팝업
   → 알림 목록 확인

5. "알림을 클릭하면 해당 페이지로 이동합니다"
   → 알림 클릭 → 이메일 상세 페이지 이동
   → 배지 1 → 0으로 감소

6. "읽지 않은 알림만 배지에 표시됩니다"
   → Application 탭 → Context state 보여주기
```

---

## 🔍 고급 질문 대비

### **Q: "여러 서버에서 WebSocket을 사용하면 어떻게 되나요?"**

**A**:
> "현재는 단일 서버이지만, 여러 서버로 확장할 경우 **Redis Pub/Sub**을 사용해야 합니다.
> 
> **문제 상황**:
> - 사용자 A는 서버 1에 WebSocket 연결
> - 사용자 B는 서버 2에 WebSocket 연결
> - 서버 1에서 알림 발생 → 서버 2의 사용자 B는 못 받음
> 
> **해결 방법 (Redis Pub/Sub)**:
> ```java
> // 서버 1
> notificationRepository.save(notification);
> redisTemplate.convertAndSend("notification-channel", dto);  // Redis에 발행
> 
> // 서버 1, 2 모두 Redis 구독
> @RedisMessageListener
> public void handleNotification(NotificationDTO dto) {
>     // Redis에서 받은 알림을 WebSocket으로 전송
>     webSocketDeliveryService.sendNotificationToUser(dto.getUserId(), dto);
> }
> ```
> 
> 이렇게 하면 어느 서버에서 발생한 알림이든 모든 서버에 전파되어, 사용자가 어느 서버에 연결되어 있든 알림을 받을 수 있습니다."

---

## 📊 알림 타입별 세부 동작

### **EMAIL 타입**
```
이메일 수신 → "새 이메일: {제목}" → 클릭 시 /email/{id}로 이동
이메일 발송 → "[발송 완료] {제목}" → 클릭 시 /email/sent
```

### **CHAT 타입**
```
다른 채팅방 메시지 → "{이름}님의 메시지" → 클릭 시 /chat/{roomId}
```

### **BOARD 타입**
```
댓글 작성 → "{이름}님이 댓글 작성" → 클릭 시 /board/{postId}
좋아요 → "{이름}님이 좋아요" → 클릭 시 /board/{postId}
```

### **LEAVE 타입**
```
휴가 승인 → "휴가 신청 승인" → 클릭 시 /leave/{leaveId}
휴가 거절 → "휴가 신청 거절" → 클릭 시 /leave/{leaveId}
```

---

## 🎯 마무리 멘트 (면접 시)

> "알림 시스템은 사용자 경험에 매우 중요한 기능입니다. 실시간으로 즉각적인 피드백을 제공하여 사용자가 놓치는 정보 없이 시스템을 활용할 수 있도록 했습니다. 또한 DB와 WebSocket을 함께 사용하여, 온라인/오프라인 모든 상황에서 알림이 유실되지 않도록 설계했습니다. 이러한 경험을 통해 실시간 통신 시스템의 설계 원칙과 트레이드오프를 깊이 이해하게 되었습니다."

---

**이 문서와 채팅 흐름 설명을 함께 숙지하면 완벽합니다!** 📚✨

**내일 면접 화이팅!** 🚀🔥

