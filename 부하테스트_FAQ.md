# 🤔 부하 테스트 & 비용 절감 FAQ

## 💰 비용 관련

### Q1: 서버를 내리면 k6로 부하 테스트를 할 수 없나요?

**A: 아니요, 할 수 있습니다!**

k6는 **부하 생성 도구**입니다. 로컬 PC에서 실행하면서 원하는 곳으로 트래픽을 보낼 수 있습니다:

```batch
# 로컬 서버로 테스트
k6 run --vus 100000 test.js  # 대상: http://localhost:8080

# AWS 서버로 테스트 (필요시에만)
k6 run --vus 100000 test.js  # 대상: http://aws-server.com
```

**핵심**: k6가 로컬에 있으면, 테스트 대상이 어디 있든 상관없습니다!

---

### Q2: 로컬에서 10만 명 테스트가 정말 가능한가요?

**A: 네, 가능합니다!**

k6는 **가상 사용자(VU)**를 생성합니다:
- 실제 사용자 10만 명 ≠ VU 10만 개
- VU는 매우 가볍습니다 (각 VU는 수 MB)
- 일반 PC에서도 10만 VU 생성 가능

**실제 리소스 요구사항:**
```
10만 VU 생성:
- CPU: 4-8 cores
- RAM: 8-16 GB
- 대부분의 개발용 PC/노트북으로 가능!
```

**검증:**
```batch
# 단계적으로 테스트
k6 run --vus 1000 --duration 30s test.js    # 가능?
k6 run --vus 10000 --duration 30s test.js   # 가능?
k6 run --vus 50000 --duration 30s test.js   # 가능?
k6 run --vus 100000 --duration 30s test.js  # 가능!
```

---

### Q3: 로컬 테스트와 AWS 테스트의 차이가 있나요?

**A: 있지만, 대부분의 문제는 로컬에서도 발견 가능합니다.**

#### 로컬에서 발견 가능 (80%):
✅ N+1 쿼리  
✅ 비효율적인 알고리즘  
✅ 메모리 누수  
✅ DB 인덱스 누락  
✅ 커넥션 풀 부족  
✅ 동기 처리 병목  

#### AWS가 필요한 경우 (20%):
⚠️ 실제 네트워크 레이턴시  
⚠️ 여러 리전 간 통신  
⚠️ CDN 캐싱 효과  
⚠️ 실제 인터넷 환경  

**결론**: 로컬에서 충분히 최적화한 후, AWS는 최종 검증용으로만 사용하세요!

---

### Q4: 월 40만원이 너무 비싼데, 어떻게 줄일 수 있나요?

**A: 여러 방법이 있습니다!**

#### 방법 1: 로컬 전환 (추천!)
```
현재: AWS 상시 운영 → 40만원/월
변경: 로컬 Docker → 0원/월
절감: 40만원/월 (100%)
```

#### 방법 2: 온디맨드 (필요시에만 시작)
```
현재: 24/7 운영 (720시간/월)
변경: 주 1회, 2시간 (8시간/월)
비용: 약 1.5만원/월
절감: 38.5만원/월 (96%)
```

#### 방법 3: 최소 구성
```
현재: t3.large × 10대, replicas: 10
변경: t3.small × 2대, replicas: 2
비용: 약 4만원/월
절감: 36만원/월 (90%)
```

#### 방법 4: 스팟 인스턴스
```
현재: 온디맨드 가격
변경: 스팟 인스턴스 (70% 할인)
비용: 약 12만원/월
절감: 28만원/월 (70%)
```

**최고의 조합: 방법 1 + 2**
- 평소: 로컬 개발 (무료)
- 검증: 월 1회 AWS (1.5만원)
- **총 절감: 38.5만원/월 (96%)**

---

## 🧪 테스트 관련

### Q5: 로컬에서 어느 정도 규모까지 테스트 가능한가요?

**A: PC 사양에 따라 다르지만, 대부분 10만 VU는 가능합니다.**

#### PC 사양별 가능 VU 수:
```
최소 사양 (CPU 2코어, RAM 4GB):
→ ~10,000 VU

일반 사양 (CPU 4코어, RAM 8GB):
→ ~50,000 VU

권장 사양 (CPU 8코어, RAM 16GB):
→ ~100,000 VU

고사양 (CPU 16코어, RAM 32GB):
→ ~500,000 VU
```

**팁**: 
- 짧은 시간(30초-1분)으로 시작
- 단계적으로 VU 증가
- `docker stats`로 리소스 모니터링

---

### Q6: 로컬 테스트로도 포트폴리오에 쓸 수 있나요?

**A: 네! 오히려 더 좋습니다!**

#### ❌ 별로인 포트폴리오:
> "AWS로 10만 명 동시 접속 테스트했습니다"
- 특별하지 않음 (돈만 있으면 누구나 가능)
- 어떤 문제를 해결했는지 불명확

#### ✅ 좋은 포트폴리오:
> "로컬 환경에서 체계적인 성능 최적화 수행"
> 
> **1단계: 문제 발견 (k6 1,000 VU)**
> - N+1 쿼리 20개 발견
> - 평균 응답 시간: 2초
> 
> **2단계: 쿼리 최적화 (Fetch Join)**
> - 쿼리 수: 21개 → 1개
> - 평균 응답 시간: 2초 → 200ms (10배 개선)
> 
> **3단계: 인덱스 최적화 (k6 10,000 VU)**
> - 복합 인덱스 5개 추가
> - P95 응답 시간: 3초 → 500ms (6배 개선)
> 
> **4단계: 최종 검증 (k6 100,000 VU - AWS)**
> - 목표 성능 달성: P95 < 1초 ✅
> - 비용 최적화: 월 40만원 → 무료 (로컬) + $2 (최종 검증)

**면접관이 보는 것:**
- ✅ 문제 해결 능력
- ✅ 체계적인 접근
- ✅ 비용 효율성
- ❌ 서버 규모 (중요하지 않음)

---

### Q7: k6 테스트 파일은 어떻게 만드나요?

**A: 프로젝트에 이미 있습니다!**

기존 파일들:
```
k6-login-test.js                    # 로그인
k6-chatroom-performance-test.js     # 채팅
k6-jwt-auth-test.js                 # JWT 인증
k6-health-load-test.js              # Health Check
```

**수정 방법:**
```javascript
// k6-chatroom-performance-test.js
export let options = {
  stages: [
    { duration: '2m', target: 1000 },   // 1,000명으로 증가
    { duration: '5m', target: 1000 },   // 5분 유지
    { duration: '2m', target: 0 },      // 종료
  ],
  thresholds: {
    http_req_duration: ['p(95)<1000'],  // 95% < 1초
    http_req_failed: ['rate<0.01'],     // 에러 < 1%
  },
};

// VU 수와 duration만 조정하면 됨!
```

---

### Q8: 10만 명 테스트를 매번 해야 하나요?

**A: 아니요, 단계적으로 하세요!**

#### 개발 단계별 추천 VU:

**1. 기능 개발 단계 (100-1,000 VU)**
```batch
k6 run --vus 100 --duration 30s test.js
```
- 목적: 기능 동작 확인
- 주기: 매 기능 개발 후

**2. 성능 최적화 단계 (1,000-10,000 VU)**
```batch
k6 run --vus 10000 --duration 5m test.js
```
- 목적: 병목 찾기, 개선 검증
- 주기: 최적화 작업 전후

**3. 스트레스 테스트 (50,000+ VU)**
```batch
k6 run --vus 50000 --duration 2m test.js
```
- 목적: 한계 확인
- 주기: 주 1회

**4. 최종 검증 (100,000 VU)**
```batch
k6 run --vus 100000 --duration 30m test.js
```
- 목적: 목표 성능 달성 확인
- 주기: 배포 직전 (월 1-2회)

---

## 🔧 기술 관련

### Q9: Docker가 너무 느린데 어떻게 하나요?

**A: Docker 리소스를 늘리세요!**

#### Windows/Mac (Docker Desktop):
1. Docker Desktop 아이콘 클릭
2. Settings > Resources
3. 다음과 같이 조정:
   - **CPU**: 4+ cores (더 많을수록 좋음)
   - **Memory**: 8+ GB (권장 16GB)
   - **Swap**: 2 GB
4. Apply & Restart

#### 또는 WSL2 설정 (Windows):
```powershell
# %USERPROFILE%\.wslconfig 파일 생성
[wsl2]
memory=16GB
processors=8
swap=2GB
```

---

### Q10: MySQL 커넥션 에러가 나요!

**A: 커넥션 풀을 늘리세요!**

#### application.yml 수정:
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 100  # 기본 10 → 100
      minimum-idle: 10        # 최소 10개 유지
      connection-timeout: 30000
```

#### MySQL 설정:
```sql
-- max_connections 확인
SHOW VARIABLES LIKE 'max_connections';
-- 현재: 151

-- 증가 필요시 (my.cnf 또는 docker-compose.yml)
max_connections = 500
```

---

### Q11: k6 테스트 중 에러율이 높아요!

**A: 단계적으로 진단하세요!**

#### 1단계: 에러 내용 확인
```batch
# k6 결과에서 에러 확인
http_req_failed................: 5.00%
```

#### 2단계: 로그 확인
```batch
docker-compose logs chat-server | findstr "ERROR"
```

#### 3단계: 원인별 해결

**에러 1: Connection Refused**
```
원인: 서버가 준비되지 않음
해결: 서버 시작 후 30-60초 대기
```

**에러 2: Timeout**
```
원인: 응답이 너무 느림
해결: 
1. N+1 쿼리 확인
2. 인덱스 확인
3. VU 수 줄여서 재테스트
```

**에러 3: 500 Internal Server Error**
```
원인: 서버 내부 에러
해결:
1. 로그 확인
2. 스택 트레이스 분석
3. 코드 수정
```

---

## 📊 결과 분석

### Q12: k6 결과를 어떻게 해석하나요?

**A: 주요 지표를 확인하세요!**

#### 핵심 지표:

**1. HTTP Request Duration**
```
http_req_duration..............: avg=250ms min=50ms med=200ms max=2s p(95)=800ms
```
- `avg` (평균): 일반적인 응답 시간
  - 목표: < 500ms
- `p(95)` (95 백분위수): 95% 사용자 체감
  - 목표: < 1s
- `max` (최대): 최악의 경우
  - 목표: < 3s

**2. HTTP Request Failed**
```
http_req_failed................: 0.50%
```
- 에러율
- 목표: < 1%

**3. Requests Per Second**
```
http_reqs......................: 50000 (1666.67/s)
```
- TPS (초당 처리 요청 수)
- 많을수록 좋음

**4. Virtual Users**
```
vus............................: 10000
vus_max........................: 10000
```
- 실제 생성된 VU 수
- 목표한 VU 수와 일치하는지 확인

---

### Q13: 좋은 성능 기준은 무엇인가요?

**A: 사용자 수에 따라 다릅니다!**

#### 소규모 (1,000 VU)
```
✅ 평균 응답: < 200ms
✅ P95: < 500ms
✅ 에러율: < 0.1%
```

#### 중규모 (10,000 VU)
```
✅ 평균 응답: < 500ms
✅ P95: < 1s
✅ 에러율: < 1%
```

#### 대규모 (100,000 VU)
```
✅ 평균 응답: < 1s
✅ P95: < 3s
✅ 에러율: < 5%
```

**업계 표준:**
- Google: 페이지 로딩 < 3초
- Amazon: 100ms 느려지면 매출 1% 감소
- 일반적: P95 < 1초면 우수

---

## 🚀 실행 관련

### Q14: 지금 당장 무엇부터 해야 하나요?

**A: 이 순서대로 하세요!**

#### ✅ 5분 안에 할 일:

**1. AWS 서버 중지**
```batch
# AWS Console 로그인
# EC2 > 인스턴스 > 중지

# 또는 CLI
aws ec2 stop-instances --instance-ids i-xxxxx --region ap-northeast-2
```

**2. 로컬 환경 시작**
```batch
# 프로젝트 폴더에서
docker-compose up -d
```

**3. 소규모 테스트**
```batch
# 1분 후
k6 run --vus 100 --duration 30s k6-login-test.js
```

**축하합니다! 첫 무료 테스트 완료!** 🎉

---

### Q15: 빠른 스크립트가 있나요?

**A: 네, 프로젝트에 포함되어 있습니다!**

#### Windows:
```batch
로컬_테스트_시작.bat
```

이 스크립트가 자동으로:
1. ✅ Docker 확인
2. ✅ Docker Compose 실행
3. ✅ 서버 준비 대기
4. ✅ k6 테스트 실행
5. ✅ 결과 분석

**사용법:**
```batch
# 더블클릭 또는
로컬_테스트_시작.bat
```

---

### Q16: AWS는 언제 사용해야 하나요?

**A: 이럴 때만 사용하세요!**

#### ✅ AWS가 필요한 경우:
1. **최종 배포 직전 검증**
   - 로컬에서 충분히 최적화 완료
   - 실제 환경에서 마지막 확인

2. **실제 네트워크 환경 테스트**
   - 여러 리전에서 접속 시뮬레이션
   - CDN 효과 측정

3. **포트폴리오/면접용 증빙**
   - "실제 AWS에서 10만 명 테스트" 필요시
   - 월 1회 정도면 충분

#### ❌ AWS가 불필요한 경우:
1. 일상적인 개발
2. 기능 테스트
3. N+1 쿼리 찾기
4. 인덱스 효과 측정
5. 코드 개선 검증

→ **이 모든 것은 로컬에서 가능!**

---

## 💡 추가 팁

### Q17: 다른 사람 프로젝트와 비교하면?

**A: 로컬 최적화가 더 인상적입니다!**

#### ❌ 평범한 프로젝트:
- "AWS로 10만 명 테스트"
- 비용 증빙 없음
- 최적화 과정 불명확

#### ✅ 돋보이는 프로젝트:
- "로컬 개발로 월 40만원 비용 절감"
- 체계적인 성능 최적화 과정
- Before/After 비교 데이터
- 실제 문제 해결 스토리

**면접에서:**
```
면접관: "10만 명 테스트를 어떻게 했나요?"

평범한 답변:
"AWS에 서버를 올려서 k6로 테스트했습니다."

✨ 뛰어난 답변:
"먼저 로컬 환경에서 1,000명 테스트로 N+1 쿼리 문제를 
발견했고, Fetch Join으로 해결하여 응답 시간을 10배 
개선했습니다. 그 후 인덱스 최적화로 6배 추가 개선, 
최종적으로 AWS에서 검증했습니다. 이 과정에서 월 40만원 
비용을 거의 무료로 절감했습니다."

→ 문제 해결 능력 + 비용 효율성 + 체계성 모두 어필!
```

---

### Q18: 참고할 문서가 있나요?

**A: 프로젝트에 이미 많이 있습니다!**

#### 생성된 문서:
- ✅ `비용절감_AWS_배포_전략.md` - 전체 전략
- ✅ `로컬_부하테스트_가이드.md` - 실행 방법
- ✅ `로컬_vs_AWS_비교.md` - 상세 비교
- ✅ `비용_절감_실행_플랜.md` - 단계별 실행
- ✅ `부하테스트_FAQ.md` (이 문서!)

#### 기존 문서:
- 📚 `K6_10만명_동시접속_테스트_가이드.md`
- 📚 `K6_부하테스트_결과_분석.md`
- 📚 `채팅_부하테스트_결과_분석.md`

#### 자동화 스크립트:
- 🤖 `로컬_테스트_시작.bat` - Windows 자동화
- 🤖 `aws-cost-optimizer.sh` - AWS 비용 관리

---

## 🎯 핵심 요약

### 가장 중요한 3가지:

1. **k6는 로컬에서도 10만 VU 생성 가능**
   - 비용: $0
   - 제한: 없음

2. **대부분의 성능 문제는 로컬에서 발견 가능**
   - N+1 쿼리
   - 인덱스 누락
   - 비효율적인 코드

3. **AWS는 최종 검증용으로만 사용**
   - 월 1-2회
   - 비용: ~$2/회
   - 총 절감: 월 38만원 이상

---

## 📞 바로 시작하기

```batch
# 1. AWS 중지 (비용 절감 시작)
aws ec2 stop-instances --instance-ids i-xxxxx

# 2. 로컬 시작
docker-compose up -d

# 3. 테스트
로컬_테스트_시작.bat

# 완료! 이제 월 40만원 절감 중! 🎉
```

---

**더 궁금한 점이 있으면 생성된 문서들을 참고하세요!** 📚


