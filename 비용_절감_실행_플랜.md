# 💰 비용 절감 실행 플랜

## 현재 상황
- **월 비용**: 40만원 이상
- **고민**: 서버를 내리면 테스트 못하는지?

## ✅ 즉시 실행 가능한 해결책

---

## 📋 체크리스트

### 🎯 Step 1: AWS 서버 중지 (5분)

#### Windows
```batch
# AWS CLI 설정 확인
aws configure

# 인스턴스 ID 확인
aws ec2 describe-instances --region ap-northeast-2 --query "Reservations[*].Instances[*].[InstanceId,State.Name,Tags[?Key=='Name'].Value|[0]]" --output table

# 인스턴스 중지
aws ec2 stop-instances --instance-ids i-xxxxx --region ap-northeast-2
```

#### 또는 AWS 콘솔
1. AWS Console 로그인
2. EC2 > 인스턴스
3. 인스턴스 선택
4. 인스턴스 상태 > 중지

**예상 절감액: 월 38만원** ✅

---

### 🏠 Step 2: 로컬 환경 구축 (10분)

#### 방법 1: 자동 스크립트 (추천)
```batch
# 프로젝트 폴더에서 실행
로컬_테스트_시작.bat
```

#### 방법 2: 수동 실행
```batch
# Docker Compose 실행
docker-compose up -d

# 상태 확인
docker-compose ps

# 로그 확인
docker-compose logs -f
```

**비용: $0** ✅

---

### 🧪 Step 3: 로컬 부하 테스트 (15분)

#### 소규모 테스트 (1,000명)
```batch
k6 run --vus 1000 --duration 1m k6-login-test.js
k6 run --vus 1000 --duration 1m k6-chatroom-performance-test.js
```

**목적**: 기본 기능 확인, 에러 체크

#### 중규모 테스트 (10,000명)
```batch
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js
```

**목적**: 병목 구간 찾기, 성능 측정

#### 대규모 테스트 (50,000명+)
```batch
k6 run --vus 50000 --duration 2m k6-chatroom-performance-test.js
```

**목적**: 시스템 한계 확인

**비용: $0** ✅

---

### 🔧 Step 4: 성능 개선 (반복)

#### 4-1. 병목 찾기
```batch
# 로그에서 느린 쿼리 찾기
docker-compose logs chat-server | findstr "took"

# 리소스 모니터링
docker stats
```

#### 4-2. N+1 쿼리 해결
- 프로젝트에 이미 있는 문서 참고:
  - `이메일_받은편지함_N+1_문제_해결_요약.md`
  - `채팅방_목록_N+1_최적화_분석.md`

#### 4-3. 인덱스 추가
```batch
# MySQL 컨테이너에서 실행
docker exec -i mysql mysql -uroot -p"Chat@2024!Secure" db_coreconnect < 인덱스_생성_간단버전.sql
```

#### 4-4. 재테스트
```batch
k6 run --vus 10000 --duration 5m k6-chatroom-performance-test.js
```

**비용: $0** ✅

---

### ☁️ Step 5: AWS 최종 검증 (선택사항, 월 1회)

**언제?**
- 코드 최적화 완료 후
- 배포 직전
- 실제 환경 확인 필요시

**방법:**
```batch
# 1. 인스턴스 시작
aws ec2 start-instances --instance-ids i-xxxxx --region ap-northeast-2

# 2. 준비 대기 (2-3분)
timeout /t 180

# 3. 부하 테스트 실행
k6 run --vus 100000 --duration 30m k6-chatroom-performance-test.js

# 4. 즉시 중지
aws ec2 stop-instances --instance-ids i-xxxxx --region ap-northeast-2
```

**비용: ~$2/회** (t3.large 10대 × 2시간)

---

## 📊 비용 절감 시뮬레이션

### 현재 (상시 운영)
```
┌─────────────────────────────────────┐
│ AWS EC2 (24/7 운영)                 │
├─────────────────────────────────────┤
│ t3.large × 10대                     │
│ $0.0832/시간 × 10 × 24h × 30일     │
│ = $599/월 (≈ 80만원)               │
└─────────────────────────────────────┘
```

### 방법 1: 로컬 개발 (추천!)
```
┌─────────────────────────────────────┐
│ 로컬 Docker (24/7 개발)             │
├─────────────────────────────────────┤
│ Docker Compose                      │
│ k6 무제한 테스트                    │
│ = $0/월                            │
│                                     │
│ 💰 절감액: 80만원/월               │
└─────────────────────────────────────┘
```

### 방법 2: 하이브리드 (개발 + 검증)
```
┌─────────────────────────────────────┐
│ 로컬 Docker (평소 개발)             │
│ + AWS (월 1회 검증, 2시간)          │
├─────────────────────────────────────┤
│ 로컬: $0                           │
│ AWS: $0.0832 × 10 × 2 = $1.66      │
│ = $2/월 (≈ 2,700원)                │
│                                     │
│ 💰 절감액: 79.8만원/월             │
└─────────────────────────────────────┘
```

### 방법 3: 최소 환경 상시 운영
```
┌─────────────────────────────────────┐
│ AWS 최소 구성 (개발용)              │
├─────────────────────────────────────┤
│ t3.small × 2대 (replicas: 2)       │
│ $0.0208/시간 × 2 × 24h × 30일      │
│ = $30/월 (≈ 4만원)                 │
│                                     │
│ 💰 절감액: 76만원/월               │
└─────────────────────────────────────┘
```

---

## 🎯 추천 전략 (단계별)

### 1주차: 로컬 전환
```
☑️ AWS 서버 중지
☑️ 로컬 Docker 구축
☑️ 소규모 테스트 (1,000 VU)
☑️ 기능 확인

절감액: 10만원 (1주)
```

### 2주차: 성능 테스트
```
☑️ 중규모 테스트 (10,000 VU)
☑️ 병목 찾기
☑️ N+1 쿼리 해결
☑️ 재테스트

절감액: 10만원 (1주)
```

### 3주차: 최적화
```
☑️ 인덱스 추가
☑️ 캐싱 추가
☑️ 대규모 테스트 (50,000 VU)
☑️ 성능 목표 달성 확인

절감액: 10만원 (1주)
```

### 4주차: 최종 검증
```
☑️ AWS 인스턴스 시작 (1회, 2시간)
☑️ 10만 VU 테스트
☑️ 실제 환경 검증
☑️ 즉시 중지

비용: $2
절감액: 9.8만원 (1주)
```

**4주 총 절감액: 39.8만원** 🎉

---

## 📱 실시간 모니터링

### 로컬 환경 모니터링

#### Docker 상태
```batch
# 실시간 리소스 확인
docker stats

# 컨테이너 목록
docker-compose ps
```

#### 로그 확인
```batch
# 전체 로그
docker-compose logs -f

# 특정 서비스만
docker-compose logs -f chat-server

# 에러만 필터링
docker-compose logs | findstr "ERROR"
```

#### MySQL 쿼리 모니터링
```sql
-- MySQL에 접속
docker exec -it mysql mysql -uroot -p"Chat@2024!Secure"

-- 느린 쿼리 확인
SHOW FULL PROCESSLIST;

-- 인덱스 사용 확인
SHOW INDEX FROM chatroom;
```

---

## 🚨 트러블슈팅

### 문제 1: Docker가 느려요
**원인**: 리소스 부족

**해결:**
1. Docker Desktop > Settings > Resources
2. CPU: 4+ cores
3. Memory: 8+ GB
4. 적용 후 재시작

### 문제 2: k6 테스트가 실패해요
**원인**: 서버가 준비되지 않음

**해결:**
```batch
# 서버 상태 확인
docker-compose ps

# 로그 확인
docker-compose logs chat-server

# 서버 재시작
docker-compose restart chat-server

# 30초 대기 후 재시도
timeout /t 30
```

### 문제 3: MySQL 연결 에러
**원인**: 커넥션 풀 부족

**해결:**
```yaml
# docker-compose.yml 또는 application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 100
      minimum-idle: 10
```

### 문제 4: Out of Memory
**원인**: JVM 메모리 부족

**해결:**
```yaml
# k8s/03-chat-server-dev.yaml 또는 docker-compose.yml
environment:
  - JAVA_OPTS=-Xms512m -Xmx2g
```

---

## 📈 성공 기준

### 기능 테스트 (1,000 VU)
- ✅ 에러율: 0%
- ✅ 평균 응답: < 500ms
- ✅ 모든 API 정상 동작

### 성능 테스트 (10,000 VU)
- ✅ 에러율: < 1%
- ✅ 평균 응답: < 1초
- ✅ P95: < 2초
- ✅ TPS: > 1,000

### 대규모 테스트 (50,000 VU)
- ✅ 에러율: < 5%
- ✅ 평균 응답: < 2초
- ✅ P95: < 5초
- ✅ 시스템 안정성 유지

---

## 💡 포트폴리오 작성 팁

### 로컬 테스트도 충분히 어필 가능!

#### Before (AWS 비용 문제)
```
❌ "AWS로 10만 명 테스트했습니다"
   → 비용만 많이 들고 특별하지 않음
```

#### After (로컬 최적화 스토리)
```
✅ "로컬 환경에서 성능 최적화 과정"
   
   1. N+1 쿼리 발견 (k6 1,000 VU)
      → Before: 2초 → After: 200ms (10배 개선)
   
   2. 인덱스 최적화 (k6 10,000 VU)
      → Before: P95 3초 → After: P95 500ms (6배 개선)
   
   3. 캐싱 추가 (k6 50,000 VU)
      → TPS 500 → 2,000 (4배 개선)
   
   4. AWS 최종 검증 (k6 100,000 VU)
      → 목표 성능 달성 ✅
   
   💰 비용 절감: 월 40만원 → 무료
```

**면접관이 관심 있는 것:**
- ❌ 얼마나 큰 서버를 썼는지
- ✅ 어떻게 문제를 찾고 해결했는지
- ✅ 비용 효율적으로 개발했는지

---

## 🎉 최종 요약

### 질문: "서버를 내리면 k6 테스트를 못하나요?"

### 답변: "아니요! 오히려 더 좋습니다!"

#### 지금 바로 하세요
1. ✅ AWS 서버 중지 → 월 38만원 절감
2. ✅ 로컬 Docker 실행 → 비용 $0
3. ✅ k6 로컬 테스트 → 무제한 반복
4. ✅ 성능 최적화 → 빠른 개발 사이클

#### 필요할 때만 하세요
5. ☁️ AWS 최종 검증 → 월 1회, $2

#### 결과
- **비용 절감**: 연 480만원 💰
- **테스트 횟수**: 무제한 → 더 많은 최적화 🚀
- **개발 속도**: 더 빠름 ⚡
- **포트폴리오**: 더 좋음 📈

---

## 📞 도움이 필요하면

1. **로컬 테스트 가이드**: `로컬_부하테스트_가이드.md`
2. **비용 비교**: `로컬_vs_AWS_비교.md`
3. **비용 절감 전략**: `비용절감_AWS_배포_전략.md`
4. **자동 스크립트**: `로컬_테스트_시작.bat`

---

**🎯 핵심 메시지:**

> "AWS 없이도 k6로 충분히 부하 테스트 가능합니다.  
> 로컬에서 최적화하고, AWS는 최종 검증용으로만 사용하세요.  
> 연 480만원을 절약하면서도 더 나은 개발이 가능합니다!" 🎉


