# 성능 모니터링 및 측정 가이드

## 1. MySQL Slow Query Log 활성화

### 1-1. Slow Query Log 설정 확인
```sql
-- 현재 설정 확인
SHOW VARIABLES LIKE 'slow_query_log%';
SHOW VARIABLES LIKE 'long_query_time';
```

### 1-2. Slow Query Log 활성화
```sql
-- 글로벌 설정 변경 (서버 재시작 시 리셋됨)
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1.0;  -- 1초 이상 걸리는 쿼리 기록
SET GLOBAL slow_query_log_file = '/var/log/mysql/slow-query.log';

-- 또는 my.cnf 또는 my.ini에 영구 설정
-- [mysqld]
-- slow_query_log = 1
-- long_query_time = 1.0
-- slow_query_log_file = /var/log/mysql/slow-query.log
-- log_queries_not_using_indexes = 1  -- 인덱스 미사용 쿼리도 기록
```

### 1-3. Slow Query Log 분석
```bash
# 로그 파일 확인
tail -f /var/log/mysql/slow-query.log

# mysqldumpslow로 요약 분석
mysqldumpslow -s t -t 10 /var/log/mysql/slow-query.log
# -s t: 실행 시간 순 정렬
# -t 10: 상위 10개만 출력
```

---

## 2. HikariCP Connection Pool 모니터링

### 2-1. 현재 설정 (application.yml)
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 40          # 최대 커넥션 수
      minimum-idle: 10               # 최소 유휴 커넥션
      connection-timeout: 15000      # 15초
      leak-detection-threshold: 60000 # 1분 (누수 감지)
```

### 2-2. JMX로 모니터링
```bash
# JMX 활성화 (application.yml 또는 JVM 옵션)
-Dcom.sun.management.jmxremote
-Dcom.sun.management.jmxremote.port=9010
-Dcom.sun.management.jmxremote.authenticate=false
-Dcom.sun.management.jmxremote.ssl=false
```

```java
// 또는 Spring Boot Actuator 사용
// application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,hikaricp
  metrics:
    enable:
      hikaricp: true
```

### 2-3. 모니터링 지표
- `hikaricp.connections.active`: 현재 사용 중인 커넥션 수
- `hikaricp.connections.idle`: 유휴 커넥션 수
- `hikaricp.connections.pending`: 대기 중인 요청 수
- `hikaricp.connections.timeout`: 타임아웃 발생 횟수
- `hikaricp.connections.creation`: 커넥션 생성 시간

**주의**: `active`가 `maximum-pool-size`에 근접하면 풀 고갈 상태 → 풀 사이즈 증가 필요

---

## 3. Tomcat Thread Pool 모니터링

### 3-1. 현재 설정 (application.yml)
```yaml
server:
  tomcat:
    threads:
      max: 200                      # 최대 스레드 수
      min-spare: 30                 # 최소 유휴 스레드
    accept-count: 150               # 요청 대기 큐 크기
    max-connections: 10000          # 최대 동시 연결 수
```

### 3-2. Actuator로 모니터링
```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,threaddump
```

```bash
# 엔드포인트 확인
curl http://localhost:8080/actuator/metrics/tomcat.threads.busy
curl http://localhost:8080/actuator/metrics/tomcat.threads.current
curl http://localhost:8080/actuator/metrics/tomcat.threads.config.max
```

### 3-3. 모니터링 지표
- `tomcat.threads.busy`: 현재 사용 중인 스레드 수
- `tomcat.threads.current`: 현재 스레드 총 수
- `tomcat.threads.config.max`: 최대 스레드 수
- `http.server.requests`: HTTP 요청 처리 시간 분포

**주의**: `busy`가 `max`에 근접하면 스레드 고갈 상태 → 스레드 풀 증가 필요

---

## 4. EXPLAIN으로 쿼리 성능 분석

### 4-1. 인덱스 사용 확인
```sql
-- 채팅방 최신 메시지 조회 쿼리 분석
EXPLAIN 
SELECT * 
FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 50;
```

### 4-2. 확인 항목
| 항목 | 좋은 값 | 나쁜 값 | 의미 |
|------|---------|---------|------|
| **type** | `ref`, `range`, `index` | `ALL` | 인덱스 사용 여부 (ALL은 전체 스캔) |
| **key** | 인덱스 이름 (예: `idx_chat_message_room_sent_at`) | `NULL` | 사용된 인덱스 |
| **rows** | 적을수록 좋음 (예: 50) | 전체 행 수 | 검사할 예상 행 수 |
| **Extra** | `Using index`, `Using where` | `Using filesort`, `Using temporary` | 추가 작업 여부 |

### 4-3. 문제 해결
- **type=ALL**: 인덱스가 없거나 사용 안 됨 → 인덱스 생성 또는 `ANALYZE TABLE` 실행
- **Using filesort**: ORDER BY를 위한 정렬 작업 발생 → 인덱스에 정렬 컬럼 포함
- **Using temporary**: GROUP BY/DISTINCT 등을 위한 임시 테이블 생성 → 쿼리 최적화 필요

---

## 5. k6 부하 테스트 재측정

### 5-1. 테스트 실행
```bash
cd ~/k6-loadtest
export K6_CLOUD_PROJECT_ID=6156169
export K6_CLOUD_TOKEN=YOUR_TOKEN
export BASE_URL=http://54.116.26.182:8080
export USER_EMAIL=admin@coreconnect.io.kr
export USER_PASSWORD=1

k6 run --out cloud chatroom-latest-cloud.js
```

### 5-2. 목표 지표
- **P95 Latency**: < 500ms (현재: ~6.3s)
- **P99 Latency**: < 1000ms (현재: ~7.4s)
- **RPS**: 10~15 이상 @20 VU (현재: ~5.3)
- **에러율**: < 1% (현재: 0%)

### 5-3. 비교 분석
- Grafana에서 "Compare with" 기능으로 개선 전/후 비교
- `chat_latency_ms` P95/P99 추이 확인
- `http_req_failed`, `chat_fail` 비율 확인
- RPS 구간별(10→15→5) 응답 분포 확인

---

## 6. 단계별 체크리스트

### ✅ Step 1: 인덱스 사용 여부 확인
- [ ] `SHOW INDEX FROM chat_message` 실행
- [ ] `idx_chat_message_room_sent_at` 존재 확인
- [ ] `EXPLAIN` 결과에서 `type=ref/range`, `key=idx_chat_message_room_sent_at` 확인
- [ ] `Extra`에 `Using filesort` 없는지 확인
- [ ] `ANALYZE TABLE chat_message` 실행

### ✅ Step 2: 최신 빌드/배포 확인
- [ ] N+1 완화 코드 (Fetch Join/EntityGraph) 반영 확인
- [ ] DTO 슬림 (`ChatRoomLatestMessageDTO`) 적용 확인
- [ ] 페이징 기본값 (limit=20) 컨트롤러에 적용 확인
- [ ] 비동기 처리 (`@Async`) 이벤트 리스너 활성화 확인
- [ ] Docker 이미지 재빌드 및 재배포 완료

### ✅ Step 3: 페이징 강제/응답 슬림 재확인
- [ ] 컨트롤러: `@RequestParam(defaultValue = "20") int size` 확인
- [ ] 서비스: `Pageable` 사용 여부 확인 (또는 리스트 슬라이싱)
- [ ] API 테스트: `curl http://54.116.26.182:8080/api/v1/chat/rooms/messages/latest` 응답 크기 확인

### ✅ Step 4: 풀/RTT 점검
- [ ] Actuator로 HikariCP `active`/`idle` 확인
- [ ] Actuator로 Tomcat `busy`/`current` 스레드 확인
- [ ] k6 실행 위치: 서버와 동일 리전 확인
- [ ] GZIP/Keep-Alive 활성화 확인 (`curl -I` 헤더 확인)

### ✅ Step 5: 재측정
- [ ] k6 테스트 재실행
- [ ] P95/P99 지표 비교 (목표: P95 < 500ms)
- [ ] RPS 비교 (목표: 10~15+ @20 VU)
- [ ] 에러율 확인 (목표: < 1%)
- [ ] Grafana "Compare with" 기능으로 전/후 대조

---

## 7. 추가 최적화 옵션 (목표 미달 시)

### 7-1. Redis 캐시 적용
```java
@Cacheable(value = "chatRoomList", key = "#userId", unless = "#result == null")
public List<ChatRoomListDTO> getChatRoomListWithUnreadCount(Integer userId) {
    // 기존 로직
}
```

### 7-2. Read Replica 분리
- 읽기 전용 엔드포인트를 별도로 분리하여 부하 분산
- `@Transactional(readOnly = true)` 활용

### 7-3. 쿼리 최적화
- Projection: 필요한 컬럼만 SELECT (예: DTO Projection)
- Batch Size 설정: `hibernate.default_batch_fetch_size=100`

### 7-4. CDN/프록시 레벨 캐싱
- Nginx/CloudFront에서 정적 리소스 캐싱
- HTTP Cache-Control 헤더 활용

---

## 8. 문의사항

- HikariCP/Tomcat 지표 확인이 필요하면 Actuator 엔드포인트 활성화
- Slow Query Log 분석 결과 공유 시 상위 10개 쿼리와 실행 시간 확인
- EXPLAIN 결과가 예상과 다르면 스크린샷 공유

이 가이드대로 하나씩 확인하시고, 재측정 후 결과를 비교해주세요!

