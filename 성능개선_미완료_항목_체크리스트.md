# CoreConnect 성능개선 미완료 항목 체크리스트

> **작성일**: 2025-12-19  
> **목적**: TO-BE 성능개선 항목 중 미완료 항목 추적 및 실행 계획  
> **기준**: 성능개선_실행플랜_난이도별.md 문서 기반

---

## 📊 전체 현황 요약

| 분류 | 완료 | 미완료 | 진행률 |
|------|------|--------|--------|
| **Level 1 (초급)** | 0/2 | 2 | 0% |
| **Level 2 (중급)** | 1/4 | 3 | 25% |
| **Level 3 (고급)** | 0/1 | 1 | 0% |
| **Level 4 (전문가)** | 0/3 | 3 | 0% |
| **전체** | **1/10** | **9** | **10%** |

### ✅ 완료된 항목
1. ✅ **N+1 쿼리 해결** (Level 2)
   - EmailRecipient에 Fetch Join 적용 완료
   - DB 쿼리 95% 감소 달성

---

## 🟢 Level 1: 초급 - 즉시 실행 필요 (2개 항목)

### ❌ #1. DB 인덱스 추가

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥🔥🔥 최우선 (가장 먼저 해야 함)
- **난이도**: ⭐☆☆☆☆ (매우 쉬움)
- **소요 시간**: 2시간
- **비용**: 0원
- **예상 효과**: 응답시간 67% 단축 (25-30ms → 8-10ms)

#### 🎯 구현 내용
```sql
-- 1. 이메일 받은편지함 최적화 (최우선!)
CREATE INDEX idx_email_user_created 
ON email(user_email, created_at DESC);

-- 2. 이메일 읽지 않은 수 최적화
CREATE INDEX idx_email_status 
ON email(user_email, is_read, deleted_at);

-- 3. 이메일 보낸편지함 최적화
CREATE INDEX idx_email_sender_created 
ON email(sender_email, created_at DESC);

-- 4. 이메일 즐겨찾기 최적화
CREATE INDEX idx_email_favorite 
ON email(user_email, is_favorite, deleted_at, created_at DESC);

-- 5. 채팅 읽지 않은 메시지 최적화
CREATE INDEX idx_chat_read_status 
ON chat_message_read_status(user_id, is_read);
```

#### ✅ 실행 체크리스트
- [ ] 현재 EXPLAIN 결과 확인 (Full Table Scan 확인)
- [ ] 테이블별 인덱스 생성 SQL 실행
- [ ] EXPLAIN으로 Index Scan 확인
- [ ] 응답시간 측정 (Before/After)
- [ ] 프로덕션 배포 (피크 시간 피하기)
- [ ] k6 재테스트로 검증

#### ⚠️ 주의사항
- 프로덕션 적용 시 `ALGORITHM=INPLACE, LOCK=NONE` 사용
- 디스크 용량 확인 (인덱스는 테이블 크기의 20-30%)
- 피크 시간 피하기 (새벽 3-4시 권장)

---

### ❌ #2. Connection Pool 증설

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥🔥🔥 최우선
- **난이도**: ⭐☆☆☆☆ (매우 쉬움)
- **소요 시간**: 30분
- **비용**: 0원
- **예상 효과**: 연결 대기 97% 단축 (72ms → 2ms)

#### 🎯 구현 내용
```yaml
# backend/src/main/resources/application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 50          # 변경: 10 → 50
      minimum-idle: 20               # 변경: 5 → 20
      connection-timeout: 20000      # 20초
      idle-timeout: 300000           # 5분
      max-lifetime: 1200000          # 20분
      leak-detection-threshold: 60000 # 1분
      connection-test-query: SELECT 1
      validation-timeout: 5000       # 5초

  server:
    tomcat:
      threads:
        max: 200
        min-spare: 50                # 변경: 10 → 50
      accept-count: 100
      max-connections: 10000
```

#### ✅ 실행 체크리스트
- [ ] 현재 Pool Size 확인 (application.yml)
- [ ] 최적 Pool Size 계산 (동시 사용자 45명 × 1.2 = 54)
- [ ] application.yml 수정
- [ ] 로컬 테스트 (로그에서 Pool Size 50 확인)
- [ ] Git commit & push
- [ ] Docker 이미지 재빌드 & 배포
- [ ] k6 재테스트로 검증

#### ⚠️ 주의사항
- DB 서버 max_connections 확인 (MySQL 기본: 151개)
- Connection 1개당 약 4MB 메모리 사용 (50개 = 200MB)
- DB max_connections의 50% 이하로 설정 권장

---

## 🟡 Level 2: 중급 - 1주일 내 완료 (3개 항목)

### ✅ #3. N+1 쿼리 해결 ✓ 완료

#### 📋 상태
- **현재 상태**: ✅ 완료
- **완료일**: 2025-12-XX
- **구현 내용**: EmailRecipient Repository에 Fetch Join 적용
- **효과**: DB 쿼리 수 95% 감소 (21개 → 1개)

---

### ❌ #4. Warm-up 스크립트

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥🔥 중요
- **난이도**: ⭐⭐☆☆☆ (보통)
- **소요 시간**: 1일
- **비용**: 0원
- **예상 효과**: Cold Start 93% 단축 (150ms → 10ms)

#### 🎯 구현 내용
```java
// WarmupService.java
@Component
@Slf4j
public class WarmupService {
    
    @Autowired
    private EmailRepository emailRepository;
    
    @Autowired
    private ChatRepository chatRepository;
    
    @Autowired
    private NotificationRepository notificationRepository;
    
    @EventListener(ApplicationReadyEvent.class)
    public void warmup() {
        log.info("🔥 Warm-up 시작...");
        
        long start = System.currentTimeMillis();
        
        // 1. Connection Pool 초기화
        warmupConnectionPool();
        
        // 2. JPA 메타데이터 로딩
        warmupJpaMetadata();
        
        // 3. 주요 쿼리 실행 계획 컴파일
        warmupQueryPlans();
        
        long duration = System.currentTimeMillis() - start;
        log.info("✅ Warm-up 완료: {}ms", duration);
    }
    
    private void warmupConnectionPool() {
        // 10개 Connection 미리 생성
        emailRepository.count();
        chatRepository.count();
        notificationRepository.count();
    }
    
    private void warmupJpaMetadata() {
        // 주요 엔티티 메타데이터 로딩
        emailRepository.findAll(PageRequest.of(0, 1));
    }
    
    private void warmupQueryPlans() {
        // 자주 사용하는 쿼리 실행 계획 컴파일
        emailRepository.findByUserEmailWithAttachments("warmup@test.com");
    }
}
```

#### ✅ 실행 체크리스트
- [ ] WarmupService.java 생성
- [ ] ApplicationReadyEvent 리스너 구현
- [ ] Connection Pool 초기화 로직 추가
- [ ] JPA 메타데이터 로딩 로직 추가
- [ ] 주요 쿼리 실행 계획 컴파일 추가
- [ ] 로컬 테스트 (시작 시간 로그 확인)
- [ ] K8s Readiness Probe 설정 (선택)
- [ ] 프로덕션 배포
- [ ] Cold Start 시간 측정 (Before/After)

---

### ❌ #5. DTO 최적화

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥 보통
- **난이도**: ⭐⭐☆☆☆ (보통)
- **소요 시간**: 2일
- **비용**: 0원
- **예상 효과**: 네트워크 전송량 60% 감소

#### 🎯 구현 내용
```java
// EmailDTO.java (기존)
@Getter @Setter
public class EmailDTO {
    private Long id;
    private String title;
    private String content;
    private String senderEmail;
    private String senderName;
    private String senderDept;
    private LocalDateTime sentTime;
    private Boolean isRead;
    // ❌ 불필요한 필드들
    private String password;      // 보안 위험
    private LocalDateTime deletedAt;  // 불필요
    private Integer version;      // 불필요
    // ... 20개 이상의 필드
}

// EmailListItemDTO.java (신규 - 최적화)
@Getter @Setter
public class EmailListItemDTO {
    private Long id;
    private String title;          // 제목만 (본문 제외)
    private String senderName;     // 발신자 이름만
    private LocalDateTime sentTime;
    private Boolean isRead;
    private Boolean hasAttachment; // boolean 플래그만
    // 총 6개 필드 → 응답 크기 70% 감소
}

// EmailDetailDTO.java (신규 - 상세)
@Getter @Setter
public class EmailDetailDTO {
    // 상세 조회 시에만 전체 필드 반환
    private Long id;
    private String title;
    private String content;        // 본문 포함
    private String senderEmail;
    private String senderName;
    private String senderDept;
    private LocalDateTime sentTime;
    private Boolean isRead;
    private List<AttachmentDTO> attachments; // 첨부파일 전체 정보
}
```

#### 📊 DTO 최적화 전략
```
1. 목록 조회 (Inbox)
   Before: EmailDTO (1,200 bytes × 20개 = 24KB)
   After:  EmailListItemDTO (480 bytes × 20개 = 9.6KB)
   효과: 60% 감소

2. 상세 조회 (Detail)
   Before: EmailDTO (1,200 bytes)
   After:  EmailDetailDTO (1,200 bytes) - 동일
   효과: 영향 없음 (필요한 정보만 반환)

3. JSON 직렬화
   Before: Jackson 기본 설정
   After:  @JsonInclude(JsonInclude.Include.NON_NULL)
   효과: null 필드 제외로 10-20% 추가 감소
```

#### ✅ 실행 체크리스트
- [ ] EmailListItemDTO 클래스 생성
- [ ] EmailDetailDTO 클래스 생성
- [ ] Repository에 DTO Projection 쿼리 추가
- [ ] Service에서 목록/상세 구분하여 DTO 반환
- [ ] Controller 응답 타입 변경
- [ ] 프론트엔드 타입 수정 (EmailListItem, EmailDetail)
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 (API 응답 확인)
- [ ] 응답 크기 측정 (Before/After)
- [ ] 프로덕션 배포

---

## 🟠 Level 3: 고급 - 2-3주 내 완료 (1개 항목)

### ❌ #6. Redis 캐싱 도입

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥🔥🔥 최우선 (효과 가장 큼)
- **난이도**: ⭐⭐⭐☆☆ (어려움)
- **소요 시간**: 3일
- **비용**: 월 $10-20
- **예상 효과**: 응답시간 70% 단축, DB 부하 70% 감소

#### 🎯 구현 내용

**1. Redis 설치 (Docker)**
```bash
docker run -d \
  --name redis \
  --restart always \
  -p 6379:6379 \
  -v redis-data:/data \
  -m 512m \
  redis:7-alpine redis-server \
    --appendonly yes \
    --maxmemory 450mb \
    --maxmemory-policy allkeys-lru
```

**2. Spring 의존성 추가**
```gradle
// build.gradle
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.boot:spring-boot-starter-cache'
}
```

**3. Redis 설정**
```yaml
# application.yml
spring:
  cache:
    type: redis
    redis:
      time-to-live: 30000
      
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
```

**4. @Cacheable 적용**
```java
// EmailService.java
@Cacheable(
    value = "email:unread",
    key = "#userEmail",
    unless = "#result == null"
)
public Long getUnreadCount(String userEmail) {
    return emailRepository.countByUserEmailAndIsRead(userEmail, false);
}

@CacheEvict(value = "email:unread", key = "#userEmail")
public void markAsRead(String userEmail, Long emailId) {
    emailRepository.updateReadStatus(emailId, true);
}
```

#### 📊 캐싱 우선순위
```
우선순위 1: 읽지 않은 메일 수 (TTL: 30초)
- 2분에 595회 조회 → 캐시 히트율 95% 예상
- DB 쿼리 95% 감소

우선순위 2: 채팅 최신 메시지 (TTL: 10초)
- 실시간성 중요 → 짧은 TTL
- DB 쿼리 90% 감소

우선순위 3: 알림 목록 (TTL: 60초)
- 빈번한 조회
- DB 쿼리 80% 감소

우선순위 4: 이메일 받은편지함 (TTL: 2분)
- 페이지 단위 캐싱
- DB 쿼리 50% 감소
```

#### ✅ 실행 체크리스트
- [ ] Redis Docker 컨테이너 실행
- [ ] Redis 연결 테스트 (redis-cli ping)
- [ ] build.gradle 의존성 추가
- [ ] application.yml Redis 설정
- [ ] RedisConfig.java 작성
- [ ] @Cacheable 적용 (4개 API)
- [ ] @CacheEvict 적용 (무효화 로직)
- [ ] 로컬 테스트 (Cache MISS → HIT 확인)
- [ ] Redis 모니터링 API 추가 (선택)
- [ ] 프로덕션 Redis 서버 준비
- [ ] 프로덕션 배포
- [ ] 캐시 히트율 모니터링 (목표: 70% 이상)
- [ ] k6 재테스트로 검증

#### ⚠️ 주의사항
- 캐시 불일치 방지: 데이터 변경 시 반드시 @CacheEvict
- TTL 설정: 실시간성과 캐시 효율 트레이드오프
- 메모리 관리: maxmemory 설정 + LRU 정책
- 직렬화: Jackson JSON 사용 (Serializable 구현 불필요)

---

## 🔴 Level 4: 전문가 - 장기 계획 (3개 항목)

### ❌ #7. Redis Cluster (고가용성)

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: ⚠️ 선택 (트래픽 200+ VUs 시)
- **난이도**: ⭐⭐⭐⭐☆ (매우 어려움)
- **소요 시간**: 1주
- **비용**: 월 $50-100
- **예상 효과**: 캐시 고가용성 99.9%, 처리량 3배

#### 🎯 구현 시점
```
조건:
- 동시 접속자 200명 이상
- Redis 단일 서버 CPU > 80%
- 캐시 다운타임 허용 불가

구현 내용:
- AWS ElastiCache Redis Cluster (3 Master, 3 Replica)
- Spring Lettuce Cluster 설정
- 샤딩 전략 설계
- 페일오버 자동화
```

#### ⏸️ 현재 상태
- [ ] 트래픽 200+ VUs 달성 시 재검토
- [ ] 현재는 단일 Redis로 충분

---

### ❌ #8. DB Sharding (수평 확장)

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔴 불필요 (현재 단계에서는 과도)
- **난이도**: ⭐⭐⭐⭐⭐ (최고 난이도)
- **소요 시간**: 1개월
- **비용**: 월 $200+
- **예상 효과**: 확장성 10배

#### 🎯 구현 시점
```
조건:
- 데이터 1억 건 이상
- 동시 접속자 500명 이상
- 단일 DB 한계 도달 (CPU 90%+, Storage 80%+)

구현 내용:
- User ID 기반 샤딩 (Horizontal Sharding)
- Shard Key 설계
- 데이터 마이그레이션 전략
- 크로스 샤드 쿼리 최적화
```

#### ⏸️ 현재 상태
- [ ] 트래픽 500+ VUs 달성 시 재검토
- [ ] 현재는 Read Replica로 충분

---

### ❌ #9. Read Replica 추가

#### 📋 기본 정보
- **현재 상태**: ❌ 미완료
- **우선순위**: 🔥 보통 (트래픽 100+ VUs 시 고려)
- **난이도**: ⭐⭐⭐☆☆ (어려움)
- **소요 시간**: 1주
- **비용**: 월 $50
- **예상 효과**: 읽기 처리량 2배, Master DB 부하 50% 감소

#### 🎯 구현 내용
```java
// 1. AWS RDS Read Replica 생성
aws rds create-db-instance-read-replica \
  --db-instance-identifier coreconnect-replica-1 \
  --source-db-instance-identifier coreconnect-master

// 2. Spring 다중 DataSource 설정
@Configuration
public class DataSourceConfig {
    @Bean
    public DataSource masterDataSource() { ... }
    
    @Bean
    public DataSource slaveDataSource() { ... }
    
    @Bean
    public DataSource routingDataSource() {
        // 읽기/쓰기 자동 라우팅
    }
}

// 3. @Transactional(readOnly = true) 적용
@Transactional(readOnly = true)  // → Slave DB
public List<EmailDTO> getInbox(String userEmail) { ... }

@Transactional  // → Master DB
public void sendEmail(EmailDTO emailDTO) { ... }
```

#### 📊 구현 시점 가이드
```
현재 상황:
- 동시 접속자: 45명
- Master DB CPU: ~30%
- 읽기:쓰기 비율: 9:1

구현 시점:
- 동시 접속자 100명 이상
- Master DB CPU 70% 이상
- 응답시간 지연 발생 시

예상 효과:
- Master DB CPU: 30% → 15%
- 읽기 처리량: 2배 증가
- 확장성: 300+ VUs 대응 가능
```

#### ✅ 실행 체크리스트
- [ ] AWS RDS Read Replica 생성
- [ ] 엔드포인트 확인
- [ ] application.yml 다중 DataSource 설정
- [ ] DataSourceConfig.java 작성
- [ ] ReplicationRoutingDataSource.java 작성
- [ ] @Transactional(readOnly = true) 적용
- [ ] Replication Lag 모니터링 추가
- [ ] 프로덕션 배포
- [ ] Master CPU 감소 확인
- [ ] 읽기 처리량 증가 확인

#### ⚠️ 주의사항
- Replication Lag: 평균 1-5초 (실시간 데이터는 Master 조회)
- Lag 모니터링: Seconds_Behind_Master 지표 확인
- 페일오버: Replica를 Master로 승격 가능

---

## 📅 실행 로드맵 (우선순위별)

### 🔥 Phase 1: 즉시 실행 (1주일 이내)
```
Week 1 (Day 1-7)
├─ Day 1: #1 DB 인덱스 추가 (2시간) ✓
├─ Day 1: #2 Connection Pool 증설 (30분) ✓
└─ 효과: 응답시간 40% 단축, 비용 0원

총 소요시간: 2.5시간
예상 효과: 응답시간 11ms → 6-7ms
```

### 🔥 Phase 2: 단기 실행 (2-3주 이내)
```
Week 2-3 (Day 8-21)
├─ Day 8-10: #6 Redis 캐싱 도입 (3일) ⭐ 최우선
├─ Day 11-12: #5 DTO 최적화 (2일)
├─ Day 13: #4 Warm-up 스크립트 (1일)
└─ 효과: 응답시간 70% 단축, 처리량 2배, 월 $10

총 소요시간: 6일
예상 효과: 응답시간 6-7ms → 2-3ms
```

### ⚠️ Phase 3: 장기 실행 (트래픽 증가 시)
```
Week 4-8 (필요시)
├─ Week 4-5: #9 Read Replica 추가 (1주)
│   → 조건: 동시 접속자 100명 이상
├─ Week 6-7: #7 Redis Cluster (1주)
│   → 조건: 동시 접속자 200명 이상
└─ Week 8+: #8 DB Sharding (1개월)
    → 조건: 동시 접속자 500명 이상

총 소요시간: 4-8주 (선택)
예상 효과: 처리량 3배, 확장성 10배
```

---

## 🎯 최종 목표

### 단기 목표 (Phase 1-2 완료 후)
```
응답시간:    11.10ms → 2-3ms (73% 단축)
처리량:      44 req/s → 90 req/s (2배)
DB 쿼리 수:  5,354개 → 1,600개 (70% 감소)
확장성:      45 VUs → 150 VUs (3배)
비용:        0원 → 월 $10
```

### 장기 목표 (Phase 3 완료 후)
```
응답시간:    2-3ms
처리량:      130+ req/s (3배)
DB 쿼리 수:  800개 (85% 감소)
확장성:      300+ VUs (7배)
비용:        월 $60-120
```

---

## 📋 다음 액션 아이템 (즉시 실행)

### 이번 주 (Week 1)
1. [ ] **DB 인덱스 추가** (2시간)
   - 테스트 환경에서 인덱스 생성
   - EXPLAIN으로 효과 확인
   - 프로덕션 적용 (피크 시간 피하기)

2. [ ] **Connection Pool 증설** (30분)
   - application.yml 수정 (50개로 증설)
   - 로컬 테스트
   - Docker 이미지 재빌드 & 배포

3. [ ] **k6 재테스트**
   - 개선 효과 측정
   - 응답시간 40% 단축 확인

### 다음 주 (Week 2-3)
1. [ ] **Redis 캐싱 도입** (3일)
   - Redis Docker 설치
   - @Cacheable 적용 (4개 API)
   - 캐시 히트율 70% 달성

2. [ ] **DTO 최적화** (2일)
   - EmailListItemDTO 생성
   - 응답 크기 60% 감소

3. [ ] **Warm-up 스크립트** (1일)
   - ApplicationReadyEvent 리스너 구현
   - Cold Start 93% 단축

---

## 📊 진행 상황 추적

### 완료 항목 (1/10)
- ✅ N+1 쿼리 해결

### 진행 중 항목 (0/10)
- (없음)

### 미완료 항목 (9/10)
- ❌ DB 인덱스 추가
- ❌ Connection Pool 증설
- ❌ Warm-up 스크립트
- ❌ DTO 최적화
- ❌ Redis 캐싱 도입
- ❌ Read Replica 추가
- ❌ Redis Cluster
- ❌ DB Sharding

---

## 🔗 관련 문서
- [성능개선_실행플랜_난이도별.md](./성능개선_실행플랜_난이도별.md) - 전체 실행 플랜
- [이메일_받은편지함_N+1_문제_해결_요약.md](./이메일_받은편지함_N+1_문제_해결_요약.md) - N+1 해결 상세
- [채팅방_목록_N+1_최적화_분석.md](./채팅방_목록_N+1_최적화_분석.md) - 채팅 N+1 분석

---

**마지막 업데이트**: 2025-12-19  
**담당자**: 개발팀  
**다음 리뷰**: 2025-12-26 (1주 후)

