# CoreConnect 성능 개선 실행 플랜 (난이도별)

> **목표**: AS-IS에서 발견된 3대 병목(DB 쿼리, Connection Pool, 캐싱)을 난이도별로 체계적 해결

---

## 📊 난이도별 개선 항목 전체 맵

```
┌─────────────────────────────────────────────────────────────────────┐
│ 난이도 매트릭스 (소요시간 × 기술 복잡도)                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  어려움 ┃                                  ⑨ Read Replica           │
│   ↑    ┃                              ⑧ DB Sharding                │
│        ┃                          ⑦ Redis Cluster                  │
│        ┃                      ⑥ Redis 캐싱                         │
│        ┃                  ⑤ DTO 최적화                             │
│        ┃              ④ Warm-up 스크립트                           │
│        ┃          ③ N+1 쿼리 해결                                  │
│        ┃      ② Connection Pool 증설                              │
│        ┃  ① DB 인덱스 추가                                         │
│  쉬움  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│         30분   2시간   1일    3일    1주    2주    1개월   2개월     │
│                           소요시간 →                                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟢 Level 1: 초급 (30분 ~ 2시간, 기술 난이도: ⭐)

### ✅ 개선 #1: DB 인덱스 추가

#### 📋 기본 정보
- **난이도**: ⭐☆☆☆☆ (매우 쉬움)
- **소요 시간**: 2시간
- **비용**: 0원
- **효과**: 응답시간 67% 단축 (25-30ms → 8-10ms)
- **리스크**: 매우 낮음 (인덱스 삭제 가능)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 이메일 받은편지함 조회: 25-30ms (평균의 2.7배)
├─ Full Table Scan 발생 (100,000+ rows 스캔)
├─ 인덱스 없어서 매번 전체 테이블 조회
└─ ORDER BY, WHERE 조건 최적화 불가
```

#### 🛠️ 기술 스택
- **DB**: MySQL 8.0
- **인덱스 타입**: B-Tree Index, Composite Index
- **도구**: MySQL Workbench, DBeaver, CLI

#### 📝 실행 단계 (체크리스트)

**Step 1: 현재 상태 확인 (10분)**
```sql
-- 1.1 느린 쿼리 확인
EXPLAIN SELECT * FROM email 
WHERE user_email = 'admin@coreconnect.io.kr' 
ORDER BY created_at DESC 
LIMIT 20;

-- 결과 확인 항목:
-- [ ] type: ALL (Full Table Scan) ← 문제!
-- [ ] rows: 100,000+ ← 너무 많음!
-- [ ] Extra: Using filesort ← 정렬 느림!

-- 1.2 기존 인덱스 확인
SHOW INDEX FROM email;

-- [ ] user_email 컬럼에 인덱스 없음 확인
-- [ ] created_at 컬럼에 인덱스 없음 확인
```

**Step 2: 인덱스 추가 (30분)**
```sql
-- 2.1 이메일 받은편지함 최적화 (최우선!)
CREATE INDEX idx_email_user_created 
ON email(user_email, created_at DESC);

-- 효과 확인:
-- [ ] EXPLAIN 다시 실행
-- [ ] type: ref (Index Scan) ← 개선됨!
-- [ ] rows: 20 (필요한 만큼만) ← 개선됨!

-- 2.2 이메일 읽지 않은 수 최적화
CREATE INDEX idx_email_status 
ON email(user_email, is_read, deleted_at);

-- 2.3 이메일 보낸편지함 최적화
CREATE INDEX idx_email_sender_created 
ON email(sender_email, created_at DESC);

-- 2.4 이메일 즐겨찾기 최적화
CREATE INDEX idx_email_favorite 
ON email(user_email, is_favorite, deleted_at, created_at DESC);

-- 2.5 채팅 읽지 않은 메시지 최적화
CREATE INDEX idx_chat_read_status 
ON chat_message_read_status(user_id, is_read);

-- 2.6 알림 조회 최적화 (기존 확인)
SHOW INDEX FROM notification;
-- [ ] user_id 인덱스 이미 존재하는지 확인
-- 없으면 추가:
-- CREATE INDEX idx_notification_user_created 
-- ON notification(user_id, created_at DESC);
```

**Step 3: 효과 측정 (30분)**
```sql
-- 3.1 EXPLAIN으로 실행 계획 비교
EXPLAIN SELECT * FROM email 
WHERE user_email = 'admin@coreconnect.io.kr' 
ORDER BY created_at DESC 
LIMIT 20;

-- 검증 체크리스트:
-- [ ] type: ref (Index Range Scan)
-- [ ] possible_keys: idx_email_user_created
-- [ ] key: idx_email_user_created (실제 사용)
-- [ ] rows: 20 (100,000+ → 20)
-- [ ] Extra: Using index condition (인덱스 사용 확인)

-- 3.2 실제 쿼리 성능 측정
SET profiling = 1;

SELECT * FROM email 
WHERE user_email = 'admin@coreconnect.io.kr' 
ORDER BY created_at DESC 
LIMIT 20;

SHOW PROFILES;

-- 목표:
-- [ ] Duration: 0.005 ~ 0.010초 (5-10ms)
-- [ ] Before 대비 70% 이상 단축 확인

-- 3.3 인덱스 크기 확인
SELECT 
    table_name,
    index_name,
    ROUND(stat_value * @@innodb_page_size / 1024 / 1024, 2) AS size_mb
FROM mysql.innodb_index_stats
WHERE table_name = 'email' 
  AND database_name = 'db_coreconnect';

-- [ ] 인덱스 크기가 테이블 크기의 30% 이내인지 확인
```

**Step 4: 프로덕션 적용 (30분)**
```bash
# 4.1 백업 (필수!)
mysqldump -u admin -p db_coreconnect > backup_before_index_$(date +%Y%m%d).sql
# [ ] 백업 파일 생성 확인
# [ ] 백업 파일 크기 확인 (0KB가 아닌지)

# 4.2 프로덕션 DB 접속
mysql -u admin -p -h 54.116.26.182 db_coreconnect

# 4.3 인덱스 추가 (피크 시간 피할 것!)
# 주의: CREATE INDEX는 테이블 락이 걸릴 수 있음
# MySQL 8.0의 경우 ALGORITHM=INPLACE 사용 (온라인 DDL)
CREATE INDEX idx_email_user_created 
ON email(user_email, created_at DESC) 
ALGORITHM=INPLACE, LOCK=NONE;

# [ ] 에러 없이 완료 확인
# [ ] SHOW INDEX로 인덱스 생성 확인

# 4.4 애플리케이션 재시작 없이 즉시 반영됨
# [ ] 백엔드 로그 확인 (에러 없는지)
```

**Step 5: k6 재테스트 (30분)**
```bash
# 5.1 k6 테스트 실행
cd ~/k6-loadtest
k6 run test.js --out json=result_after_index.json

# 5.2 결과 비교
# Before:
# - 이메일 받은편지함: 25-30ms
# - P95: 17ms

# After (목표):
# - 이메일 받은편지함: 8-10ms (67% 단축)
# - P95: 10ms 이하 (41% 단축)

# [ ] 목표 달성 확인
```

#### ✅ 완료 체크리스트
- [ ] EXPLAIN으로 Index Scan 확인
- [ ] 응답시간 67% 단축 확인 (25-30ms → 8-10ms)
- [ ] k6 재테스트로 개선 검증
- [ ] 프로덕션 배포 완료
- [ ] 모니터링 대시보드에서 개선 확인

#### 💡 주의사항
```
⚠️ 인덱스 추가 시 주의점:
1. 테이블 락 방지: ALGORITHM=INPLACE, LOCK=NONE 사용
2. 디스크 용량 확인: 인덱스는 테이블 크기의 20-30% 차지
3. 쓰기 성능 영향: INSERT/UPDATE가 약간 느려질 수 있음 (5% 이하)
4. 피크 시간 피하기: 새벽 3-4시 작업 권장

💡 롤백 방법:
DROP INDEX idx_email_user_created ON email;
```

---

### ✅ 개선 #2: Connection Pool 증설

#### 📋 기본 정보
- **난이도**: ⭐☆☆☆☆ (매우 쉬움)
- **소요 시간**: 30분
- **비용**: 0원
- **효과**: 연결 대기 97% 단축 (72ms → 2ms)
- **리스크**: 매우 낮음 (설정 복원 가능)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 현재 Connection Pool: 10개
├─ 요청: 45개 동시 요청
├─ 결과: 35개 요청 대기 (최대 72ms)
└─ Cold Start 시 150ms 스파이크
```

#### 🛠️ 기술 스택
- **Connection Pool**: HikariCP (Spring Boot 기본)
- **Thread Pool**: Tomcat Embedded
- **모니터링**: Spring Actuator, Prometheus

#### 📝 실행 단계

**Step 1: 현재 설정 확인 (5분)**
```bash
# 1.1 현재 application.yml 확인
cat backend/src/main/resources/application.yml | grep -A 10 "hikari"

# [ ] maximum-pool-size 확인 (현재: 10 또는 기본값)
# [ ] minimum-idle 확인 (현재: 5 또는 기본값)
```

**Step 2: 최적 Pool Size 계산 (5분)**
```
공식: Pool Size = (코어 수 × 2) + 디스크 수

EC2 인스턴스 확인:
[ ] 인스턴스 타입: t3.medium (2 vCPU)
[ ] 디스크 수: 1개
[ ] 계산: (2 × 2) + 1 = 5개 (최소)

실제 부하 기준:
[ ] 동시 사용자: 45명
[ ] 여유율 20%: 45 × 1.2 = 54개
[ ] 권장: 50-60개

최종 결정:
[ ] maximum-pool-size: 50
[ ] minimum-idle: 20 (항상 20개 유지)
```

**Step 3: 설정 변경 (10분)**
```yaml
# backend/src/main/resources/application.yml
spring:
  datasource:
    hikari:
      # Connection Pool 크기
      maximum-pool-size: 50          # 변경: 10 → 50
      minimum-idle: 20               # 변경: 5 → 20
      
      # 타임아웃 설정
      connection-timeout: 20000      # 20초 (기본: 30초)
      idle-timeout: 300000           # 5분 (유휴 연결 유지)
      max-lifetime: 1200000          # 20분 (연결 갱신)
      
      # 모니터링 및 검증
      leak-detection-threshold: 60000 # 1분 (연결 누수 감지)
      connection-test-query: SELECT 1 # 연결 검증
      validation-timeout: 5000       # 5초

  # Tomcat Thread Pool도 함께 조정
  server:
    tomcat:
      threads:
        max: 200                      # 최대 스레드 (기본: 200)
        min-spare: 50                 # 최소 유휴 스레드 (변경: 10 → 50)
      accept-count: 100               # 요청 대기 큐
      max-connections: 10000          # 최대 동시 연결

# [ ] 설정 파일 저장
```

**Step 4: 로컬 테스트 (5분)**
```bash
# 4.1 로컬에서 애플리케이션 실행
cd backend
./gradlew bootRun --args='--spring.profiles.active=local'

# 4.2 로그 확인
# [ ] HikariCP가 50개 Pool로 초기화되는지 확인
# 로그 예시:
# HikariPool-1 - Starting...
# HikariPool-1 - Start completed (maximum pool size: 50)

# 4.3 Actuator로 Pool 상태 확인
curl http://localhost:8080/actuator/metrics/hikari.connections.active
curl http://localhost:8080/actuator/metrics/hikari.connections.max

# [ ] max: 50 확인
# [ ] active: 1-5 (유휴 상태)
```

**Step 5: 프로덕션 배포 (5분)**
```bash
# 5.1 Git commit & push
git add backend/src/main/resources/application.yml
git commit -m "chore: HikariCP 설정 최적화 (Pool 10→50)"
git push origin main

# 5.2 Docker 이미지 재빌드
cd backend
docker build -t coreconnect-backend:v1.1 .

# 5.3 컨테이너 재시작
docker stop boot-container
docker rm boot-container
docker run -d \
  --name boot-container \
  --restart always \
  -p 8080:8080 \
  -e SPRING_PROFILES_ACTIVE=secure \
  coreconnect-backend:v1.1

# 5.4 로그 확인
docker logs -f boot-container | grep HikariCP

# [ ] "maximum pool size: 50" 확인
```

**Step 6: 효과 측정 (5분)**
```bash
# 6.1 k6 재테스트
k6 run test.js

# 6.2 결과 확인
# Before:
# - http_req_blocked max: 72.63ms (연결 대기)
# - Cold Start: 150ms

# After (목표):
# - http_req_blocked max: 2-5ms (97% 단축)
# - Cold Start: 50ms 이하 (67% 단축)

# [ ] 목표 달성 확인
```

#### ✅ 완료 체크리스트
- [ ] application.yml 수정 완료
- [ ] 로컬 테스트 성공
- [ ] 프로덕션 배포 완료
- [ ] 로그에서 Pool Size 50 확인
- [ ] k6 재테스트로 개선 검증
- [ ] 연결 대기 시간 97% 단축 확인

#### 💡 주의사항
```
⚠️ Pool Size 설정 시 주의점:
1. DB 서버 최대 연결 수 확인:
   SHOW VARIABLES LIKE 'max_connections';
   (MySQL 기본: 151개)
   
2. 메모리 사용량:
   Connection 1개당 약 4MB 메모리 사용
   50개 = 200MB (허용 가능)

3. DB 서버 부하:
   Connection이 너무 많으면 DB 메모리 부족
   권장: DB max_connections의 50% 이하

💡 롤백 방법:
application.yml에서 maximum-pool-size: 10으로 복원
```

---

## 🟡 Level 2: 중급 (1일 ~ 3일, 기술 난이도: ⭐⭐)

### ✅ 개선 #3: N+1 쿼리 해결

#### 📋 기본 정보
- **난이도**: ⭐⭐☆☆☆ (보통)
- **소요 시간**: 1일
- **비용**: 0원
- **효과**: DB 쿼리 수 95% 감소 (21개 → 1개)
- **리스크**: 중간 (코드 수정 필요)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 이메일 20개 조회 시 21개 쿼리 실행 (1 + 20)
├─ 각 이메일마다 첨부파일 별도 조회 (Lazy Loading)
├─ 응답시간 불필요하게 증가
└─ DB 부하 과다
```

#### 🛠️ 기술 스택
- **ORM**: Spring Data JPA, Hibernate
- **기법**: Fetch Join, @EntityGraph
- **도구**: IntelliJ IDEA, JPA show-sql

#### 📝 실행 단계

**Step 1: N+1 문제 확인 (30분)**
```yaml
# 1.1 application.yml에서 SQL 로깅 활성화
spring:
  jpa:
    show-sql: true               # SQL 콘솔 출력
    properties:
      hibernate:
        format_sql: true         # SQL 포맷팅
        use_sql_comments: true   # 주석 추가

# [ ] 설정 추가 완료
```

```bash
# 1.2 애플리케이션 실행 후 API 호출
curl "http://localhost:8080/api/v1/email/inbox?userEmail=admin@coreconnect.io.kr&page=0&size=20"

# 1.3 콘솔 로그 확인
# [ ] SELECT ... FROM email WHERE ... (1개)
# [ ] SELECT ... FROM attachment WHERE email_id = ? (20개 반복!)
# [ ] 총 21개 쿼리 확인 (N+1 문제)
```

**Step 2: Entity 관계 확인 (30분)**
```java
// 2.1 Email 엔티티 확인
@Entity
public class Email {
    @Id
    private Long id;
    
    // ⚠️ Lazy Loading 설정 확인
    @OneToMany(mappedBy = "email", fetch = FetchType.LAZY)
    private List<Attachment> attachments;
    
    // [ ] fetch = LAZY 확인 (기본값)
}

// 2.2 서비스 로직 확인
public List<EmailDTO> getInbox(String userEmail) {
    List<Email> emails = emailRepository.findByUserEmail(userEmail);
    
    return emails.stream()
        .map(email -> {
            // ⚠️ 여기서 N+1 발생!
            List<Attachment> attachments = email.getAttachments();
            return toDTO(email, attachments);
        })
        .collect(Collectors.toList());
}
// [ ] N+1 발생 지점 확인
```

**Step 3: Fetch Join으로 해결 (2시간)**

**방법 1: JPQL Fetch Join (권장)**
```java
// EmailRepository.java
@Repository
public interface EmailRepository extends JpaRepository<Email, Long> {
    
    // ❌ Before: N+1 쿼리 발생
    List<Email> findByUserEmail(String userEmail);
    
    // ✅ After: Fetch Join으로 한 번에 조회
    @Query("SELECT DISTINCT e FROM Email e " +
           "LEFT JOIN FETCH e.attachments " +
           "WHERE e.userEmail = :userEmail " +
           "ORDER BY e.createdAt DESC")
    List<Email> findByUserEmailWithAttachments(
        @Param("userEmail") String userEmail
    );
    
    // [ ] 새 메서드 추가 완료
}
```

**방법 2: @EntityGraph (대안)**
```java
// EmailRepository.java
@Repository
public interface EmailRepository extends JpaRepository<Email, Long> {
    
    @EntityGraph(attributePaths = {"attachments"})
    @Query("SELECT e FROM Email e " +
           "WHERE e.userEmail = :userEmail " +
           "ORDER BY e.createdAt DESC")
    List<Email> findByUserEmailWithAttachments(
        @Param("userEmail") String userEmail
    );
    
    // [ ] @EntityGraph 방식으로 추가
}
```

**Step 4: 서비스 로직 수정 (30분)**
```java
// EmailService.java
@Service
@RequiredArgsConstructor
public class EmailService {
    
    private final EmailRepository emailRepository;
    
    // ❌ Before
    public List<EmailDTO> getInbox(String userEmail) {
        List<Email> emails = emailRepository.findByUserEmail(userEmail);
        return emails.stream()
            .map(this::toDTO)
            .collect(Collectors.toList());
    }
    
    // ✅ After
    public List<EmailDTO> getInbox(String userEmail) {
        List<Email> emails = emailRepository
            .findByUserEmailWithAttachments(userEmail); // 메서드 변경
        
        return emails.stream()
            .map(this::toDTO)
            .collect(Collectors.toList());
    }
    
    // [ ] 메서드 호출 변경 완료
}
```

**Step 5: 단위 테스트 (1시간)**
```java
// EmailRepositoryTest.java
@SpringBootTest
@Transactional
class EmailRepositoryTest {
    
    @Autowired
    private EmailRepository emailRepository;
    
    @Test
    @DisplayName("N+1 문제 해결 확인")
    void testFetchJoin() {
        // Given: 테스트 데이터 준비
        String userEmail = "test@example.com";
        
        // 이메일 2개 생성
        Email email1 = createEmail(userEmail);
        email1.addAttachment(createAttachment("file1.pdf"));
        email1.addAttachment(createAttachment("file2.pdf"));
        emailRepository.save(email1);
        
        Email email2 = createEmail(userEmail);
        email2.addAttachment(createAttachment("file3.pdf"));
        emailRepository.save(email2);
        
        entityManager.flush();
        entityManager.clear(); // 영속성 컨텍스트 초기화
        
        // When: Fetch Join으로 조회
        List<Email> emails = emailRepository
            .findByUserEmailWithAttachments(userEmail);
        
        // Then: 쿼리 1개만 실행되는지 확인
        // (show-sql 로그로 수동 확인)
        assertThat(emails).hasSize(2);
        assertThat(emails.get(0).getAttachments()).hasSize(2);
        assertThat(emails.get(1).getAttachments()).hasSize(1);
        
        // [ ] 테스트 통과 확인
    }
}
```

**Step 6: 쿼리 수 검증 (30분)**
```bash
# 6.1 애플리케이션 재시작
./gradlew bootRun

# 6.2 API 호출
curl "http://localhost:8080/api/v1/email/inbox?userEmail=admin@coreconnect.io.kr&page=0&size=20"

# 6.3 콘솔 로그 확인
# ✅ Before: 21개 쿼리
# SELECT ... FROM email WHERE ... (1개)
# SELECT ... FROM attachment WHERE email_id = ? (20개)

# ✅ After: 1개 쿼리
# SELECT ... FROM email e 
# LEFT JOIN attachment a ON e.id = a.email_id 
# WHERE ... (1개)

# [ ] 쿼리 수 95% 감소 확인 (21 → 1)
```

**Step 7: 프로덕션 배포 (1시간)**
```bash
# 7.1 Git commit
git add backend/src/main/java/com/goodee/coreconnect/email/
git commit -m "fix: 이메일 조회 N+1 쿼리 해결 (Fetch Join)"
git push origin main

# 7.2 CI/CD 빌드 및 배포
# (Jenkins, GitHub Actions 등)

# 7.3 배포 후 모니터링
# [ ] 에러 로그 없는지 확인
# [ ] 응답시간 감소 확인
# [ ] DB 쿼리 수 감소 확인

# 7.4 k6 재테스트
k6 run test.js

# Before:
# - 평균 응답시간: 8-10ms (인덱스 추가 후)
# - DB 쿼리 수: 높음

# After (목표):
# - 평균 응답시간: 5-7ms (30% 단축)
# - DB 쿼리 수: 95% 감소

# [ ] 목표 달성 확인
```

#### ✅ 완료 체크리스트
- [ ] show-sql로 N+1 문제 확인
- [ ] Fetch Join 쿼리 작성 완료
- [ ] 서비스 로직 수정 완료
- [ ] 단위 테스트 통과
- [ ] 쿼리 수 95% 감소 확인 (21 → 1)
- [ ] 프로덕션 배포 완료
- [ ] k6 재테스트로 개선 검증

#### 💡 주의사항
```
⚠️ Fetch Join 사용 시 주의점:
1. MultipleBagFetchException 방지:
   - 2개 이상의 OneToMany를 동시에 Fetch Join하면 에러
   - 해결: @BatchSize 또는 여러 쿼리로 분리

2. DISTINCT 필수:
   - LEFT JOIN으로 중복 row 발생
   - "SELECT DISTINCT e" 사용 필수

3. 페이징 문제:
   - Fetch Join + Pageable은 메모리에서 페이징 (느림)
   - 해결: DTO Projection 또는 2단계 쿼리

💡 롤백 방법:
findByUserEmail() 메서드로 복원
```

---

### ✅ 개선 #4: Warm-up 스크립트

#### 📋 기본 정보
- **난이도**: ⭐⭐☆☆☆ (보통)
- **소요 시간**: 1일
- **비용**: 0원
- **효과**: Cold Start 93% 단축 (150ms → 10ms)
- **리스크**: 낮음

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 애플리케이션 시작 시 150ms 지연
├─ DB Connection Pool 초기화 느림
├─ JPA 메타데이터 로딩 지연
└─ 첫 쿼리 실행 계획 컴파일 느림
```

#### 🛠️ 기술 스택
- **Spring**: ApplicationReadyEvent
- **K8s**: Readiness Probe
- **모니터링**: Custom Health Indicator

#### 📝 실행 단계 (상세 생략, 파일 참조)

---

### ✅ 개선 #5: DTO 최적화

#### 📋 기본 정보
- **난이도**: ⭐⭐☆☆☆ (보통)
- **소요 시간**: 2일
- **비용**: 0원
- **효과**: 네트워크 전송량 60% 감소
- **리스크**: 중간 (코드 수정 필요)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 응답 크기: 1,200 bytes (불필요한 필드 포함)
├─ 전체 엔티티 반환 (password, deleted_at 등 포함)
├─ JSON 직렬화 시간 낭비
└─ 모바일 데이터 비용 증가
```

#### 📝 실행 단계 (상세 생략, 파일 참조)

---

## 🟠 Level 3: 고급 (3일 ~ 1주, 기술 난이도: ⭐⭐⭐)

### ✅ 개선 #6: Redis 캐싱 도입

#### 📋 기본 정보
- **난이도**: ⭐⭐⭐☆☆ (어려움)
- **소요 시간**: 3일
- **비용**: 월 $10-20
- **효과**: 응답시간 70% 단축, DB 부하 70% 감소
- **리스크**: 높음 (캐시 불일치, 메모리 관리)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ 캐시 히트율: 0% (모든 요청이 DB 직행)
├─ 같은 데이터를 2분에 595회 반복 조회
├─ DB 부하 100% (70% 절감 가능)
└─ 확장성 제한 (트래픽 증가 시 DB 병목)
```

#### 🛠️ 기술 스택
- **캐시**: Redis 7 (AWS ElastiCache 또는 Docker)
- **Client**: Lettuce (Spring Boot 기본)
- **추상화**: Spring Cache (@Cacheable)
- **직렬화**: Jackson JSON

#### 📝 실행 단계

**Step 1: Redis 설치 (Docker) (30분)**
```bash
# 1.1 Redis Docker 실행 (개발 환경)
docker run -d \
  --name redis \
  --restart always \
  -p 6379:6379 \
  redis:7-alpine

# [ ] 컨테이너 실행 확인
docker ps | grep redis

# 1.2 Redis 접속 테스트
docker exec -it redis redis-cli
> ping
PONG
> set test "hello"
OK
> get test
"hello"
> exit

# [ ] Redis 정상 동작 확인
```

**프로덕션용 (영구 저장 + 메모리 제한)**
```bash
docker run -d \
  --name redis \
  --restart always \
  -p 6379:6379 \
  -v redis-data:/data \
  -m 512m \  # 메모리 제한 512MB
  redis:7-alpine redis-server \
    --appendonly yes \  # AOF 영구 저장
    --maxmemory 450mb \ # 최대 메모리
    --maxmemory-policy allkeys-lru  # LRU 제거 정책

# [ ] 프로덕션 Redis 실행 확인
```

**Step 2: Spring 의존성 추가 (10분)**
```gradle
// build.gradle
dependencies {
    // Redis
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.boot:spring-boot-starter-cache'
    
    // (선택) Redis 모니터링
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
}
```

```bash
# Gradle 빌드
./gradlew build

# [ ] 의존성 추가 확인
```

**Step 3: Redis 설정 (30분)**
```yaml
# application.yml
spring:
  cache:
    type: redis
    redis:
      time-to-live: 30000  # 30초 (기본 TTL)
      cache-null-values: false  # null 캐싱 비활성화
      
  data:
    redis:
      host: localhost  # 개발: localhost, 프로덕션: Redis 서버 IP
      port: 6379
      timeout: 3000ms  # 연결 타임아웃
      
      lettuce:
        pool:
          max-active: 20   # 최대 연결 수
          max-idle: 10     # 최대 유휴 연결
          min-idle: 5      # 최소 유휴 연결
          max-wait: 3000ms # 연결 대기 시간

# [ ] 설정 파일 추가 완료
```

**Step 4: Redis Configuration 클래스 (1시간)**
```java
// RedisConfig.java
@Configuration
@EnableCaching  // Spring Cache 활성화
public class RedisConfig {
    
    @Bean
    public CacheManager cacheManager(RedisConnectionFactory connectionFactory) {
        // 캐시별 TTL 설정
        Map<String, RedisCacheConfiguration> cacheConfigurations = new HashMap<>();
        
        // 1. 읽지 않은 메일 수 (30초)
        cacheConfigurations.put("email:unread",
            RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofSeconds(30))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                    .fromSerializer(new GenericJackson2JsonRedisSerializer()))
        );
        
        // 2. 채팅 최신 메시지 (10초)
        cacheConfigurations.put("chat:latest",
            RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofSeconds(10))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                    .fromSerializer(new GenericJackson2JsonRedisSerializer()))
        );
        
        // 3. 알림 목록 (60초)
        cacheConfigurations.put("notification:list",
            RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofSeconds(60))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                    .fromSerializer(new GenericJackson2JsonRedisSerializer()))
        );
        
        // 4. 이메일 받은편지함 (2분)
        cacheConfigurations.put("email:inbox",
            RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofMinutes(2))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                    .fromSerializer(new GenericJackson2JsonRedisSerializer()))
        );
        
        return RedisCacheManager.builder(connectionFactory)
            .cacheDefaults(RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofMinutes(1)))  // 기본 TTL
            .withInitialCacheConfigurations(cacheConfigurations)
            .build();
    }
    
    // [ ] RedisConfig 클래스 생성 완료
}
```

**Step 5: @Cacheable 적용 (2시간)**

**우선순위 1: 읽지 않은 메일 수 (가장 효과 큼)**
```java
// EmailService.java
@Service
@RequiredArgsConstructor
public class EmailService {
    
    private final EmailRepository emailRepository;
    
    // ✅ 캐싱 적용
    @Cacheable(
        value = "email:unread",          // 캐시 이름
        key = "#userEmail",              // 캐시 키
        unless = "#result == null"       // null은 캐싱 안함
    )
    public Long getUnreadCount(String userEmail) {
        log.info("🔍 Cache MISS: DB 조회 - {}", userEmail);
        return emailRepository.countByUserEmailAndIsRead(userEmail, false);
    }
    
    // ✅ 캐시 무효화 (메일 읽음 처리 시)
    @CacheEvict(value = "email:unread", key = "#userEmail")
    public void markAsRead(String userEmail, Long emailId) {
        log.info("🗑️ Cache EVICT: {}", userEmail);
        emailRepository.updateReadStatus(emailId, true);
    }
    
    // [ ] @Cacheable, @CacheEvict 적용 완료
}
```

**우선순위 2: 채팅 최신 메시지**
```java
// ChatService.java
@Service
@RequiredArgsConstructor
public class ChatService {
    
    @Cacheable(
        value = "chat:latest",
        key = "#roomId",
        unless = "#result == null || #result.isEmpty()"
    )
    public List<ChatMessageDTO> getLatestMessages(Long roomId) {
        log.info("🔍 Cache MISS: DB 조회 - roomId={}", roomId);
        return chatRepository.findLatestByRoomId(roomId, PageRequest.of(0, 50));
    }
    
    // 메시지 전송 시 캐시 무효화
    @CacheEvict(value = "chat:latest", key = "#roomId")
    public void sendMessage(Long roomId, String content) {
        log.info("🗑️ Cache EVICT: roomId={}", roomId);
        chatRepository.save(new ChatMessage(roomId, content));
    }
}
```

**우선순위 3: 알림 목록**
```java
// NotificationService.java
@Service
@RequiredArgsConstructor
public class NotificationService {
    
    @Cacheable(
        value = "notification:list",
        key = "#userId"
    )
    public List<NotificationDTO> getNotifications(Long userId) {
        log.info("🔍 Cache MISS: DB 조회 - userId={}", userId);
        return notificationRepository.findByUserIdOrderByCreatedAtDesc(
            userId, PageRequest.of(0, 30)
        );
    }
    
    // 알림 생성 시 캐시 무효화
    @CacheEvict(value = "notification:list", key = "#userId")
    public void createNotification(Long userId, String message) {
        log.info("🗑️ Cache EVICT: userId={}", userId);
        notificationRepository.save(new Notification(userId, message));
    }
}
```

**Step 6: 캐시 동작 테스트 (1시간)**
```bash
# 6.1 애플리케이션 재시작
./gradlew bootRun

# 6.2 첫 번째 호출 (Cache MISS)
curl "http://localhost:8080/api/v1/email/inbox/unread-count?userEmail=admin@coreconnect.io.kr"

# 로그 확인:
# 🔍 Cache MISS: DB 조회 - admin@coreconnect.io.kr
# Hibernate: SELECT COUNT(*) FROM email WHERE ...

# [ ] Cache MISS 로그 확인
# [ ] DB 쿼리 실행 확인

# 6.3 두 번째 호출 (Cache HIT)
curl "http://localhost:8080/api/v1/email/inbox/unread-count?userEmail=admin@coreconnect.io.kr"

# 로그 확인:
# (DB 쿼리 없음! 캐시에서 반환)

# [ ] Cache HIT 확인 (로그 없음)
# [ ] 응답 시간 단축 확인 (15ms → 1ms)

# 6.4 Redis에서 직접 확인
docker exec -it redis redis-cli
> KEYS *
1) "email:unread::admin@coreconnect.io.kr"
> TTL "email:unread::admin@coreconnect.io.kr"
(integer) 25  # 30초 중 25초 남음
> GET "email:unread::admin@coreconnect.io.kr"
"{\"@class\":\"java.lang.Long\",\"value\":5}"

# [ ] Redis에 데이터 저장 확인
```

**Step 7: 캐시 모니터링 추가 (1시간)**
```java
// CacheStatsController.java
@RestController
@RequestMapping("/api/admin/cache")
@RequiredArgsConstructor
public class CacheStatsController {
    
    private final CacheManager cacheManager;
    private final RedisTemplate<String, Object> redisTemplate;
    
    @GetMapping("/stats")
    public Map<String, CacheStats> getCacheStats() {
        Map<String, CacheStats> stats = new HashMap<>();
        
        cacheManager.getCacheNames().forEach(cacheName -> {
            Cache cache = cacheManager.getCache(cacheName);
            if (cache instanceof RedisCache) {
                // Redis 캐시 통계
                long keyCount = getKeyCount(cacheName);
                long memoryUsage = getMemoryUsage(cacheName);
                
                stats.put(cacheName, CacheStats.builder()
                    .name(cacheName)
                    .keyCount(keyCount)
                    .memoryUsageMB(memoryUsage / 1024 / 1024)
                    .build());
            }
        });
        
        return stats;
    }
    
    @GetMapping("/keys/{cacheName}")
    public List<String> getCacheKeys(@PathVariable String cacheName) {
        Set<String> keys = redisTemplate.keys(cacheName + "::*");
        return new ArrayList<>(keys);
    }
    
    @DeleteMapping("/evict/{cacheName}")
    public ResponseEntity<String> evictCache(@PathVariable String cacheName) {
        Cache cache = cacheManager.getCache(cacheName);
        if (cache != null) {
            cache.clear();
            return ResponseEntity.ok("Cache evicted: " + cacheName);
        }
        return ResponseEntity.notFound().build();
    }
}
```

**Step 8: 프로덕션 배포 및 검증 (1일)**
```bash
# 8.1 프로덕션 Redis 서버 설정
# AWS ElastiCache 생성 또는 EC2에 Redis 설치

# 8.2 application-prod.yml 수정
spring:
  data:
    redis:
      host: redis.coreconnect.internal  # 프로덕션 Redis IP
      port: 6379

# 8.3 배포
git add .
git commit -m "feat: Redis 캐싱 도입 (읽지 않은 메일 수, 채팅, 알림)"
git push origin main

# 8.4 k6 재테스트
k6 run test.js

# Before (Phase 1 개선 후):
# - 평균 응답시간: 6-7ms
# - DB 쿼리 수: 5,354개

# After (목표):
# - 평균 응답시간: 2-3ms (70% 단축)
# - DB 쿼리 수: 1,600개 (70% 감소)
# - 캐시 히트율: 70% 이상

# [ ] 목표 달성 확인
```

**Step 9: 캐시 히트율 모니터링 (지속)**
```bash
# Grafana 대시보드 설정
# Prometheus metrics:
# - cache_gets_total{cache="email:unread",result="hit"}
# - cache_gets_total{cache="email:unread",result="miss"}
# - cache_hit_rate = hits / (hits + misses)

# 목표:
# - Cache Hit Rate: 70% 이상
# - P95 응답시간: 5ms 이하
```

#### ✅ 완료 체크리스트
- [ ] Redis Docker 실행 확인
- [ ] Spring 의존성 추가 완료
- [ ] RedisConfig 설정 완료
- [ ] @Cacheable 적용 (4개 API)
- [ ] 캐시 동작 테스트 통과 (MISS → HIT)
- [ ] 캐시 모니터링 API 추가
- [ ] 프로덕션 배포 완료
- [ ] k6 재테스트로 개선 검증
- [ ] 캐시 히트율 70% 이상 달성

#### 💡 주의사항
```
⚠️ Redis 캐싱 시 주의점:
1. 캐시 불일치 (Cache Invalidation):
   - 데이터 변경 시 반드시 @CacheEvict 실행
   - TTL을 짧게 설정 (30초-2분)
   - 실시간성이 중요한 데이터는 캐싱 비추천

2. 메모리 관리:
   - maxmemory 설정 필수
   - LRU 정책으로 자동 제거
   - 모니터링: redis-cli INFO memory

3. 직렬화 문제:
   - DTO는 Serializable 구현 필요 없음 (Jackson JSON 사용)
   - LocalDateTime 등은 Jackson 모듈 추가 필요

4. 성능 저하 케이스:
   - 캐시 키가 너무 많으면 Redis 메모리 부족
   - 캐시 데이터가 너무 크면 직렬화 느림 (1MB 이하 권장)

💡 롤백 방법:
@Cacheable 어노테이션 제거 또는 주석 처리
Redis 컨테이너 중지: docker stop redis
```

---

## 🔴 Level 4: 전문가 (1주 ~ 2개월, 기술 난이도: ⭐⭐⭐⭐)

### ✅ 개선 #7: Redis Cluster (고가용성)

#### 📋 기본 정보
- **난이도**: ⭐⭐⭐⭐☆ (매우 어려움)
- **소요 시간**: 1주
- **비용**: 월 $50-100
- **효과**: 캐시 고가용성 99.9%, 처리량 3배
- **리스크**: 매우 높음 (클러스터 설정 복잡)

#### 📝 실행 단계 (개요만 제공)
```
1. AWS ElastiCache Redis Cluster 생성 (3 Master, 3 Replica)
2. Spring Lettuce Cluster 설정
3. 샤딩 전략 설계
4. 페일오버 테스트
```

---

### ✅ 개선 #8: DB Sharding (수평 확장)

#### 📋 기본 정보
- **난이도**: ⭐⭐⭐⭐⭐ (최고 난이도)
- **소요 시간**: 1개월
- **비용**: 월 $200+
- **효과**: 확장성 10배
- **리스크**: 매우 높음 (데이터 마이그레이션 복잡)

---

### ✅ 개선 #9: Read Replica 추가

#### 📋 기본 정보
- **난이도**: ⭐⭐⭐☆☆ (어려움)
- **소요 시간**: 1주
- **비용**: 월 $50
- **효과**: 읽기 처리량 2배, Master DB 부하 50% 감소
- **리스크**: 중간 (Replication Lag)

#### 🎯 해결할 문제
```
AS-IS 문제:
├─ Master DB CPU 80% (읽기:쓰기 = 9:1)
├─ 읽기 쿼리 증가 시 Master 병목
├─ 확장성 제한 (스케일 업만 가능)
└─ 단일 장애점 (Master 다운 시 전체 중단)
```

#### 📝 실행 단계 (상세)

**Step 1: AWS RDS Read Replica 생성 (30분)**
```bash
# 1.1 AWS Console 또는 CLI로 생성
aws rds create-db-instance-read-replica \
  --db-instance-identifier coreconnect-replica-1 \
  --source-db-instance-identifier coreconnect-master \
  --db-instance-class db.t3.medium \
  --availability-zone ap-northeast-2a \
  --publicly-accessible

# [ ] Read Replica 생성 완료 (5-10분 소요)
# [ ] 엔드포인트 확인: coreconnect-replica-1.xxx.rds.amazonaws.com
```

**Step 2: Spring 다중 DataSource 설정 (2시간)**
```yaml
# application.yml
spring:
  datasource:
    # Master (쓰기 전용)
    master:
      jdbc-url: jdbc:mysql://coreconnect-master.rds.amazonaws.com:3306/db_coreconnect
      username: admin
      password: ${DB_PASSWORD}
      hikari:
        maximum-pool-size: 30
        read-only: false
    
    # Slave (읽기 전용)
    slave:
      jdbc-url: jdbc:mysql://coreconnect-replica-1.rds.amazonaws.com:3306/db_coreconnect
      username: admin
      password: ${DB_PASSWORD}
      hikari:
        maximum-pool-size: 50  # 읽기가 더 많으므로 크게 설정
        read-only: true
```

**Step 3: DataSource 라우팅 설정 (4시간)**
```java
// DataSourceConfig.java
@Configuration
public class DataSourceConfig {
    
    @Bean
    @ConfigurationProperties(prefix = "spring.datasource.master")
    public DataSource masterDataSource() {
        return DataSourceBuilder.create().build();
    }
    
    @Bean
    @ConfigurationProperties(prefix = "spring.datasource.slave")
    public DataSource slaveDataSource() {
        return DataSourceBuilder.create().build();
    }
    
    @Bean
    public DataSource routingDataSource(
        @Qualifier("masterDataSource") DataSource master,
        @Qualifier("slaveDataSource") DataSource slave
    ) {
        ReplicationRoutingDataSource routingDataSource = 
            new ReplicationRoutingDataSource();
        
        Map<Object, Object> dataSources = new HashMap<>();
        dataSources.put("master", master);
        dataSources.put("slave", slave);
        
        routingDataSource.setTargetDataSources(dataSources);
        routingDataSource.setDefaultTargetDataSource(master);
        
        return routingDataSource;
    }
    
    @Primary
    @Bean
    public DataSource dataSource(
        @Qualifier("routingDataSource") DataSource routingDataSource
    ) {
        return new LazyConnectionDataSourceProxy(routingDataSource);
    }
}

// ReplicationRoutingDataSource.java
public class ReplicationRoutingDataSource extends AbstractRoutingDataSource {
    
    @Override
    protected Object determineCurrentLookupKey() {
        // 현재 트랜잭션이 읽기 전용인지 확인
        boolean isReadOnly = TransactionSynchronizationManager.isCurrentTransactionReadOnly();
        
        log.debug("DataSource 라우팅: {}", isReadOnly ? "slave" : "master");
        
        return isReadOnly ? "slave" : "master";
    }
}
```

**Step 4: 서비스에서 @Transactional 설정 (1시간)**
```java
// EmailService.java
@Service
@RequiredArgsConstructor
public class EmailService {
    
    // ✅ 읽기 전용 → Slave DB 사용
    @Transactional(readOnly = true)
    public List<EmailDTO> getInbox(String userEmail) {
        return emailRepository.findByUserEmailWithAttachments(userEmail);
    }
    
    // ✅ 쓰기 작업 → Master DB 사용
    @Transactional
    public void sendEmail(EmailDTO emailDTO) {
        emailRepository.save(toEntity(emailDTO));
    }
    
    // [ ] @Transactional(readOnly = true) 적용 확인
}
```

**Step 5: Replication Lag 모니터링 (1일)**
```java
// ReplicationLagMonitor.java
@Component
@Slf4j
public class ReplicationLagMonitor {
    
    @Autowired
    @Qualifier("masterDataSource")
    private DataSource masterDataSource;
    
    @Autowired
    @Qualifier("slaveDataSource")
    private DataSource slaveDataSource;
    
    @Scheduled(fixedDelay = 10000)  // 10초마다 확인
    public void checkReplicationLag() {
        try (Connection master = masterDataSource.getConnection();
             Connection slave = slaveDataSource.getConnection()) {
            
            // Master의 현재 위치
            String masterQuery = "SHOW MASTER STATUS";
            ResultSet masterRs = master.createStatement().executeQuery(masterQuery);
            masterRs.next();
            long masterPos = masterRs.getLong("Position");
            
            // Slave의 현재 위치
            String slaveQuery = "SHOW SLAVE STATUS";
            ResultSet slaveRs = slave.createStatement().executeQuery(slaveQuery);
            slaveRs.next();
            long slavePos = slaveRs.getLong("Exec_Master_Log_Pos");
            int secondsBehind = slaveRs.getInt("Seconds_Behind_Master");
            
            // Lag 계산
            long lag = masterPos - slavePos;
            
            if (secondsBehind > 5) {
                log.warn("⚠️ Replication Lag: {} seconds, {} bytes", secondsBehind, lag);
            } else {
                log.debug("✅ Replication OK: {} seconds lag", secondsBehind);
            }
            
        } catch (Exception e) {
            log.error("Replication 모니터링 실패", e);
        }
    }
}
```

#### ✅ 완료 체크리스트
- [ ] AWS Read Replica 생성 완료
- [ ] 다중 DataSource 설정 완료
- [ ] 라우팅 로직 구현 완료
- [ ] @Transactional(readOnly) 적용 (모든 조회 API)
- [ ] Replication Lag 모니터링 추가
- [ ] 프로덕션 배포 완료
- [ ] Master CPU 50% 감소 확인
- [ ] 읽기 처리량 2배 증가 확인

---

## 📊 전체 개선 로드맵 타임라인

```
Week 1 (즉시 실행)
├─ Day 1-2: ✅ #1 DB 인덱스 추가 (2시간)
├─ Day 1-2: ✅ #2 Connection Pool 증설 (30분)
├─ Day 3-5: ✅ #3 N+1 쿼리 해결 (1일)
└─ 효과: 응답시간 40% 단축, 비용 0원

Week 2-3 (단기 실행)
├─ Day 8-10: ✅ #6 Redis 캐싱 도입 (3일)
├─ Day 11-12: ✅ #5 DTO 최적화 (2일)
├─ Day 13: ✅ #4 Warm-up 스크립트 (1일)
└─ 효과: 응답시간 70% 단축, 처리량 2배, 월 $10

Week 4-8 (장기 실행)
├─ Week 4-5: ✅ #9 Read Replica 추가 (1주)
├─ Week 6-7: ⚠️ #7 Redis Cluster (고급, 선택)
├─ Week 8+: 🔴 #8 DB Sharding (최종 단계, 필요시)
└─ 효과: 처리량 3배, 확장성 6배, 월 $60-120
```

---

## 🎯 최종 효과 예측

| 지표 | AS-IS | Week 1 | Week 3 | Week 8 |
|------|-------|--------|--------|--------|
| **평균 응답시간** | 11.10ms | 6-7ms | 3-4ms | 2-3ms |
| **P95 응답시간** | 17ms | 10ms | 5ms | 3ms |
| **처리량 (RPS)** | 44 req/s | 60 req/s | 90 req/s | 130+ req/s |
| **분당 처리량** | 2,653 | 3,600 | 5,400 | 7,800+ |
| **DB 쿼리 수** | 5,354 | 1,070 | 1,600 | 800 |
| **확장 가능 VUs** | 45 | 80 | 150 | 300+ |
| **월 비용** | - | 0원 | $10 | $60-120 |

---

## 📋 우선순위 의사결정 가이드

### 즉시 시작해야 하는 항목 (🔥 최우선)
```
✅ #1 DB 인덱스 추가
✅ #2 Connection Pool 증설
→ 이유: 비용 0원, 소요시간 2시간, 효과 67-97% 개선
```

### 1주일 내 완료해야 하는 항목 (⚠️ 중요)
```
✅ #3 N+1 쿼리 해결
→ 이유: DB 쿼리 수 95% 감소, 확장성 기반
```

### 2-3주 내 완료 권장 (💡 권장)
```
✅ #6 Redis 캐싱 도입
✅ #5 DTO 최적화
✅ #4 Warm-up 스크립트
→ 이유: 비용 대비 효과 매우 높음 (월 $10로 2배 개선)
```

### 장기 계획 (📅 선택)
```
✅ #9 Read Replica 추가
⚠️ #7 Redis Cluster (트래픽 200+ VUs 시)
🔴 #8 DB Sharding (트래픽 500+ VUs 시)
→ 이유: 확장성 확보, 트래픽 증가에 대비
```

---

**작성일**: 2025-12-17  
**목적**: AS-IS 문제 해결 실행 플랜  
**버전**: 1.0  
**예상 완료**: Phase 1-2는 3주, Phase 3는 2개월

