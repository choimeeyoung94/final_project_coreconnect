# 이메일 받은편지함 조회 쿼리 최적화 상세 분석

> **핵심 문제**: `email_recipient`와 `email` 테이블의 JOIN 쿼리가 느림 (25-30ms)

---

## 📊 현재 쿼리 구조 분석

### 1. 테이블 스키마

#### `email_recipient` 테이블 (수신자 정보)
```sql
CREATE TABLE email_recipient (
    emailRecipientId INT PRIMARY KEY AUTO_INCREMENT,
    emailRecipientAddress VARCHAR(100),    -- 수신자 이메일 (예: user@example.com)
    emailRecipientType VARCHAR(10),        -- TO/CC/BCC
    emailReadYn BOOLEAN DEFAULT FALSE,     -- 읽음 여부
    deleted BOOLEAN DEFAULT FALSE,         -- 삭제 여부
    email_id INT,                          -- FK to email.emailId
    userId INT,                            -- FK to user.id (내부 직원)
    emailReadAt DATETIME,
    deletedAt DATETIME,
    emailIsAlarmSent BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (email_id) REFERENCES email(emailId)
);
```

#### `email` 테이블 (메일 본문)
```sql
CREATE TABLE email (
    emailId INT PRIMARY KEY AUTO_INCREMENT,
    emailTitle TEXT NOT NULL,              -- 제목
    emailContent TEXT NOT NULL,            -- 본문
    senderEmail VARCHAR(100) NOT NULL,     -- 발신자 이메일
    senderId INT NOT NULL,                 -- 발신자 ID (FK to user.id)
    emailSentTime DATETIME,                -- 발송 시간
    emailStatus VARCHAR(20),               -- SENT/TRASH/DELETED/DRAFT/RESERVED
    favoriteStatus BOOLEAN DEFAULT FALSE,  -- 중요 표시
    emailDeleteYn BOOLEAN DEFAULT FALSE,
    emailSaveStatusYn BOOLEAN DEFAULT FALSE,
    emailType VARCHAR(50),
    emailBounceReason VARCHAR(255),
    emailDeletedTime DATETIME,
    emailReadAt DATETIME,
    reservedAt DATETIME,
    emailFolder VARCHAR(50),               -- INBOX/SENT/TRASH
    replyToEmailId VARCHAR(50),
    emailId2 INT,
    FOREIGN KEY (senderId) REFERENCES user(id)
);
```

---

### 2. 현재 JPQL 쿼리 (가장 많이 사용)

**Repository: `EmailRecipientRepository.findInboxExcludingTrash`**

```java
@Query("SELECT r FROM EmailRecipient r " +
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +  // ① 수신자 필터
       "AND r.emailRecipientType IN :emailRecipientType " +          // ② TO/CC/BCC 필터
       "AND r.email.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED') " + // ③ 상태 필터
       "AND (r.deleted IS NULL OR r.deleted = false) " +             // ④ 삭제 여부
       "AND (:keyword IS NULL OR :keyword = '' OR " +                // ⑤ 검색 조건
       "    ((:searchType = 'TITLE' AND LOWER(r.email.emailTitle) LIKE LOWER(CONCAT('%', :keyword, '%'))) OR " +
       "     (:searchType = 'CONTENT' AND LOWER(r.email.emailContent) LIKE LOWER(CONCAT('%', :keyword, '%'))) OR " +
       "     (:searchType = 'TITLE_CONTENT' AND (" +
       "         LOWER(r.email.emailTitle) LIKE LOWER(CONCAT('%', :keyword, '%')) OR " +
       "         LOWER(r.email.emailContent) LIKE LOWER(CONCAT('%', :keyword, '%'))" +
       "     )))" +
       ") " +
       "ORDER BY r.email.emailSentTime DESC, r.email.reservedAt DESC")  // ⑥ 정렬
Page<EmailRecipient> findInboxExcludingTrash(
    @Param("emailRecipientAddress") String emailRecipientAddress,
    @Param("emailRecipientType") List<String> emailRecipientType,
    Pageable pageable,
    @Param("searchType") String searchType,
    @Param("keyword") String keyword
);
```

---

### 3. 실제 실행되는 MySQL 쿼리 (추정)

```sql
SELECT 
    r.emailRecipientId,
    r.emailRecipientAddress,
    r.emailRecipientType,
    r.emailReadYn,
    r.deleted,
    r.email_id,
    r.userId,
    e.*  -- email 테이블의 모든 컬럼
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.emailId  -- ⚠️ JOIN 발생!
WHERE 
    r.emailRecipientAddress = 'admin@coreconnect.io.kr'  -- ① 인덱스 없음!
    AND r.emailRecipientType IN ('TO', 'CC', 'BCC')      -- ② 인덱스 없음!
    AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')  -- ③ 인덱스 없음!
    AND (r.deleted IS NULL OR r.deleted = FALSE)          -- ④ 인덱스 없음!
    AND (
        :keyword IS NULL 
        OR LOWER(e.emailTitle) LIKE CONCAT('%', LOWER(:keyword), '%')
        OR LOWER(e.emailContent) LIKE CONCAT('%', LOWER(:keyword), '%')
    )
ORDER BY 
    e.emailSentTime DESC,  -- ⑥ 인덱스 없음! 정렬 느림!
    e.reservedAt DESC
LIMIT 20 OFFSET 0;
```

---

## 🔍 병목 지점 분석 (EXPLAIN 실행)

### 현재 상태 (인덱스 없음)

```sql
EXPLAIN SELECT 
    r.emailRecipientId,
    r.emailRecipientAddress,
    r.emailRecipientType,
    r.emailReadYn,
    r.deleted,
    r.email_id,
    e.emailId,
    e.emailTitle,
    e.senderEmail,
    e.emailSentTime,
    e.emailStatus,
    e.reservedAt
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.emailId
WHERE 
    r.emailRecipientAddress = 'admin@coreconnect.io.kr'
    AND r.emailRecipientType IN ('TO', 'CC', 'BCC')
    AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
    AND (r.deleted IS NULL OR r.deleted = FALSE)
ORDER BY 
    e.emailSentTime DESC,
    e.reservedAt DESC
LIMIT 20;
```

#### 예상 EXPLAIN 결과 (인덱스 없을 때)

| id | select_type | table | type | possible_keys | key  | rows    | Extra |
|----|-------------|-------|------|---------------|------|---------|-------|
| 1  | SIMPLE      | r     | **ALL** | **NULL** | **NULL** | **50,000** | **Using where; Using filesort** |
| 1  | SIMPLE      | e     | eq_ref | PRIMARY | PRIMARY | 1 | Using where |

#### 문제점 분석

1. **`r` (email_recipient) 테이블**: 
   - **type: ALL** → **Full Table Scan** (전체 테이블 스캔!)
   - **rows: 50,000** → 5만 개 행을 모두 읽음
   - **possible_keys: NULL** → 사용 가능한 인덱스 없음
   - **key: NULL** → 실제 사용된 인덱스 없음
   - **Extra: Using where; Using filesort** → 메모리/디스크에서 정렬

2. **`e` (email) 테이블**:
   - **type: eq_ref** → PRIMARY KEY 사용 (양호)
   - **rows: 1** → JOIN당 1개 행만 읽음
   - **Using where** → WHERE 조건 추가 필터링

3. **총 스캔 행수**: 50,000 + 50,000 = **100,000 rows** (최악의 경우)

4. **정렬 비용**: 
   - `ORDER BY e.emailSentTime DESC` → **filesort** 발생
   - 50,000개 행을 정렬해야 함 (매우 느림!)

---

## 🚨 병목 순위 (심각도 순)

### 🔥 병목 #1: `emailRecipientAddress` 인덱스 없음 (심각도: ★★★★★)

**문제**:
```sql
WHERE r.emailRecipientAddress = 'admin@coreconnect.io.kr'
```
- **Full Table Scan 발생**: 50,000개 행을 모두 읽음
- **예상 스캔 시간**: 10-15ms

**영향**:
- 전체 쿼리 시간의 50% 차지

---

### 🔥 병목 #2: `emailSentTime` 인덱스 없음 (심각도: ★★★★★)

**문제**:
```sql
ORDER BY e.emailSentTime DESC, e.reservedAt DESC
```
- **Using filesort 발생**: 메모리 또는 디스크에서 정렬
- **예상 정렬 시간**: 5-10ms

**영향**:
- 전체 쿼리 시간의 30% 차지

---

### 🔥 병목 #3: `emailStatus` 인덱스 없음 (심각도: ★★★★☆)

**문제**:
```sql
AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
```
- **NOT IN 조건**: 인덱스 사용 불가 (일반적으로)
- **추가 필터링 비용**: 2-3ms

**영향**:
- 전체 쿼리 시간의 15% 차지

---

### ⚠️ 병목 #4: 복합 조건 최적화 부족 (심각도: ★★★☆☆)

**문제**:
```sql
AND r.emailRecipientType IN ('TO', 'CC', 'BCC')
AND (r.deleted IS NULL OR r.deleted = FALSE)
```
- 단일 컬럼 인덱스로는 복합 조건 최적화 불가
- **복합 인덱스 필요**

---

### ⚠️ 병목 #5: TEXT 컬럼 검색 (심각도: ★★☆☆☆)

**문제**:
```sql
AND LOWER(e.emailTitle) LIKE CONCAT('%', LOWER(:keyword), '%')
AND LOWER(e.emailContent) LIKE CONCAT('%', LOWER(:keyword), '%')
```
- **LIKE '%keyword%'**: 인덱스 사용 불가
- **TEXT 컬럼**: 전체 스캔 필요
- **해결**: Full-Text Search 또는 Elasticsearch 필요 (장기)

---

## ✅ 해결 방안 (우선순위별)

### 🔥 우선순위 1: 복합 인덱스 추가 (즉시 실행, 2시간)

#### 인덱스 #1: `email_recipient` 테이블

```sql
-- ✅ 복합 인덱스 (가장 중요!)
CREATE INDEX idx_email_recipient_lookup 
ON email_recipient(
    emailRecipientAddress,   -- ① WHERE 조건 (Equality)
    emailRecipientType,      -- ② WHERE 조건 (IN)
    deleted,                 -- ③ WHERE 조건 (IS NULL OR = FALSE)
    email_id                 -- ④ JOIN 조건 (커버링 인덱스 효과)
);

-- 예상 효과: Full Table Scan → Index Range Scan
-- 스캔 행수: 50,000 → 100-500 (99% 감소)
-- 응답시간: 15ms → 2ms (87% 단축)
```

#### 인덱스 설계 원칙 (중요!)

**복합 인덱스 컬럼 순서**:
1. **Equality 조건** (`=`) 먼저
2. **IN 조건** 그 다음
3. **범위 조건** (`>`, `<`, `BETWEEN`) 마지막

**이 경우**:
- `emailRecipientAddress = ?` (Equality) → 1순위
- `emailRecipientType IN (...)` (IN) → 2순위
- `deleted IS NULL OR deleted = FALSE` (조건부) → 3순위
- `email_id` (JOIN 최적화) → 4순위

---

#### 인덱스 #2: `email` 테이블

```sql
-- ✅ 정렬 최적화 인덱스
CREATE INDEX idx_email_status_sent_time 
ON email(
    emailStatus,      -- ① WHERE 조건 (NOT IN)
    emailSentTime DESC  -- ② ORDER BY 조건
);

-- 예상 효과: Using filesort → Using index
-- 정렬 시간: 10ms → 1ms (90% 단축)
-- 응답시간: 10ms → 1ms
```

#### 인덱스 #3: `email` 테이블 (선택, 중요 메일용)

```sql
-- ✅ 중요 메일 조회 최적화
CREATE INDEX idx_email_favorite 
ON email(
    favoriteStatus,
    emailStatus,
    emailSentTime DESC
);

-- 예상 효과: 중요 메일 조회 시 사용
-- 응답시간: 18ms → 5ms (72% 단축)
```

---

### 🔥 우선순위 2: 쿼리 재작성 (중기, 1일)

#### 문제: NOT IN은 인덱스 최적화가 어려움

```sql
-- ❌ Before: NOT IN (인덱스 사용 어려움)
WHERE e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')

-- ✅ After: IN 또는 = (인덱스 사용 가능)
WHERE e.emailStatus IN ('SENT', 'BOUNCE', 'FAILED')
-- 또는
WHERE e.emailStatus = 'SENT'
```

#### 수정된 JPQL

```java
@Query("SELECT r FROM EmailRecipient r " +
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +
       "AND r.emailRecipientType IN :emailRecipientType " +
       "AND r.email.emailStatus = 'SENT' " +  // ✅ 수정: NOT IN → =
       "AND r.deleted = false " +              // ✅ 수정: IS NULL 제거
       "ORDER BY r.email.emailSentTime DESC")
Page<EmailRecipient> findInboxExcludingTrash(...);
```

---

### ⚠️ 우선순위 3: N+1 쿼리 방지 (단기, 1일)

#### 문제: EmailRecipient 조회 후 Email 필드 접근 시 추가 쿼리

```java
// ❌ Before: N+1 쿼리 발생
recipientPage.getContent().forEach(recipient -> {
    Email email = recipient.getEmail();  // ⚠️ Lazy Loading으로 추가 쿼리!
    String title = email.getEmailTitle();
    String sender = email.getSenderEmail();
});
```

#### 해결: Fetch Join

```java
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email e " +  // ✅ Fetch Join 추가!
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +
       "AND r.emailRecipientType IN :emailRecipientType " +
       "AND e.emailStatus = 'SENT' " +
       "AND r.deleted = false " +
       "ORDER BY e.emailSentTime DESC")
Page<EmailRecipient> findInboxWithEmail(...);
```

**예상 효과**:
- 쿼리 수: 21개 (1 + 20) → **1개** (95% 감소)
- 응답시간: 5ms → **2ms** (60% 단축)

---

### 💡 우선순위 4: DTO Projection (중기, 2일)

#### 문제: 불필요한 필드 조회

```java
// ❌ Before: 전체 엔티티 조회 (emailContent, emailBounceReason 등 불필요)
SELECT r FROM EmailRecipient r LEFT JOIN FETCH r.email e

// ✅ After: 필요한 필드만 조회
@Query("SELECT new com.goodee.coreconnect.email.dto.EmailInboxDTO(" +
       "r.emailRecipientId, " +
       "r.emailReadYn, " +
       "e.emailId, " +
       "e.emailTitle, " +    // 제목만
       "e.senderEmail, " +   // 발신자 이메일만
       "e.emailSentTime, " + // 발송 시간만
       "e.favoriteStatus) " +
       "FROM EmailRecipient r " +
       "LEFT JOIN r.email e " +
       "WHERE ... ")
Page<EmailInboxDTO> findInboxDTOs(...);
```

**예상 효과**:
- 응답 크기: 1,200 bytes → **400 bytes** (67% 감소)
- 네트워크 전송 시간: 0.17ms → **0.06ms** (65% 단축)

---

## 📊 최종 효과 예측

| 단계 | 개선 항목 | Before | After | 개선율 |
|------|----------|--------|-------|--------|
| **1** | 복합 인덱스 추가 | 25-30ms | 8-10ms | ⬇️ 67% |
| **2** | 쿼리 재작성 (NOT IN → =) | 8-10ms | 5-7ms | ⬇️ 30% |
| **3** | N+1 방지 (Fetch Join) | 5-7ms | 3-4ms | ⬇️ 40% |
| **4** | DTO Projection | 3-4ms | **2-3ms** | ⬇️ 33% |
| **총합** | - | 25-30ms | **2-3ms** | **⬇️ 88-90%** |

---

## 🛠️ 실행 단계 (체크리스트)

### Step 1: 현재 상태 확인 (10분)

```sql
-- 1.1 EXPLAIN 실행
EXPLAIN SELECT 
    r.emailRecipientId,
    r.emailRecipientAddress,
    r.emailRecipientType,
    r.emailReadYn,
    r.deleted,
    r.email_id,
    e.emailId,
    e.emailTitle,
    e.senderEmail,
    e.emailSentTime,
    e.emailStatus
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.emailId
WHERE 
    r.emailRecipientAddress = 'admin@coreconnect.io.kr'
    AND r.emailRecipientType IN ('TO', 'CC', 'BCC')
    AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
    AND (r.deleted IS NULL OR r.deleted = FALSE)
ORDER BY 
    e.emailSentTime DESC
LIMIT 20;

-- [ ] type: ALL 확인 (Full Table Scan)
-- [ ] rows: 50,000+ 확인
-- [ ] Extra: Using filesort 확인

-- 1.2 기존 인덱스 확인
SHOW INDEX FROM email_recipient;
SHOW INDEX FROM email;

-- [ ] emailRecipientAddress 인덱스 없음 확인
-- [ ] emailSentTime 인덱스 없음 확인
```

---

### Step 2: 인덱스 추가 (30분)

```sql
-- 2.1 email_recipient 복합 인덱스 (최우선!)
CREATE INDEX idx_email_recipient_lookup 
ON email_recipient(
    emailRecipientAddress(100),  -- VARCHAR 길이 지정
    emailRecipientType,
    deleted,
    email_id
) 
ALGORITHM=INPLACE, LOCK=NONE;  -- 온라인 DDL (서비스 중단 없음)

-- [ ] 인덱스 생성 완료 (3-5분 소요)

-- 2.2 email 정렬 인덱스
CREATE INDEX idx_email_status_sent_time 
ON email(
    emailStatus,
    emailSentTime DESC
)
ALGORITHM=INPLACE, LOCK=NONE;

-- [ ] 인덱스 생성 완료 (2-3분 소요)

-- 2.3 email 중요 메일 인덱스 (선택)
CREATE INDEX idx_email_favorite 
ON email(
    favoriteStatus,
    emailStatus,
    emailSentTime DESC
)
ALGORITHM=INPLACE, LOCK=NONE;

-- [ ] 인덱스 생성 완료 (2-3분 소요)
```

---

### Step 3: 효과 검증 (30분)

```sql
-- 3.1 EXPLAIN 다시 실행
EXPLAIN SELECT 
    r.emailRecipientId,
    r.emailRecipientAddress,
    r.emailRecipientType,
    r.emailReadYn,
    r.deleted,
    r.email_id,
    e.emailId,
    e.emailTitle,
    e.senderEmail,
    e.emailSentTime,
    e.emailStatus
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.emailId
WHERE 
    r.emailRecipientAddress = 'admin@coreconnect.io.kr'
    AND r.emailRecipientType IN ('TO', 'CC', 'BCC')
    AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
    AND (r.deleted IS NULL OR r.deleted = FALSE)
ORDER BY 
    e.emailSentTime DESC
LIMIT 20;

-- 검증 체크리스트:
-- [ ] type: ref 또는 range (Index Scan)
-- [ ] possible_keys: idx_email_recipient_lookup
-- [ ] key: idx_email_recipient_lookup (실제 사용)
-- [ ] rows: 100-500 (50,000 → 100-500)
-- [ ] Extra: Using index condition (filesort 제거됨!)

-- 3.2 실제 쿼리 성능 측정
SET profiling = 1;

SELECT 
    r.emailRecipientId,
    r.emailRecipientAddress,
    r.emailRecipientType,
    r.emailReadYn,
    r.deleted,
    r.email_id,
    e.emailId,
    e.emailTitle,
    e.senderEmail,
    e.emailSentTime,
    e.emailStatus
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.emailId
WHERE 
    r.emailRecipientAddress = 'admin@coreconnect.io.kr'
    AND r.emailRecipientType IN ('TO', 'CC', 'BCC')
    AND e.emailStatus NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
    AND (r.deleted IS NULL OR r.deleted = FALSE)
ORDER BY 
    e.emailSentTime DESC
LIMIT 20;

SHOW PROFILES;

-- 목표:
-- [ ] Duration: 0.005 ~ 0.010초 (5-10ms)
-- [ ] Before 대비 67% 이상 단축 확인

-- 3.3 인덱스 크기 확인
SELECT 
    table_name,
    index_name,
    ROUND(stat_value * @@innodb_page_size / 1024 / 1024, 2) AS size_mb
FROM mysql.innodb_index_stats
WHERE table_name IN ('email_recipient', 'email')
  AND database_name = 'db_coreconnect';

-- [ ] 인덱스 크기가 테이블 크기의 50% 이내인지 확인
```

---

### Step 4: 애플리케이션 테스트 (1시간)

```bash
# 4.1 Spring Boot 애플리케이션 재시작
docker restart boot-container

# 4.2 로그 확인 (Hibernate SQL)
docker logs -f boot-container | grep "SELECT"

# [ ] 인덱스 사용 확인 (Using index 로그)
# [ ] 응답 시간 단축 확인

# 4.3 k6 재테스트
k6 run test.js

# Before:
# - 이메일 받은편지함: 25-30ms

# After (목표):
# - 이메일 받은편지함: 8-10ms (67% 단축)

# [ ] 목표 달성 확인
```

---

## 📋 완료 체크리스트

- [ ] EXPLAIN으로 Full Table Scan 확인
- [ ] 복합 인덱스 3개 추가 완료
- [ ] EXPLAIN으로 Index Scan 확인
- [ ] 응답시간 67% 단축 확인 (25-30ms → 8-10ms)
- [ ] k6 재테스트로 개선 검증
- [ ] 프로덕션 배포 완료
- [ ] 모니터링 대시보드에서 개선 확인

---

## ⚠️ 주의사항

### 인덱스 추가 시 고려사항

1. **디스크 용량**:
   - 복합 인덱스 크기: 테이블 크기의 20-30%
   - email_recipient 테이블 50MB → 인덱스 15MB
   - email 테이블 200MB → 인덱스 60MB
   - **총 75MB 추가 필요**

2. **쓰기 성능 영향**:
   - INSERT/UPDATE 시 인덱스도 함께 업데이트
   - 예상 성능 저하: **5-10%** (허용 가능)
   - 읽기:쓰기 비율이 9:1이므로 전체적으로 이득

3. **인덱스 유지보수**:
   - 정기적 통계 업데이트: `ANALYZE TABLE email_recipient;`
   - 조각화 모니터링: `OPTIMIZE TABLE` (필요시)

4. **테이블 락**:
   - `ALGORITHM=INPLACE, LOCK=NONE` 사용 → 온라인 DDL
   - MySQL 8.0에서 지원
   - 서비스 중단 없이 인덱스 추가 가능

---

## 💡 장기 개선 방안

### 1. Full-Text Search (6개월)

```sql
-- TEXT 컬럼 검색 최적화
CREATE FULLTEXT INDEX idx_email_fulltext 
ON email(emailTitle, emailContent);

-- 사용:
SELECT * FROM email
WHERE MATCH(emailTitle, emailContent) AGAINST('키워드' IN NATURAL LANGUAGE MODE);

-- 예상 효과: LIKE '%keyword%' 대비 10배 빠름
```

### 2. Elasticsearch 도입 (1년)

- 대용량 TEXT 검색 최적화
- 자동완성, 형태소 분석 지원
- 월 비용: $50-100

### 3. Read Replica (3개월)

- 읽기 전용 DB 분리
- 이메일 조회는 Slave DB
- 이메일 발송은 Master DB

---

**작성일**: 2025-12-17  
**목적**: 이메일 받은편지함 쿼리 최적화 상세 분석  
**버전**: 1.0  
**예상 개선 효과**: 25-30ms → 2-3ms (88-90% 단축)

