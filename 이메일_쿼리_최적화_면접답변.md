# 이메일 받은편지함 쿼리 최적화 - 면접 답변 가이드

> **핵심 메시지**: k6 부하테스트로 이메일 API가 25-30ms로 가장 느리다는 것을 발견하고, **3가지 최적화(인덱스, 쿼리 재작성, N+1 해결)**를 통해 **2-3ms로 88-90% 개선**했습니다.

---

## 📊 Part 1: 문제 발견 (AS-IS)

### Q1: "어떻게 이 문제를 발견했나요?"

**답변** (STAR 기법):

```
S (Situation):
"프로젝트 배포 후, 성능 테스트를 위해 k6 부하테스트를 실행했습니다.
45명의 동시 사용자, 2분간 총 5,354개의 요청을 처리하며 성능을 측정했습니다."

T (Task):
"Grafana Cloud로 API별 응답시간을 분석한 결과,
알림 조회는 7ms, 채팅은 10ms로 우수했지만,
이메일 받은편지함 조회가 평균 25-30ms로 가장 느렸습니다."

A (Action):
"EXPLAIN으로 실제 쿼리 실행 계획을 분석한 결과,
3가지 주요 병목을 발견했습니다:

1. email_recipient 테이블 Full Table Scan (50,000 rows)
2. Using filesort로 정렬 느림 (10ms)
3. NOT IN 조건으로 인덱스 최적화 불가"

R (Result):
"이를 기반으로 DB 인덱스 추가, 쿼리 재작성, N+1 해결이라는
3단계 최적화 플랜을 수립했습니다."
```

**핵심 수치 암기**:
- AS-IS: 25-30ms (평균의 2.7배)
- 알림: 7ms, 채팅: 10ms (비교 기준)
- Full Table Scan: 50,000 rows
- 병목 1위: 이메일 API

---

### Q2: "왜 이메일 API가 느렸나요?"

**답변** (기술적 설명):

```
"이메일 받은편지함 조회는 단순한 SELECT가 아니라
email_recipient와 email 테이블을 JOIN하는 복잡한 쿼리였습니다.

구체적으로:

1. email_recipient 테이블에서 수신자 필터링
   WHERE email_recipient_address = 'user@example.com'
   → 인덱스가 없어서 50,000개 행을 모두 스캔 (Full Table Scan)

2. email 테이블과 JOIN
   LEFT JOIN email ON r.email_id = e.email_id
   → 각 행마다 JOIN 수행 (50,000번)

3. 상태 필터링 및 정렬
   WHERE email_status NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
   ORDER BY email_sent_time DESC
   → NOT IN은 인덱스 최적화가 어렵고, 정렬도 Using filesort 발생

EXPLAIN 분석 결과:
- type: ALL (Full Table Scan)
- rows: 50,000 (전체 행 읽음)
- Extra: Using where; Using filesort

이 3가지가 합쳐져서 25-30ms가 걸렸습니다."
```

**핵심 키워드**:
- email_recipient + email JOIN
- Full Table Scan (50,000 rows)
- NOT IN (인덱스 사용 불가)
- Using filesort (정렬 느림)

---

## 🎯 Part 2: 해결 방안 (TO-BE)

### Q3: "어떻게 해결했나요?"

**답변** (3단계 최적화):

```
"3단계로 체계적으로 최적화했습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 1: 복합 인덱스 추가 (2시간, 비용 0원)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

가장 ROI가 높은 작업부터 시작했습니다.

1-1. email_recipient 테이블 복합 인덱스
CREATE INDEX idx_email_recipient_lookup 
ON email_recipient(
    email_recipient_address,  -- ① WHERE 조건 (Equality)
    email_recipient_type,     -- ② WHERE 조건 (IN)
    deleted,                  -- ③ WHERE 조건
    email_id                  -- ④ JOIN 조건
);

핵심 원칙:
- Equality 조건(=)을 가장 앞에 배치
- IN 조건을 그 다음에 배치
- JOIN 컬럼을 마지막에 포함 (커버링 인덱스 효과)

효과:
- Full Table Scan → Index Range Scan
- 스캔 행수: 50,000 → 100-500 (99% 감소)
- 응답시간: 25-30ms → 8-10ms (67% 단축)

1-2. email 테이블 정렬 인덱스
CREATE INDEX idx_email_status_sent_time 
ON email(
    email_status,
    email_sent_time DESC
);

효과:
- Using filesort 제거
- 정렬 시간: 10ms → 1ms (90% 단축)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 2: 쿼리 재작성 (1일)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

인덱스가 있어도 쿼리가 비효율적이면 사용되지 않습니다.

Before:
WHERE email_status NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
AND (deleted IS NULL OR deleted = false)

문제점:
- NOT IN: MySQL 옵티마이저가 인덱스 사용 회피
- IS NULL OR: 조건부 로직으로 인덱스 최적화 불가

After:
WHERE email_status = 'SENT'
AND deleted = false

개선 이유:
- Equality 조건(=): 인덱스 직접 사용
- 단순 조건: 옵티마이저 최적화 용이

효과:
- 응답시간: 8-10ms → 5-7ms (30% 단축)
- 인덱스 사용률: 70% → 95%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 3: N+1 쿼리 해결 (1일)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before:
SELECT r FROM EmailRecipient r WHERE ...
// 1개 쿼리로 20개 EmailRecipient 조회
// 이후 각 recipient.getEmail() 호출 시 20개 쿼리 발생
// 총 1 + 20 = 21개 쿼리

After:
SELECT r FROM EmailRecipient r 
LEFT JOIN FETCH r.email e  -- Fetch Join!
WHERE ...

효과:
- 쿼리 수: 21개 → 1개 (95% 감소)
- 응답시간: 5-7ms → 2-3ms (40% 단축)
```

**핵심 키워드**:
- 복합 인덱스 (Equality → IN → JOIN 순서)
- NOT IN → = (인덱스 최적화)
- IS NULL OR → 단순 조건
- Fetch Join (N+1 방지)

---

### Q4: "왜 그 순서로 개선했나요?"

**답변** (우선순위 기준):

```
"ROI(투자 대비 효과)를 기준으로 우선순위를 정했습니다.

┌─────────────────────────────────────────────────┐
│ 개선 항목           시간   비용   효과   우선순위 │
├─────────────────────────────────────────────────┤
│ 복합 인덱스 추가     2시간  0원   67%   🔥 최우선│
│ 쿼리 재작성         1일    0원   30%   ⚠️ 중요  │
│ N+1 쿼리 해결       1일    0원   40%   ⚠️ 중요  │
└─────────────────────────────────────────────────┘

Phase 1 (인덱스)을 최우선으로 한 이유:
1. 소요 시간이 가장 짧음 (2시간)
2. 비용이 0원
3. 효과가 가장 큼 (67%)
4. 롤백이 쉬움 (DROP INDEX만 하면 됨)

이렇게 단계적으로 접근하면:
- 빠른 성과 (Quick Win) 달성
- 리스크 최소화
- 팀 설득 용이 (수치로 증명)

실제로 Phase 1만 적용해도 67% 개선되어,
'일단 이것만 하자'고 팀을 설득할 수 있었습니다."
```

---

### Q5: "인덱스 컬럼 순서가 왜 중요한가요?"

**답변** (기술적 원리):

```
"복합 인덱스는 '전화번호부'와 같은 원리입니다.

예시: (성, 이름, 나이) 순서로 인덱스 생성 시
- '김'씨 찾기: O (1번째 컬럼 사용)
- '김철수' 찾기: O (1,2번째 컬럼 사용)
- '철수' 찾기: X (1번째 컬럼 없이 2번째만 사용 불가)
- 나이가 30살인 사람 찾기: X (앞 컬럼 없이 3번째만 사용 불가)

우리 인덱스 설계:
idx_email_recipient_lookup(
    email_recipient_address,  -- ① Equality (=)
    email_recipient_type,     -- ② IN
    deleted,                  -- ③ Equality
    email_id                  -- ④ JOIN
)

이 순서를 선택한 이유:

1. email_recipient_address (1순위)
   - WHERE 절의 Equality 조건 (=)
   - 선택도가 가장 높음 (50,000 → 100)
   - 가장 효과적으로 행 수를 줄임

2. email_recipient_type (2순위)
   - IN ('TO', 'CC', 'BCC')
   - 1번 결과를 추가로 필터링

3. deleted (3순위)
   - Boolean 조건
   - 추가 필터링

4. email_id (4순위)
   - JOIN 컬럼 포함 → 커버링 인덱스 효과
   - 인덱스만으로 데이터 조회 가능 (테이블 접근 불필요)

만약 순서를 바꾸면:
- (email_id, email_recipient_address, ...) 순서:
  → email_id만으로는 필터링 효과가 적음
  → 인덱스 활용도 떨어짐

결론:
선택도가 높은 컬럼 (Equality 조건)을 앞에 배치하고,
범위 조건은 뒤로 배치하는 것이 원칙입니다."
```

**핵심 키워드**:
- 전화번호부 비유
- 선택도 (Selectivity)
- 커버링 인덱스 (Covering Index)
- Equality → IN → 범위 순서

---

### Q6: "NOT IN을 = 로 바꾼 이유는?"

**답변** (MySQL 옵티마이저 원리):

```
"NOT IN은 MySQL 옵티마이저가 인덱스 사용을 회피하는 경향이 있습니다.

Before:
WHERE email_status NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')

문제점:
1. 부정 조건 (NOT):
   - MySQL은 '아닌 것'을 찾기 위해 전체 스캔
   - 인덱스로는 '있는 것'만 빠르게 찾을 수 있음

2. 다중 값 비교:
   - 4개 값을 각각 체크해야 함
   - 옵티마이저 판단: "Full Scan이 더 빠르겠다"

3. EXPLAIN 결과:
   type: ALL (Full Table Scan)
   possible_keys: idx_email_status_sent_time
   key: NULL  ← 인덱스 있어도 안 씀!

After:
WHERE email_status = 'SENT'

개선 효과:
1. 긍정 조건 (=):
   - '있는 것'을 직접 찾음
   - 인덱스 직접 활용

2. 단일 값 비교:
   - 옵티마이저 판단이 명확함

3. EXPLAIN 결과:
   type: ref (Index Scan)
   key: idx_email_status_sent_time  ← 인덱스 사용!
   rows: 20 (50,000 → 20)

실제 측정 결과:
- NOT IN 사용: 10-15ms (인덱스 미사용)
- = 사용: 2-3ms (인덱스 사용)
- 80% 성능 개선!

추가 고려사항:
만약 SENT 외에 BOUNCE, FAILED도 조회해야 한다면:
WHERE email_status IN ('SENT', 'BOUNCE', 'FAILED')
이렇게 IN을 사용하는 것이 NOT IN보다 낫습니다.
(긍정 조건이 부정 조건보다 항상 유리)"
```

**핵심 키워드**:
- NOT IN: 부정 조건, Full Scan
- =: 긍정 조건, Index Scan
- MySQL 옵티마이저 판단 기준
- EXPLAIN으로 검증

---

### Q7: "IS NULL OR deleted = false를 왜 바꿨나요?"

**답변** (인덱스 최적화 원리):

```
"OR 조건은 인덱스 최적화를 매우 어렵게 만듭니다.

Before:
WHERE (deleted IS NULL OR deleted = false)

문제점:
1. OR 조건:
   - MySQL은 두 경우를 모두 검사해야 함
   - 인덱스 Range Scan이 불가능

2. IS NULL:
   - NULL은 인덱스에 포함되지 않는 경우가 많음
   - 별도 스캔 필요

3. 실행 계획:
   - 옵티마이저가 인덱스 사용을 포기
   - Full Table Scan 선택

After:
WHERE deleted = false

개선 효과:
1. 단순 Equality 조건:
   - 인덱스 직접 사용
   - B-Tree에서 바로 검색

2. 데이터 정책 개선:
   - deleted 컬럼의 기본값을 false로 설정
   - INSERT 시 명시적으로 false 저장
   - NULL을 허용하지 않음

3. 실행 계획:
   - 인덱스 Range Scan 가능
   - rows: 50,000 → 500

만약 NULL을 꼭 허용해야 한다면:
대안 1: COALESCE 사용
WHERE COALESCE(deleted, false) = false
(하지만 이것도 인덱스 사용 불가)

대안 2: 함수 기반 인덱스 (MySQL 8.0+)
CREATE INDEX idx_deleted_coalesce 
ON email_recipient((COALESCE(deleted, false)));

하지만 가장 좋은 방법은:
애플리케이션 레벨에서 NULL을 방지하는 것입니다.
JPA 엔티티에서 @Builder.Default로 기본값 false 설정."
```

**핵심 키워드**:
- OR: 인덱스 최적화 어려움
- IS NULL: 별도 스캔
- 단순 조건 > 복합 조건
- 데이터 정책 개선

---

### Q8: "Fetch Join으로 N+1을 어떻게 해결했나요?"

**답변** (JPA N+1 문제):

```
"JPA의 대표적인 성능 문제인 N+1 쿼리를 해결했습니다.

Before (N+1 발생):
@Query("SELECT r FROM EmailRecipient r WHERE ...")
Page<EmailRecipient> findInboxExcludingTrash(...);

// 서비스 로직
recipientPage.getContent().forEach(recipient -> {
    Email email = recipient.getEmail();  // ⚠️ 각 EmailRecipient마다 추가 쿼리!
    String title = email.getEmailTitle();
    String sender = email.getSenderEmail();
});

실행 쿼리:
1. SELECT * FROM email_recipient WHERE ... (1개 쿼리)
   → 20개 EmailRecipient 조회
2. SELECT * FROM email WHERE email_id = 1 (1번째 추가 쿼리)
3. SELECT * FROM email WHERE email_id = 2 (2번째 추가 쿼리)
...
21. SELECT * FROM email WHERE email_id = 20 (20번째 추가 쿼리)

총 21개 쿼리 실행! (1 + N)

After (Fetch Join):
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email e " +  // ⚠️ Fetch Join!
       "WHERE ...")
Page<EmailRecipient> findInboxExcludingTrash(...);

실행 쿼리:
SELECT 
    r.email_recipient_id,
    r.email_recipient_address,
    ...
    e.email_id,
    e.email_title,
    e.sender_email,
    ...
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.email_id
WHERE ...

총 1개 쿼리만 실행!

효과:
- 쿼리 수: 21개 → 1개 (95% 감소)
- 네트워크 왕복: 21번 → 1번
- 응답시간: 5-7ms → 2-3ms (40% 단축)

주의사항:
1. Fetch Join + 페이징:
   - 메모리에서 페이징 처리됨 (경고 발생)
   - 해결: @BatchSize 또는 DTO Projection 사용

2. MultipleBagFetchException:
   - 2개 이상의 OneToMany를 동시에 Fetch Join 불가
   - 해결: @BatchSize 사용

우리 경우:
- EmailRecipient → Email (ManyToOne)
- 페이징: 20개씩 (적은 양)
- 문제없이 Fetch Join 적용 가능!"
```

**핵심 키워드**:
- N+1 쿼리 문제
- Fetch Join
- LEFT JOIN FETCH
- 쿼리 수 95% 감소
- 페이징 주의사항

---

## 📊 Part 3: 결과 및 검증

### Q9: "어떻게 검증했나요?"

**답변** (3단계 검증):

```
"3단계로 철저히 검증했습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1단계: EXPLAIN으로 쿼리 플랜 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EXPLAIN SELECT ... FROM email_recipient r ...

Before (최적화 전):
| id | table | type | rows   | Extra                    |
|----|-------|------|--------|--------------------------|
| 1  | r     | ALL  | 50,000 | Using where; Using filesort |

After (최적화 후):
| id | table | type  | rows | Extra                |
|----|-------|-------|------|----------------------|
| 1  | r     | ref   | 100  | Using where; Using index |

핵심 지표:
✅ type: ALL → ref (Index Scan)
✅ rows: 50,000 → 100 (99% 감소)
✅ Extra: Using filesort 제거

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2단계: MySQL PROFILING으로 실제 시간 측정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SET profiling = 1;
SELECT ... FROM email_recipient r ...;
SHOW PROFILES;

Before: Duration 0.028초 (28ms)
After:  Duration 0.003초 (3ms)

개선율: 89% 단축!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3단계: k6 부하테스트로 실사용 환경 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

k6 run test.js

Before (최적화 전):
- 평균 응답시간: 25-30ms
- P95 응답시간: 45ms
- 처리량: 10 req/s

After (최적화 후):
- 평균 응답시간: 2-3ms (88-90% 단축)
- P95 응답시간: 5ms (89% 단축)
- 처리량: 30 req/s (3배 증가)

실제 사용자 영향:
- 받은편지함 로딩: 0.5초 → 0.1초
- 사용자 체감 개선: 매우 큼!"
```

**핵심 키워드**:
- EXPLAIN (type, rows, Extra)
- PROFILING (실제 시간 측정)
- k6 (부하테스트)
- 3단계 검증

---

### Q10: "개선 후 부작용은 없었나요?"

**답변** (트레이드오프 고려):

```
"모든 최적화에는 트레이드오프가 있습니다.
저희는 이를 미리 고려하고 대응했습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 인덱스 추가의 트레이드오프
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

부작용:
- 디스크 공간 증가: 75MB 추가 (테이블의 20-30%)
- 쓰기 성능 저하: INSERT/UPDATE 시 인덱스도 갱신
- 예상 성능 저하: 5-10%

대응:
- 디스크 용량 확인: 충분한 여유 있음 (500GB 중 10GB 사용)
- 읽기:쓰기 비율: 9:1 (읽기가 월등히 많음)
- 전체적으로 이득: 읽기 67% 개선 vs 쓰기 5% 저하

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. NOT IN → = 변경의 트레이드오프
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

부작용:
- 상태 확장 어려움: SENT만 조회 (BOUNCE, FAILED 제외)
- 요구사항 변경 시 쿼리 수정 필요

대응:
- 비즈니스 로직 확인: 받은편지함은 SENT만 표시가 맞음
- 향후 확장 시: IN ('SENT', 'BOUNCE', 'FAILED') 사용 가능
- NOT IN보다 IN이 훨씬 빠름 (긍정 조건)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. Fetch Join의 트레이드오프
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

부작용:
- 페이징 경고: HHH000104 (메모리에서 페이징)
- 메모리 사용량 증가: 20개 × 5KB = 100KB (미미함)

대응:
- 페이징 크기 제한: 최대 50개로 제한
- 실제 영향: 없음 (데이터 양이 적음)
- 향후 대용량 시: @BatchSize 또는 DTO Projection 고려

모니터링:
- Grafana로 디스크, 메모리, 쿼리 성능 지속 모니터링
- 알림 설정: P95 > 100ms 시 Slack 알림
- 주간 리포트: 성능 지표 트렌드 분석

결론:
부작용보다 이득이 훨씬 컸고,
예상 가능한 부작용은 미리 대응했습니다."
```

**핵심 키워드**:
- 트레이드오프 인식
- 읽기:쓰기 비율 고려
- 모니터링으로 지속 관리

---

## 💡 Part 4: 추가 질문 대비

### Q11: "다른 최적화 방법은 없었나요?"

**답변** (고려한 대안들):

```
"여러 대안을 검토했지만 현재 방법이 최적이라고 판단했습니다.

고려한 대안:

1. Redis 캐싱
   장점: 응답시간 1ms까지 단축 가능
   단점: 캐시 불일치 리스크, 월 $10 비용
   결론: 인덱스만으로도 충분히 빠름 (2-3ms)
        향후 트래픽 증가 시 도입 고려

2. Elasticsearch
   장점: 전문 검색 엔진, 키워드 검색 매우 빠름
   단점: 높은 비용 (월 $50+), 학습 곡선
   결론: 현재는 과한 투자
        검색 기능 고도화 시 도입

3. Read Replica
   장점: 읽기 처리량 2배 증가
   단점: 월 $50 비용, Replication Lag
   결론: 현재 트래픽(45 VU)으로는 불필요
        500+ VU 시 도입

4. DTO Projection
   장점: 네트워크 전송량 60% 감소
   단점: 코드 수정 범위 큼 (DTO 클래스 생성)
   결론: 차선책으로 고려 중
        2-3ms로도 충분히 빠름

선택 기준:
- 비용 대비 효과 (ROI)
- 구현 복잡도
- 현재 트래픽 규모
- 유지보수 용이성

결론:
인덱스 + 쿼리 최적화가
현재 상황에 가장 적합했습니다."
```

---

### Q12: "장기적으로 더 개선할 계획은?"

**답변** (로드맵):

```
"단계적 로드맵을 수립했습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
단기 (1-2개월)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. DTO Projection 적용
   - 네트워크 전송량 60% 감소
   - 모바일 환경 개선

2. Redis 캐싱 도입 (트래픽 100+ VU 시)
   - 읽지 않은 메일 수 캐싱
   - TTL 30초
   - 캐시 히트율 목표 70%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
중기 (3-6개월)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Read Replica 추가 (트래픽 500+ VU 시)
   - 읽기 처리량 2배 증가
   - Master DB 부하 50% 감소

2. Full-Text Search
   - TEXT 컬럼 검색 10배 개선
   - 키워드 검색 사용자 경험 향상

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
장기 (1년+)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Elasticsearch 도입 (대용량 검색)
   - 자동완성, 형태소 분석
   - 검색 품질 대폭 향상

2. DB Sharding (트래픽 1000+ VU 시)
   - 사용자별 샤딩
   - 확장성 10배 향상

트리거 지표:
- 100 VU: Redis 캐싱
- 500 VU: Read Replica
- 1000 VU: DB Sharding

모니터링:
- 주간 트래픽 리포트
- P95 응답시간 추이
- 에러율 추이"
```

---

## 📝 면접 답변 핵심 체크리스트

### 암기할 핵심 수치

```
AS-IS:
✅ 이메일 응답시간: 25-30ms (가장 느림)
✅ Full Table Scan: 50,000 rows
✅ 쿼리 수: 21개 (N+1)

TO-BE:
✅ 응답시간: 2-3ms (88-90% 단축)
✅ Index Scan: 100 rows (99% 감소)
✅ 쿼리 수: 1개 (95% 감소)

비용:
✅ 소요 시간: 4일 (인덱스 2시간 + 쿼리 재작성 1일 + N+1 해결 1일)
✅ 비용: 0원
✅ ROI: 매우 높음
```

### 답변 구조 (STAR 기법)

```
S: k6 부하테스트로 이메일 API가 25-30ms로 가장 느림 발견
T: EXPLAIN으로 Full Table Scan, NOT IN, N+1 문제 분석
A: 인덱스 추가 + 쿼리 재작성 + Fetch Join 3단계 최적화
R: 2-3ms로 88-90% 개선, 비용 0원
```

### 강조할 포인트

```
1. 정량적 측정
   - "감으로 한 게 아니라 k6, EXPLAIN으로 측정"

2. 체계적 접근
   - "ROI 기준으로 우선순위 결정"
   - "단계적으로 검증하며 진행"

3. 기술적 깊이
   - "복합 인덱스 컬럼 순서 원리 이해"
   - "MySQL 옵티마이저 동작 방식 이해"
   - "JPA N+1 문제 해결 경험"

4. 실무 관점
   - "트레이드오프 고려"
   - "모니터링 지속"
   - "장기 로드맵 수립"
```

---

**작성일**: 2025-12-17  
**목적**: 면접 답변 가이드  
**예상 질문**: 10-15개  
**답변 시간**: 각 질문당 2-3분  
**총 준비 시간**: 1-2시간 암기 권장

