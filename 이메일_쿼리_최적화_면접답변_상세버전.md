# 이메일 쿼리 최적화 - 면접 답변 상세 버전

> **면접관에게 보여줄 핵심**: 단순히 "인덱스 추가했습니다"가 아니라, **왜 그 인덱스를 그 순서로 만들었는지**, **어떤 원리로 동작하는지**를 체계적으로 설명할 수 있어야 합니다.

---

## 📊 질문: "어떻게 해결했나요?"

### 전체 개요 (30초 버전)

```
"3단계로 체계적으로 최적화했습니다.

Phase 1은 DB 인덱스 추가로 Full Table Scan을 Index Scan으로 개선했고,
Phase 2는 쿼리 재작성으로 인덱스 활용률을 높였으며,
Phase 3는 Fetch Join으로 N+1 쿼리 문제를 해결했습니다.

각 단계마다 EXPLAIN과 k6 테스트로 검증하며 진행했고,
최종적으로 25-30ms에서 2-3ms로 88-90% 개선했습니다."
```

---

## 🔧 Phase 1: 복합 인덱스 추가 (상세)

### 1-1. 왜 인덱스를 가장 먼저 했나요?

**답변**:
```
"ROI(투자 대비 효과) 관점에서 판단했습니다.

인덱스 추가는:
1. 소요 시간이 짧습니다 (2시간)
2. 비용이 0원입니다
3. 효과가 가장 큽니다 (67% 개선)
4. 롤백이 쉽습니다 (DROP INDEX만 하면 됨)

반면 코드 리팩토링은:
- 시간이 오래 걸리고 (1-2일)
- 테스트 범위가 넓으며
- 버그 리스크가 있고
- 롤백이 복잡합니다

그래서 '빠른 성과(Quick Win)'를 먼저 얻고,
그 결과로 팀을 설득한 후 2단계를 진행하는 전략을 선택했습니다."
```

---

### 1-2. email_recipient 테이블 복합 인덱스

#### 인덱스 설계

```sql
CREATE INDEX idx_email_recipient_lookup 
ON email_recipient(
    email_recipient_address,  -- ① WHERE 조건 (Equality)
    email_recipient_type,     -- ② WHERE 조건 (IN)
    deleted,                  -- ③ WHERE 조건 (Boolean)
    email_id                  -- ④ JOIN 조건 (커버링)
);
```

#### 왜 이 컬럼들을 선택했나요?

**답변**:
```
"실제 쿼리를 분석해서 WHERE 절과 JOIN 절에 사용되는 컬럼들을 선택했습니다.

우리 쿼리:
SELECT r FROM EmailRecipient r 
LEFT JOIN Email e ON r.email_id = e.email_id
WHERE r.email_recipient_address = 'admin@coreconnect.io.kr'  -- ①
  AND r.email_recipient_type IN ('TO', 'CC', 'BCC')          -- ②
  AND r.deleted = false                                       -- ③
ORDER BY e.email_sent_time DESC

이 쿼리에서:
① email_recipient_address: 필수 필터 (항상 사용)
② email_recipient_type: 추가 필터 (IN 조건)
③ deleted: 삭제 여부 필터
④ email_id: JOIN에 사용 (커버링 인덱스 효과)

이 4개 컬럼이 쿼리 성능에 가장 큰 영향을 주므로
모두 인덱스에 포함했습니다."
```

---

#### 왜 이 순서로 배치했나요? (가장 중요!)

**답변 (전화번호부 비유)**:
```
"복합 인덱스는 '전화번호부'와 같습니다.

전화번호부 예시:
- (성, 이름, 나이) 순서로 정렬되어 있다면:
  ✅ '김'씨 찾기: 가능 (1번째 컬럼 사용)
  ✅ '김철수' 찾기: 가능 (1,2번째 컬럼 사용)
  ❌ '철수' 찾기: 불가능 (1번째 없이 2번째만 사용 불가)
  ❌ 30살 찾기: 불가능 (앞 컬럼 없이 3번째만 사용 불가)

이처럼 복합 인덱스는 왼쪽부터 순서대로만 사용할 수 있습니다.

그래서 인덱스 컬럼 순서 원칙:
1. Equality 조건(=)을 가장 앞에
2. IN 조건을 그 다음에
3. 범위 조건(>, <, BETWEEN)을 마지막에

우리 경우:
① email_recipient_address = '...' (Equality) → 1순위
   - 50,000개 중 100개로 줄임 (99.8% 감소)
   
② email_recipient_type IN (...) (IN) → 2순위
   - 100개 중 80개로 줄임 (20% 감소)
   
③ deleted = false (Equality) → 3순위
   - 80개 중 70개로 줄임 (12% 감소)
   
④ email_id (JOIN) → 4순위
   - JOIN 시 인덱스만으로 조회 가능 (커버링 인덱스)

이 순서로 하면 각 단계에서 데이터를 효과적으로 줄일 수 있습니다."
```

---

#### 선택도(Selectivity)란 무엇인가요?

**답변**:
```
"선택도는 '해당 조건으로 얼마나 많은 행을 걸러낼 수 있는가'입니다.

선택도 계산:
선택도 = (조건을 만족하는 행 수) / (전체 행 수)

낮을수록 좋습니다 (더 많이 걸러짐)

우리 테이블 (50,000 rows):

① email_recipient_address = 'admin@coreconnect.io.kr'
   - 조건 만족: 100개
   - 선택도: 100 / 50,000 = 0.002 (0.2%)
   - 평가: ⭐⭐⭐⭐⭐ 매우 좋음

② email_recipient_type IN ('TO', 'CC', 'BCC')
   - 조건 만족: 40,000개
   - 선택도: 40,000 / 50,000 = 0.8 (80%)
   - 평가: ⭐⭐ 나쁨

③ deleted = false
   - 조건 만족: 45,000개
   - 선택도: 45,000 / 50,000 = 0.9 (90%)
   - 평가: ⭐ 매우 나쁨

그래서:
- ① (선택도 0.2%)를 가장 앞에 배치
- ②, ③은 ① 이후에 추가 필터링용

만약 순서를 바꾸면:
(deleted, email_recipient_type, email_recipient_address)
→ 처음에 45,000개를 스캔해야 함 (비효율적)

(email_recipient_address, email_recipient_type, deleted)
→ 처음에 100개만 스캔 (효율적!)

이것이 인덱스 컬럼 순서가 중요한 이유입니다."
```

---

#### 커버링 인덱스(Covering Index)란?

**답변**:
```
"커버링 인덱스는 '인덱스만으로 모든 데이터를 조회할 수 있는' 인덱스입니다.

일반적인 경우:
1. 인덱스에서 조건에 맞는 행 찾기
2. 해당 행의 실제 데이터를 테이블에서 읽기 (Random I/O)
→ 2단계 필요, 느림

커버링 인덱스:
1. 인덱스에서 조건에 맞는 행 찾기
2. 인덱스에 필요한 컬럼이 모두 있음 (테이블 접근 불필요)
→ 1단계만 필요, 빠름!

우리 경우:
쿼리에서 필요한 컬럼:
- email_recipient_address (WHERE)
- email_recipient_type (WHERE)
- deleted (WHERE)
- email_id (JOIN)

인덱스에 포함된 컬럼:
idx_email_recipient_lookup(
    email_recipient_address,
    email_recipient_type,
    deleted,
    email_id  ← 이것 때문에 커버링 인덱스!
)

email_id를 인덱스에 포함했기 때문에:
- JOIN 시 email_recipient 테이블을 다시 읽을 필요 없음
- 인덱스만으로 모든 정보 확인 가능
- I/O 50% 감소!

EXPLAIN 결과:
Extra: Using index  ← 커버링 인덱스 사용됨!"
```

---

#### 효과는 어떻게 측정했나요?

**답변**:
```
"3단계로 검증했습니다.

1단계: EXPLAIN (실행 계획 분석)
EXPLAIN SELECT ... FROM email_recipient r ...;

Before (인덱스 없음):
| type | rows   | Extra                    |
|------|--------|--------------------------|
| ALL  | 50,000 | Using where; Using filesort |

After (인덱스 추가):
| type | rows | Extra         |
|------|------|---------------|
| ref  | 100  | Using index   |

변화:
✅ type: ALL → ref (Full Scan → Index Scan)
✅ rows: 50,000 → 100 (99% 감소)
✅ Extra: Using index (커버링 인덱스)

2단계: MySQL PROFILING (실제 시간 측정)
SET profiling = 1;
SELECT ...;
SHOW PROFILES;

Before: 0.025초 (25ms)
After:  0.008초 (8ms)
개선: 67% 단축

3단계: k6 부하테스트 (실사용 환경)
k6 run test.js

Before: 평균 25-30ms
After:  평균 8-10ms
개선: 67% 단축 (PROFILING과 일치!)

이렇게 3가지 방법으로 교차 검증했습니다."
```

---

### 1-3. email 테이블 정렬 인덱스

#### 인덱스 설계

```sql
CREATE INDEX idx_email_status_sent_time 
ON email(
    email_status,           -- ① WHERE 조건
    email_sent_time DESC    -- ② ORDER BY 조건
);
```

#### 왜 정렬용 인덱스가 필요한가요?

**답변**:
```
"ORDER BY 절이 있는 쿼리는 정렬 작업이 필요합니다.

우리 쿼리:
SELECT ...
WHERE email_status = 'SENT'
ORDER BY email_sent_time DESC

정렬 방법 2가지:

방법 1: Using filesort (인덱스 없음)
1. WHERE 조건으로 데이터 필터링
2. 결과를 메모리 또는 디스크에 임시 저장
3. 정렬 알고리즘 실행 (Quick Sort 등)
4. 정렬된 결과 반환
→ 비용: O(n log n), 느림 (10-15ms)

방법 2: Using index (인덱스 있음)
1. 인덱스가 이미 정렬되어 있음
2. 인덱스에서 순서대로 읽기만 하면 됨
3. 추가 정렬 불필요
→ 비용: O(1), 빠름 (1-2ms)

우리는 방법 2를 선택했습니다.

인덱스 구조:
idx_email_status_sent_time(email_status, email_sent_time DESC)

B-Tree 인덱스 내부:
[SENT, 2025-12-17 10:00:00]
[SENT, 2025-12-17 09:00:00]
[SENT, 2025-12-17 08:00:00]
...

이미 정렬되어 있으므로 그냥 순서대로 읽으면 됩니다!"
```

---

#### 왜 email_status를 앞에 배치했나요?

**답변**:
```
"WHERE 절의 조건을 먼저, ORDER BY를 나중에 배치하는 것이 원칙입니다.

우리 쿼리:
WHERE email_status = 'SENT'      -- ① 필터링
ORDER BY email_sent_time DESC    -- ② 정렬

인덱스 순서:
(email_status, email_sent_time DESC)

동작 과정:
1. email_status = 'SENT'인 부분을 B-Tree에서 찾음
   → 10,000개 발견
2. 그 범위 내에서 email_sent_time이 이미 DESC로 정렬되어 있음
3. 앞에서부터 20개만 읽으면 됨 (LIMIT 20)
4. 추가 정렬 불필요!

만약 순서를 바꾸면:
(email_sent_time DESC, email_status)
1. email_sent_time 전체를 스캔하며 email_status = 'SENT' 찾기
2. 불연속적으로 분포 (비효율적)
3. 결국 filesort 발생

결론:
WHERE 조건(Equality) → ORDER BY 조건 순서가 최적입니다."
```

---

#### DESC는 왜 명시했나요?

**답변**:
```
"ORDER BY의 정렬 방향과 인덱스의 정렬 방향이 일치해야 합니다.

우리 쿼리:
ORDER BY email_sent_time DESC  (내림차순)

인덱스 생성 시:
email_sent_time DESC

일치함! ✅

만약 인덱스를 ASC로 만들면:
email_sent_time ASC (기본값)

불일치! ❌
→ MySQL이 인덱스를 역방향으로 읽어야 함
→ 비효율적 (Backward Index Scan)

MySQL 5.7 이하:
- DESC 인덱스 지원 안함
- 항상 ASC로 저장됨
- 역방향 스캔으로 대응

MySQL 8.0 이상:
- DESC 인덱스 지원
- 실제로 내림차순으로 저장
- 정방향 스캔으로 빠름!

우리는 MySQL 8.0을 사용하므로 DESC를 명시했습니다."
```

---

#### 효과

**답변**:
```
"EXPLAIN과 PROFILING으로 검증했습니다.

EXPLAIN 결과:

Before:
| Extra                    |
|--------------------------|
| Using where; Using filesort |

After:
| Extra         |
|---------------|
| Using index   |

변화:
✅ Using filesort 제거!
✅ Using index (정렬용 인덱스 사용)

PROFILING 결과:

Before:
- Sorting result: 10ms (전체 시간의 40%)

After:
- Sorting result: 1ms (거의 0ms)

개선:
정렬 시간 90% 단축!

k6 테스트:
Phase 1 전체 효과:
25-30ms → 8-10ms (67% 단축)"
```

---

## 🔄 Phase 2: 쿼리 재작성 (상세)

### 2-1. 왜 쿼리 재작성이 필요한가요?

**답변**:
```
"인덱스를 만들었어도 쿼리가 비효율적이면 인덱스가 사용되지 않습니다.

Phase 1 결과 분석:
- 인덱스 추가로 67% 개선
- 하지만 여전히 8-10ms (목표: 2-3ms)
- EXPLAIN 확인 결과: 인덱스 사용률 70%

왜 100% 사용되지 않을까?

EXPLAIN 상세 분석:
possible_keys: idx_email_status_sent_time  (후보)
key: NULL  ← 실제로는 사용 안함!
rows: 10,000  (여전히 많음)

원인: 쿼리의 NOT IN과 IS NULL OR 조건

MySQL 옵티마이저가 판단:
'NOT IN과 OR 조건이 있으면 인덱스보다 Full Scan이 빠르겠다'
→ 인덱스 무시!

해결: 쿼리를 인덱스 친화적으로 재작성"
```

---

### 2-2. NOT IN → = 변경

#### Before vs After

```sql
-- Before
WHERE email_status NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')

-- After  
WHERE email_status = 'SENT'
```

#### 왜 NOT IN은 느린가요? (핵심!)

**답변**:
```
"NOT IN은 MySQL 옵티마이저가 가장 싫어하는 조건입니다.

이유 1: 부정 조건
- 인덱스는 '있는 것'을 빠르게 찾는 자료구조
- '없는 것'을 찾으려면 전체를 확인해야 함

비유:
'전화번호부에서 성이 김씨가 아닌 사람을 찾아라'
→ 전체를 다 봐야 함!

'전화번호부에서 성이 이씨인 사람을 찾아라'
→ 'ㅇ' 부분만 보면 됨!

이유 2: 다중 값 비교
NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
→ 각 행마다 4번 비교

= 'SENT'
→ 각 행마다 1번 비교

이유 3: 옵티마이저 판단
MySQL 옵티마이저의 내부 비용 계산:

NOT IN 사용 시:
- 인덱스 스캔 비용: 10
- 4번 비교 비용: 8
- 총 비용: 18

Full Table Scan 시:
- 전체 스캔 비용: 15
- 총 비용: 15

옵티마이저 판단: 15 < 18
→ Full Table Scan 선택! (인덱스 무시)

= 사용 시:
- 인덱스 스캔 비용: 10
- 1번 비교 비용: 2
- 총 비용: 12

옵티마이저 판단: 12 < 15
→ Index Scan 선택! (인덱스 사용)

이것이 NOT IN → = 변경의 핵심 이유입니다."
```

---

#### 비즈니스 로직 변경 우려는 없나요?

**답변**:
```
"비즈니스 로직을 면밀히 검토했습니다.

Before:
NOT IN ('TRASH', 'DELETED', 'DRAFT', 'RESERVED')
→ 이 4개를 제외한 모든 상태 조회

After:
= 'SENT'
→ SENT 상태만 조회

차이점 분석:

우리 email_status ENUM:
'SENT'      - 정상 발송
'BOUNCE'    - 반송 (수신 서버 거부)
'FAILED'    - 실패 (네트워크 오류)
'TRASH'     - 휴지통
'DELETED'   - 영구 삭제
'DRAFT'     - 임시저장
'RESERVED'  - 예약 발송

Before는 SENT, BOUNCE, FAILED를 모두 조회
After는 SENT만 조회

BOUNCE와 FAILED는 어디로?

조사 결과:
1. BOUNCE: 별도 '반송 메일함'에서 관리
   - 발신자에게 에러 알림
   - 받은편지함에 표시 안함 (맞음!)

2. FAILED: 에러 로그에서 관리
   - 시스템 관리자가 처리
   - 받은편지함에 표시 안함 (맞음!)

결론:
받은편지함은 SENT만 표시하는 것이 정확한 비즈니스 로직!

추가 검증:
- 프론트엔드 코드 확인: ✅ SENT만 처리
- QA 팀 확인: ✅ SENT만 표시가 맞음
- 실사용자 테스트: ✅ 정상 동작
- 데이터 분석: BOUNCE, FAILED는 전체의 0.5% (매우 적음)

안심하고 변경 가능!"
```

---

#### 만약 여러 상태를 조회해야 한다면?

**답변**:
```
"NOT IN 대신 IN을 사용합니다.

나쁜 예:
WHERE email_status NOT IN ('TRASH', 'DELETED', 'DRAFT')

좋은 예:
WHERE email_status IN ('SENT', 'BOUNCE', 'FAILED')

왜?
IN도 여러 값을 비교하지만:
1. 긍정 조건 (인덱스 사용 가능)
2. MySQL이 OR로 최적화
   WHERE email_status = 'SENT' 
      OR email_status = 'BOUNCE' 
      OR email_status = 'FAILED'
3. 인덱스 Range Scan 사용

EXPLAIN 결과:
type: range (Index Range Scan)
key: idx_email_status_sent_time (인덱스 사용!)
rows: 1,000 (3개 상태만 스캔)

성능:
NOT IN: 15ms (Full Scan)
IN:     3ms (Index Scan)
=:      2ms (Index Seek)

순위:
= (최고) > IN (좋음) > NOT IN (나쁨)

결론:
가능하면 = 사용, 불가능하면 IN 사용, NOT IN은 절대 사용 금지!"
```

---

### 2-3. IS NULL OR → 단순 조건

#### Before vs After

```sql
-- Before
WHERE (deleted IS NULL OR deleted = false)

-- After
WHERE deleted = false
```

#### 왜 OR 조건이 문제인가요?

**답변**:
```
"OR 조건은 인덱스 최적화를 매우 어렵게 만듭니다.

이유 1: 인덱스 범위 선택 불가

(deleted IS NULL OR deleted = false)

MySQL 옵티마이저 입장:
- IS NULL: NULL 값들을 찾아야 함
- = false: false 값들을 찾아야 함
- OR: 둘 다 찾아야 함
→ 연속된 범위가 아님!

인덱스 구조:
[NULL, NULL, NULL, false, false, true, true]
 ^^^^^^^^^^^^^^     ^^^^^^^^^^^
  찾아야 함          찾아야 함

→ 불연속적!
→ 인덱스 Range Scan 불가
→ 인덱스 무시하고 Full Scan

이유 2: IS NULL의 특수성

NULL은 '값이 없음'을 의미
→ 비교 연산이 특수함
→ WHERE deleted = NULL (X)
→ WHERE deleted IS NULL (O)

인덱스에서 NULL 처리:
- MySQL 5.7 이하: NULL을 인덱스에 저장 안함
- MySQL 8.0: NULL도 인덱스에 저장
- 하지만 NULL 검색은 여전히 비효율적

이유 3: 조건부 로직

(deleted IS NULL OR deleted = false)
→ 각 행마다 2번 체크
→ 단일 조건보다 2배 느림

deleted = false
→ 각 행마다 1번 체크
→ 빠름!

EXPLAIN 비교:

Before (OR 사용):
type: ALL  (Full Scan)
rows: 50,000
Extra: Using where

After (단순 조건):
type: ref  (Index Scan)
rows: 100
Extra: Using index

결과:
OR 제거만으로 500배 개선!"
```

---

#### NULL을 어떻게 처리했나요?

**답변**:
```
"애플리케이션 레벨에서 NULL을 방지했습니다.

3가지 대응:

1. JPA 엔티티 수정
@Entity
public class EmailRecipient {
    @Column(name = "deleted", nullable = false)
    @Builder.Default
    private Boolean deleted = false;  // 기본값 false
}

효과:
- 새로 INSERT되는 데이터는 자동으로 false
- NULL 저장 불가 (nullable = false)

2. 기존 데이터 마이그레이션
UPDATE email_recipient 
SET deleted = false 
WHERE deleted IS NULL;

효과:
- 기존 NULL 데이터를 false로 변경
- 현재: 1,234개 NULL → 0개 NULL

3. ALTER TABLE (선택)
ALTER TABLE email_recipient 
MODIFY deleted BOOLEAN NOT NULL DEFAULT false;

효과:
- 데이터베이스 레벨에서 NULL 차단
- 더블 체크

검증:
SELECT COUNT(*) FROM email_recipient WHERE deleted IS NULL;
→ 0 (성공!)

이제 안심하고 WHERE deleted = false 사용 가능!"
```

---

#### 효과

**답변**:
```
"Phase 2 전체 효과:

EXPLAIN 비교:

Before (Phase 1):
possible_keys: idx_email_status_sent_time
key: NULL  (인덱스 미사용)
rows: 10,000

After (Phase 2):
possible_keys: idx_email_status_sent_time
key: idx_email_status_sent_time  (인덱스 사용!)
rows: 500

PROFILING:
Before: 8-10ms
After:  5-7ms
개선: 30% 단축

k6 테스트:
Before: 8-10ms
After:  5-7ms
개선: 30% 단축

인덱스 사용률:
Before: 70%
After:  95%
개선: 25%p 증가

누적 개선:
Phase 1: 25-30ms → 8-10ms (67%)
Phase 2: 8-10ms → 5-7ms (30%)
누적: 25-30ms → 5-7ms (77%)

아직 목표(2-3ms)까지는 부족 → Phase 3 필요!"
```

---

## 🔗 Phase 3: N+1 쿼리 해결 (상세)

### 3-1. N+1 쿼리란 무엇인가요?

**답변**:
```
"N+1 쿼리는 ORM(JPA)의 가장 흔한 성능 문제입니다.

이름의 의미:
- 1: 부모 엔티티를 조회하는 쿼리 1개
- N: 각 부모의 자식 엔티티를 조회하는 쿼리 N개
- 총 1 + N개의 쿼리 실행

예시 (우리 경우):

1개 쿼리: EmailRecipient 20개 조회
SELECT * FROM email_recipient 
WHERE email_recipient_address = 'admin@coreconnect.io.kr'
LIMIT 20;

20개 쿼리: 각 EmailRecipient의 Email 조회
SELECT * FROM email WHERE email_id = 1;  -- 1번째
SELECT * FROM email WHERE email_id = 2;  -- 2번째
...
SELECT * FROM email WHERE email_id = 20; -- 20번째

총 21개 쿼리!

왜 발생하나?

JPA의 Lazy Loading 때문:
@Entity
public class EmailRecipient {
    @ManyToOne(fetch = FetchType.LAZY)  // 기본값
    private Email email;
}

LAZY 의미:
- EmailRecipient 조회 시 Email을 바로 가져오지 않음
- email 필드에 접근할 때 비로소 조회
→ 각 접근마다 쿼리 발생!

서비스 코드:
Page<EmailRecipient> recipients = repository.findInbox(...);
recipients.forEach(recipient -> {
    Email email = recipient.getEmail();  // 쿼리 발생!
    String title = email.getEmailTitle();
    String sender = email.getSenderEmail();
});

결과:
20개 EmailRecipient → 20개 쿼리 → N+1 문제!"
```

---

### 3-2. 왜 이제야 N+1을 해결하나요?

**답변**:
```
"단계적 접근 전략이었습니다.

Phase 1, 2로 먼저 DB 레이어를 최적화한 이유:

1. 빠른 성과 (Quick Win)
   - Phase 1, 2: 4시간 (인덱스 2시간 + 쿼리 수정 2시간)
   - Phase 3: 1일 (코드 수정 + 테스트)
   - 먼저 쉬운 것부터 → 빠른 성과

2. 리스크 최소화
   - Phase 1, 2: DB만 수정 (롤백 쉬움)
   - Phase 3: 코드 수정 (테스트 범위 넓음)
   - 리스크 큰 것은 나중에

3. 정확한 진단
   - Phase 1, 2 후에도 5-7ms (여전히 느림)
   - 로그 분석: 쿼리가 21개 실행됨 발견
   - N+1이 진짜 문제임을 확인

4. 팀 설득
   - Phase 1, 2로 77% 개선 증명
   - '코드 수정도 하면 더 빨라집니다' 설득
   - 승인 획득 후 Phase 3 진행

결과:
단계적 접근으로 리스크를 최소화하며 개선!"
```

---

### 3-3. Fetch Join으로 어떻게 해결하나요?

#### Before

```java
@Query("SELECT r FROM EmailRecipient r " +
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +
       "AND r.emailRecipientType IN :emailRecipientType " +
       "AND r.email.emailStatus = 'SENT' " +
       "AND r.deleted = false " +
       "ORDER BY r.email.emailSentTime DESC")
Page<EmailRecipient> findInboxExcludingTrash(...);
```

**실행되는 SQL**:
```sql
-- 1번 쿼리
SELECT * FROM email_recipient r
WHERE r.email_recipient_address = 'admin@coreconnect.io.kr'
  AND r.email_recipient_type IN ('TO', 'CC', 'BCC')
  AND r.deleted = false
LIMIT 20;

-- 2-21번 쿼리 (각 recipient마다)
SELECT * FROM email WHERE email_id = 1;
SELECT * FROM email WHERE email_id = 2;
...
SELECT * FROM email WHERE email_id = 20;
```

**총 21개 쿼리!**

---

#### After

```java
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email e " +  // ⭐ Fetch Join!
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +
       "AND r.emailRecipientType IN :emailRecipientType " +
       "AND e.emailStatus = 'SENT' " +
       "AND r.deleted = false " +
       "ORDER BY e.emailSentTime DESC")
Page<EmailRecipient> findInboxExcludingTrash(...);
```

**실행되는 SQL**:
```sql
-- 1번 쿼리 (한 방에!)
SELECT r.*, e.*
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.email_id
WHERE r.email_recipient_address = 'admin@coreconnect.io.kr'
  AND r.email_recipient_type IN ('TO', 'CC', 'BCC')
  AND e.email_status = 'SENT'
  AND r.deleted = false
ORDER BY e.email_sent_time DESC
LIMIT 20;
```

**총 1개 쿼리!**

---

### 3-4. LEFT JOIN vs LEFT JOIN FETCH 차이

**답변**:
```
"매우 중요한 차이입니다!

1. LEFT JOIN (일반 JOIN)
SELECT r FROM EmailRecipient r
LEFT JOIN r.email e
WHERE ...

실행되는 SQL:
SELECT r.email_recipient_id, r.email_recipient_address, ...
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.email_id
WHERE ...

주의!
- JOIN은 했지만 Email 컬럼을 SELECT 안함!
- EmailRecipient만 조회
- 나중에 email 접근 시 또 쿼리 발생 (여전히 N+1!)

2. LEFT JOIN FETCH (Fetch Join)
SELECT r FROM EmailRecipient r
LEFT JOIN FETCH r.email e
WHERE ...

실행되는 SQL:
SELECT r.email_recipient_id, r.email_recipient_address, ...,
       e.email_id, e.email_title, e.sender_email, ...  -- Email 컬럼도 포함!
FROM email_recipient r
LEFT JOIN email e ON r.email_id = e.email_id
WHERE ...

효과:
- JOIN + Email 컬럼도 SELECT
- EmailRecipient와 Email을 함께 조회
- 나중에 email 접근해도 쿼리 발생 안함!

핵심 차이:
LEFT JOIN: JOIN만 함 (데이터 안 가져옴)
LEFT JOIN FETCH: JOIN + 데이터 가져옴

반드시 FETCH를 붙여야 N+1 해결!"
```

---

### 3-5. 왜 LEFT JOIN이고 INNER JOIN이 아닌가요?

**답변**:
```
"데이터 정합성 보장을 위해서입니다.

INNER JOIN:
SELECT r FROM EmailRecipient r
INNER JOIN FETCH r.email e
WHERE ...

의미:
- EmailRecipient와 Email이 둘 다 있는 경우만 조회
- Email이 없으면 EmailRecipient도 제외됨

문제:
만약 email_id가 잘못되어 Email이 없다면?
→ 해당 EmailRecipient가 결과에서 사라짐
→ 사용자는 받은 메일이 안 보임
→ 심각한 버그!

LEFT JOIN:
SELECT r FROM EmailRecipient r
LEFT JOIN FETCH r.email e
WHERE ...

의미:
- EmailRecipient는 무조건 조회
- Email이 있으면 같이 가져오고, 없으면 NULL

효과:
만약 email_id가 잘못되어 Email이 없더라도:
→ EmailRecipient는 조회됨
→ email 필드만 NULL
→ 최소한 메일 목록은 보임
→ 더 안전!

우리 경우:
@ManyToOne (EmailRecipient → Email)
- Email이 없을 가능성은 거의 없음 (Foreign Key)
- 하지만 만약을 위해 LEFT JOIN 사용
- 방어적 프로그래밍

추가 처리:
if (recipient.getEmail() == null) {
    log.error("Email not found for recipient: {}", recipient.getId());
    // 에러 처리
}

결론:
안전성 > 성능
→ LEFT JOIN 선택!"
```

---

### 3-6. Fetch Join의 주의사항

**답변**:
```
"Fetch Join은 강력하지만 주의사항이 있습니다.

주의사항 1: 페이징 경고

경고 메시지:
HHH000104: firstResult/maxResults specified with collection fetch; 
applying in memory!

의미:
- Fetch Join + Pagination
- DB에서 페이징 안하고 메모리에서 페이징
- 모든 데이터를 메모리에 로드 후 페이징

우리 경우:
@ManyToOne Fetch Join: ✅ 문제없음
@OneToMany Fetch Join: ❌ 문제 발생!

왜?
- @ManyToOne: EmailRecipient(N) → Email(1)
  - 20개 EmailRecipient → 20개 Email
  - 1:1 매핑
  - 페이징 가능

- @OneToMany: Email(1) → EmailRecipient(N)
  - 1개 Email → 100개 EmailRecipient
  - 1:N 매핑
  - 중복 행 발생
  - 페이징 불가능

대안:
1. @BatchSize 사용
@OneToMany
@BatchSize(size = 100)
private List<EmailRecipient> recipients;

2. DTO Projection
@Query("SELECT new EmailDTO(e.id, e.title, ...) FROM Email e")

3. 별도 쿼리로 분리

우리는 @ManyToOne이므로 문제없음!

주의사항 2: MultipleBagFetchException

에러:
cannot simultaneously fetch multiple bags

의미:
- 2개 이상의 @OneToMany를 동시에 Fetch Join 불가

예시:
SELECT e FROM Email e
LEFT JOIN FETCH e.recipients  -- @OneToMany
LEFT JOIN FETCH e.attachments -- @OneToMany
→ 에러!

대안:
1. 하나만 Fetch Join, 나머지는 @BatchSize
2. DTO Projection
3. 별도 쿼리

우리는 @ManyToOne 하나만 사용 → 문제없음!

주의사항 3: Cartesian Product

예시:
Email 1개 → Recipient 3개 + Attachment 2개

일반 JOIN:
SELECT * FROM email e
LEFT JOIN email_recipient r ON e.id = r.email_id
LEFT JOIN email_attachment a ON e.id = a.email_id

결과:
3 × 2 = 6 rows (Cartesian Product!)

중복 제거 필요:
- DISTINCT 사용
- 또는 Set 사용

우리는 @ManyToOne 하나만 → Cartesian Product 없음!

결론:
우리 경우는 모든 주의사항에 해당 안됨 → 안전!"
```

---

### 3-7. 효과

**답변**:
```
"Phase 3 효과:

쿼리 수 측정:

Before (로그 분석):
[2025-12-17 10:00:00.123] SQL: SELECT * FROM email_recipient...
[2025-12-17 10:00:00.145] SQL: SELECT * FROM email WHERE email_id = 1
[2025-12-17 10:00:00.152] SQL: SELECT * FROM email WHERE email_id = 2
...
[2025-12-17 10:00:00.298] SQL: SELECT * FROM email WHERE email_id = 20

총 21개 쿼리 (1 + 20)

After (로그 분석):
[2025-12-17 10:00:00.123] SQL: SELECT r.*, e.* FROM email_recipient r LEFT JOIN email e...

총 1개 쿼리!

쿼리 수 감소: 21개 → 1개 (95% 감소)

PROFILING:

Before:
- Query 1: 5ms
- Query 2-21: 각 0.1ms × 20 = 2ms
- 총: 7ms

After:
- Query 1: 3ms (JOIN 비용 포함)
- 총: 3ms

개선: 7ms → 3ms (57% 단축)

k6 테스트:

Before: 5-7ms
After:  2-3ms
개선: 40% 단축

네트워크 왕복:

Before: 21번 (DB 왕복 21회)
After:  1번 (DB 왕복 1회)
개선: 95% 감소

전체 누적 개선:

Phase 1: 25-30ms → 8-10ms (67% 단축)
Phase 2: 8-10ms → 5-7ms (30% 단축)
Phase 3: 5-7ms → 2-3ms (40% 단축)

최종: 25-30ms → 2-3ms (88-90% 단축)

목표 달성! ✅"
```

---

## 📊 전체 흐름 요약 (면접 답변용)

### 1분 버전

```
"이메일 조회가 25-30ms로 느려서 3단계로 최적화했습니다.

Phase 1: 복합 인덱스 추가
- email_recipient와 email 테이블에 인덱스 생성
- 컬럼 순서: Equality → IN → JOIN 순으로 배치
- Full Table Scan → Index Scan 개선
- 효과: 25-30ms → 8-10ms (67% 단축)

Phase 2: 쿼리 재작성
- NOT IN → = 변경 (인덱스 활용률 향상)
- IS NULL OR → 단순 조건 (옵티마이저 최적화)
- 효과: 8-10ms → 5-7ms (30% 단축)

Phase 3: N+1 쿼리 해결
- LEFT JOIN FETCH로 한 방에 조회
- 쿼리 수: 21개 → 1개 (95% 감소)
- 효과: 5-7ms → 2-3ms (40% 단축)

최종: 25-30ms → 2-3ms (88-90% 단축)

각 단계마다 EXPLAIN, PROFILING, k6로 검증하며 진행했습니다."
```

---

### 3분 버전 (상세)

```
[30초] 문제 인식
"k6 부하테스트 결과 이메일 API가 25-30ms로 가장 느렸습니다.
EXPLAIN으로 분석한 결과 3가지 병목을 발견했습니다:
1) Full Table Scan (50,000 rows)
2) NOT IN으로 인덱스 미사용
3) N+1 쿼리 (21개 쿼리 실행)"

[60초] Phase 1 - 인덱스
"먼저 ROI가 가장 높은 인덱스부터 추가했습니다.

email_recipient 테이블에는:
(email_recipient_address, email_recipient_type, deleted, email_id)
순서로 복합 인덱스를 만들었습니다.

이 순서를 선택한 이유는 선택도 때문입니다.
email_recipient_address는 50,000개 중 100개로 줄이므로
선택도가 0.2%로 가장 효과적이어서 가장 앞에 배치했습니다.

email 테이블에는:
(email_status, email_sent_time DESC)
순서로 정렬용 인덱스를 만들어 Using filesort를 제거했습니다.

결과: 25-30ms → 8-10ms (67% 단축)"

[60초] Phase 2 - 쿼리 재작성
"인덱스를 만들었지만 사용률이 70%에 그쳤습니다.
원인은 NOT IN과 IS NULL OR 조건이었습니다.

NOT IN은 부정 조건이라 MySQL 옵티마이저가 인덱스보다
Full Scan이 빠르다고 판단해서 인덱스를 무시했습니다.

그래서 NOT IN ('TRASH', 'DELETED', ...)를
= 'SENT'로 변경했습니다.
비즈니스 로직을 검토한 결과 받은편지함은 SENT만 표시하는 것이
정확했기 때문에 안전하게 변경할 수 있었습니다.

IS NULL OR deleted = false도
deleted = false로 단순화했습니다.
JPA 엔티티에 @Builder.Default를 설정해서 NULL을 방지했습니다.

결과: 8-10ms → 5-7ms (30% 단축)"

[60초] Phase 3 - N+1 해결
"여전히 5-7ms로 목표인 2-3ms에는 못 미쳤습니다.
로그를 분석한 결과 쿼리가 21개나 실행되고 있었습니다.

JPA의 Lazy Loading 때문에 발생한 N+1 문제였습니다.
EmailRecipient 20개를 조회한 후,
각각의 email 필드에 접근할 때마다 쿼리가 발생했습니다.

LEFT JOIN FETCH를 사용해서 한 번에 조회하도록 수정했습니다.
LEFT JOIN을 선택한 이유는 데이터 정합성 보장 때문입니다.
혹시 Email이 없더라도 EmailRecipient는 조회되어야 하므로
안전한 LEFT JOIN을 사용했습니다.

결과: 5-7ms → 2-3ms (40% 단축)
쿼리 수: 21개 → 1개 (95% 감소)"

[30초] 검증 및 결론
"각 단계마다 3가지 방법으로 검증했습니다:
1) EXPLAIN으로 실행 계획 확인
2) PROFILING으로 실제 시간 측정
3) k6 부하테스트로 실사용 환경 검증

최종적으로 25-30ms에서 2-3ms로 88-90% 개선했으며,
비용은 0원이었습니다."
```

---

## 🎯 면접 팁

### 강조할 포인트

```
1. 체계적 접근
   ❌ "인덱스 추가했어요"
   ✅ "ROI 분석 후 우선순위를 정해 3단계로 진행했습니다"

2. 기술 깊이
   ❌ "복합 인덱스를 만들었어요"
   ✅ "선택도를 분석해서 Equality 조건을 앞에 배치했습니다"

3. 검증 방법
   ❌ "빨라졌어요"
   ✅ "EXPLAIN, PROFILING, k6로 교차 검증했습니다"

4. 트레이드오프 인식
   ❌ "문제없어요"
   ✅ "디스크 증가, 쓰기 성능 저하를 고려했지만 읽기 비중이 90%라 이득입니다"

5. 비즈니스 이해
   ❌ "쿼리를 바꿨어요"
   ✅ "비즈니스 로직을 검증한 후 안전하게 변경했습니다"
```

---

**작성일**: 2025-12-17  
**목적**: 면접 상세 답변 가이드  
**예상 질문 시간**: 5-10분  
**준비 시간**: 2-3시간 권장

