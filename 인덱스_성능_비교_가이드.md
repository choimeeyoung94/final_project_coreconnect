# 📊 인덱스 성능 비교 가이드 (Before vs After)

## 🎯 목표
인덱스 적용 전후의 쿼리 실행 시간을 정확하게 측정하고 비교합니다.

---

## 📋 목차
1. [성능 측정 준비](#1-성능-측정-준비)
2. [인덱스 적용 전 성능 측정](#2-인덱스-적용-전-성능-측정-before)
3. [인덱스 생성](#3-인덱스-생성)
4. [인덱스 적용 후 성능 측정](#4-인덱스-적용-후-성능-측정-after)
5. [결과 비교 및 분석](#5-결과-비교-및-분석)

---

## 1. 성능 측정 준비

### Step 1: 타이머 활성화

```sql
-- MySQL 쿼리 실행 시간 표시 활성화
SET profiling = 1;

-- 확인
SHOW VARIABLES LIKE 'profiling';
-- Result: profiling = ON
```

---

## 2. 인덱스 적용 전 성능 측정 (BEFORE)

### 🔍 성능 측정 스크립트 (인덱스 적용 전)

```sql
-- =================================================================
-- 📊 인덱스 적용 전 성능 측정 (BEFORE)
-- =================================================================

USE coreconnect;

-- 타이머 활성화
SET profiling = 1;

-- 캐시 초기화 (정확한 측정을 위해)
RESET QUERY CACHE;
FLUSH TABLES;

SELECT '🔍 인덱스 적용 전 성능 측정 시작...' AS status;

-- -----------------------------------------------------------------
-- 테스트 1: 채팅방별 메시지 조회 (가장 중요!)
-- -----------------------------------------------------------------
SELECT '📝 테스트 1: 채팅방 메시지 조회' AS test_name;

SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;

-- -----------------------------------------------------------------
-- 테스트 2: 읽지 않은 메시지 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 2: 읽지 않은 메시지 조회' AS test_name;

SELECT * FROM chat_message 
WHERE chat_room_id = 1 AND read_yn = 'N' 
ORDER BY sent_at DESC;

-- -----------------------------------------------------------------
-- 테스트 3: 사용자별 참여 채팅방 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 3: 사용자 참여 채팅방 조회' AS test_name;

SELECT * FROM chat_room_user 
WHERE user_id = 1;

-- -----------------------------------------------------------------
-- 테스트 4: 채팅방 참여자 목록 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 4: 채팅방 참여자 목록 조회' AS test_name;

SELECT cru.*, u.user_name, u.user_email 
FROM chat_room_user cru
JOIN users u ON cru.user_id = u.user_id
WHERE cru.chat_room_id = 1;

-- -----------------------------------------------------------------
-- 테스트 5: 읽지 않은 메시지 개수 카운트
-- -----------------------------------------------------------------
SELECT '📝 테스트 5: 읽지 않은 메시지 카운트' AS test_name;

SELECT COUNT(*) 
FROM chat_message_read_status 
WHERE user_id = 1 AND chat_message_read_status_read_yn = 'N';

-- -----------------------------------------------------------------
-- 테스트 6: 최근 메시지 100개 조회 (복잡한 쿼리)
-- -----------------------------------------------------------------
SELECT '📝 테스트 6: 최근 메시지 100개 조회' AS test_name;

SELECT 
    cm.*,
    u.user_name AS sender_name,
    cr.room_name
FROM chat_message cm
LEFT JOIN users u ON cm.sender_id = u.user_id
LEFT JOIN chat_room cr ON cm.chat_room_id = cr.chat_room_id
WHERE cm.chat_room_id = 1
ORDER BY cm.sent_at DESC
LIMIT 100;

-- =================================================================
-- 📊 실행 시간 확인
-- =================================================================

SELECT '✅ 측정 완료! 아래에서 실행 시간 확인' AS status;

-- 최근 쿼리 실행 시간 확인
SHOW PROFILES;

-- 상세 프로파일링 (가장 최근 쿼리)
SHOW PROFILE FOR QUERY 1;
SHOW PROFILE FOR QUERY 2;
SHOW PROFILE FOR QUERY 3;
SHOW PROFILE FOR QUERY 4;
SHOW PROFILE FOR QUERY 5;
SHOW PROFILE FOR QUERY 6;

-- =================================================================
-- 📝 EXPLAIN 분석 (인덱스 사용 여부 확인)
-- =================================================================

SELECT '🔍 EXPLAIN 분석 (인덱스 적용 전)' AS status;

-- 테스트 1: 채팅방 메시지 조회
EXPLAIN SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;
-- 기대: type = ALL (Full Table Scan), key = NULL

-- 테스트 3: 사용자 채팅방 목록
EXPLAIN SELECT * FROM chat_room_user 
WHERE user_id = 1;
-- 기대: type = ALL (Full Table Scan), key = NULL

-- 테스트 5: 읽지 않은 메시지 카운트
EXPLAIN SELECT COUNT(*) 
FROM chat_message_read_status 
WHERE user_id = 1 AND chat_message_read_status_read_yn = 'N';
-- 기대: type = ALL (Full Table Scan), key = NULL
```

### 📊 결과 기록 템플릿 (BEFORE)

```
=================================================================
📊 인덱스 적용 전 성능 측정 결과 (BEFORE)
=================================================================

테스트 1: 채팅방 메시지 조회
- 실행 시간: _______ 초
- EXPLAIN type: ALL (Full Table Scan)
- EXPLAIN key: NULL
- EXPLAIN rows: _______

테스트 2: 읽지 않은 메시지 조회
- 실행 시간: _______ 초
- EXPLAIN type: ALL
- EXPLAIN key: NULL
- EXPLAIN rows: _______

테스트 3: 사용자 채팅방 목록
- 실행 시간: _______ 초
- EXPLAIN type: ALL
- EXPLAIN key: NULL
- EXPLAIN rows: _______

테스트 4: 채팅방 참여자 목록 (JOIN)
- 실행 시간: _______ 초

테스트 5: 읽지 않은 메시지 카운트
- 실행 시간: _______ 초
- EXPLAIN type: ALL
- EXPLAIN key: NULL
- EXPLAIN rows: _______

테스트 6: 최근 메시지 100개 (복잡한 JOIN)
- 실행 시간: _______ 초

=================================================================
```

---

## 3. 인덱스 생성

### ✅ 인덱스 생성 스크립트

```sql
-- =================================================================
-- 🚀 인덱스 생성
-- =================================================================

USE coreconnect;

SELECT '🚀 인덱스 생성 시작...' AS status;

-- chat_message 테이블 (4개)
CREATE INDEX idx_chat_room_sent_at ON chat_message(chat_room_id, sent_at DESC);
CREATE INDEX idx_chat_room_read_yn ON chat_message(chat_room_id, read_yn, sent_at DESC);
CREATE INDEX idx_sender_sent_at ON chat_message(sender_id, sent_at DESC);
CREATE INDEX idx_sent_at ON chat_message(sent_at DESC);

-- chat_room 테이블 (3개)
CREATE INDEX idx_room_name ON chat_room(room_name);
CREATE INDEX idx_room_type ON chat_room(room_type);
CREATE INDEX idx_drafter_id ON chat_room(user_id);

-- chat_room_user 테이블 (2개)
CREATE INDEX idx_user_id ON chat_room_user(user_id);
CREATE INDEX idx_chat_room_id ON chat_room_user(chat_room_id);

-- chat_message_read_status 테이블 (3개)
CREATE INDEX idx_chat_message_id ON chat_message_read_status(chat_message_id);
CREATE INDEX idx_user_read_status ON chat_message_read_status(user_id);
CREATE INDEX idx_user_unread ON chat_message_read_status(user_id, chat_message_read_status_read_yn);

-- 통계 정보 업데이트
ANALYZE TABLE chat_message;
ANALYZE TABLE chat_room;
ANALYZE TABLE chat_room_user;
ANALYZE TABLE chat_message_read_status;

SELECT '✅ 인덱스 생성 완료!' AS status;
```

---

## 4. 인덱스 적용 후 성능 측정 (AFTER)

### 🚀 성능 측정 스크립트 (인덱스 적용 후)

```sql
-- =================================================================
-- 📊 인덱스 적용 후 성능 측정 (AFTER)
-- =================================================================

USE coreconnect;

-- 타이머 리셋
SET profiling = 0;
SET profiling = 1;

-- 캐시 초기화 (공정한 비교를 위해)
RESET QUERY CACHE;
FLUSH TABLES;

SELECT '🚀 인덱스 적용 후 성능 측정 시작...' AS status;

-- -----------------------------------------------------------------
-- 테스트 1: 채팅방별 메시지 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 1: 채팅방 메시지 조회 (인덱스 적용)' AS test_name;

SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;

-- -----------------------------------------------------------------
-- 테스트 2: 읽지 않은 메시지 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 2: 읽지 않은 메시지 조회 (인덱스 적용)' AS test_name;

SELECT * FROM chat_message 
WHERE chat_room_id = 1 AND read_yn = 'N' 
ORDER BY sent_at DESC;

-- -----------------------------------------------------------------
-- 테스트 3: 사용자별 참여 채팅방 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 3: 사용자 참여 채팅방 조회 (인덱스 적용)' AS test_name;

SELECT * FROM chat_room_user 
WHERE user_id = 1;

-- -----------------------------------------------------------------
-- 테스트 4: 채팅방 참여자 목록 조회
-- -----------------------------------------------------------------
SELECT '📝 테스트 4: 채팅방 참여자 목록 조회 (인덱스 적용)' AS test_name;

SELECT cru.*, u.user_name, u.user_email 
FROM chat_room_user cru
JOIN users u ON cru.user_id = u.user_id
WHERE cru.chat_room_id = 1;

-- -----------------------------------------------------------------
-- 테스트 5: 읽지 않은 메시지 개수 카운트
-- -----------------------------------------------------------------
SELECT '📝 테스트 5: 읽지 않은 메시지 카운트 (인덱스 적용)' AS test_name;

SELECT COUNT(*) 
FROM chat_message_read_status 
WHERE user_id = 1 AND chat_message_read_status_read_yn = 'N';

-- -----------------------------------------------------------------
-- 테스트 6: 최근 메시지 100개 조회 (복잡한 쿼리)
-- -----------------------------------------------------------------
SELECT '📝 테스트 6: 최근 메시지 100개 조회 (인덱스 적용)' AS test_name;

SELECT 
    cm.*,
    u.user_name AS sender_name,
    cr.room_name
FROM chat_message cm
LEFT JOIN users u ON cm.sender_id = u.user_id
LEFT JOIN chat_room cr ON cm.chat_room_id = cr.chat_room_id
WHERE cm.chat_room_id = 1
ORDER BY cm.sent_at DESC
LIMIT 100;

-- =================================================================
-- 📊 실행 시간 확인
-- =================================================================

SELECT '✅ 측정 완료! 아래에서 실행 시간 확인' AS status;

-- 최근 쿼리 실행 시간 확인
SHOW PROFILES;

-- =================================================================
-- 📝 EXPLAIN 분석 (인덱스 사용 여부 확인)
-- =================================================================

SELECT '🔍 EXPLAIN 분석 (인덱스 적용 후)' AS status;

-- 테스트 1: 채팅방 메시지 조회
EXPLAIN SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;
-- 기대: type = ref, key = idx_chat_room_sent_at

-- 테스트 3: 사용자 채팅방 목록
EXPLAIN SELECT * FROM chat_room_user 
WHERE user_id = 1;
-- 기대: type = ref, key = idx_user_id

-- 테스트 5: 읽지 않은 메시지 카운트
EXPLAIN SELECT COUNT(*) 
FROM chat_message_read_status 
WHERE user_id = 1 AND chat_message_read_status_read_yn = 'N';
-- 기대: type = ref, key = idx_user_unread
```

### 📊 결과 기록 템플릿 (AFTER)

```
=================================================================
📊 인덱스 적용 후 성능 측정 결과 (AFTER)
=================================================================

테스트 1: 채팅방 메시지 조회
- 실행 시간: _______ 초
- EXPLAIN type: ref (Index Scan)
- EXPLAIN key: idx_chat_room_sent_at
- EXPLAIN rows: _______

테스트 2: 읽지 않은 메시지 조회
- 실행 시간: _______ 초
- EXPLAIN type: ref
- EXPLAIN key: idx_chat_room_read_yn
- EXPLAIN rows: _______

테스트 3: 사용자 채팅방 목록
- 실행 시간: _______ 초
- EXPLAIN type: ref
- EXPLAIN key: idx_user_id
- EXPLAIN rows: _______

테스트 4: 채팅방 참여자 목록 (JOIN)
- 실행 시간: _______ 초

테스트 5: 읽지 않은 메시지 카운트
- 실행 시간: _______ 초
- EXPLAIN type: ref
- EXPLAIN key: idx_user_unread
- EXPLAIN rows: _______

테스트 6: 최근 메시지 100개 (복잡한 JOIN)
- 실행 시간: _______ 초

=================================================================
```

---

## 5. 결과 비교 및 분석

### 📊 성능 비교표

```sql
-- =================================================================
-- 📊 성능 비교 분석
-- =================================================================

SELECT '📊 인덱스 적용 전후 성능 비교' AS title;

-- 비교표 생성 (수동으로 채워넣기)
SELECT 
    '테스트' AS test_case,
    'BEFORE (초)' AS before_time,
    'AFTER (초)' AS after_time,
    '개선율 (%)' AS improvement

UNION ALL SELECT '1. 채팅방 메시지 조회', '0.500', '0.050', '90%'
UNION ALL SELECT '2. 읽지 않은 메시지', '0.350', '0.040', '89%'
UNION ALL SELECT '3. 사용자 채팅방 목록', '0.200', '0.020', '90%'
UNION ALL SELECT '4. 채팅방 참여자 (JOIN)', '0.800', '0.080', '90%'
UNION ALL SELECT '5. 읽지 않은 메시지 수', '0.150', '0.015', '90%'
UNION ALL SELECT '6. 최근 메시지 100개', '1.500', '0.150', '90%';
```

**예상 결과:**

| 테스트 | BEFORE (초) | AFTER (초) | 개선율 (%) | 비고 |
|-------|------------|-----------|-----------|------|
| 1. 채팅방 메시지 조회 | 0.500 | 0.050 | **90% ↓** | 200배 향상 |
| 2. 읽지 않은 메시지 | 0.350 | 0.040 | **89% ↓** | - |
| 3. 사용자 채팅방 목록 | 0.200 | 0.020 | **90% ↓** | 250배 향상 |
| 4. 채팅방 참여자 (JOIN) | 0.800 | 0.080 | **90% ↓** | - |
| 5. 읽지 않은 메시지 수 | 0.150 | 0.015 | **90% ↓** | - |
| 6. 최근 메시지 100개 | 1.500 | 0.150 | **90% ↓** | 복잡한 JOIN |

---

### 📈 시각화 비교

```
성능 비교 (실행 시간)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

테스트 1: 채팅방 메시지 조회
BEFORE: ████████████████████████████████████████████████ 0.500초
AFTER:  █████ 0.050초
개선율: 90% ↓

테스트 2: 읽지 않은 메시지 조회
BEFORE: ███████████████████████████████████ 0.350초
AFTER:  ████ 0.040초
개선율: 89% ↓

테스트 3: 사용자 채팅방 목록
BEFORE: ████████████████████ 0.200초
AFTER:  ██ 0.020초
개선율: 90% ↓

테스트 4: 채팅방 참여자 (JOIN)
BEFORE: ████████████████████████████████████████████████████████████████████████████ 0.800초
AFTER:  ████████ 0.080초
개선율: 90% ↓

테스트 5: 읽지 않은 메시지 카운트
BEFORE: ███████████████ 0.150초
AFTER:  ██ 0.015초
개선율: 90% ↓

테스트 6: 최근 메시지 100개 (복잡한 쿼리)
BEFORE: ████████████████████████████████████████████████████████████████████████████████████████████████████ 1.500초
AFTER:  ███████████ 0.150초
개선율: 90% ↓

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### 📊 EXPLAIN 비교

#### BEFORE (인덱스 없음)

```
+----+-------------+--------------+------+---------------+------+---------+------+--------+----------+
| id | select_type | table        | type | possible_keys | key  | key_len | ref  | rows   | Extra    |
+----+-------------+--------------+------+---------------+------+---------+------+--------+----------+
|  1 | SIMPLE      | chat_message | ALL  | NULL          | NULL | NULL    | NULL | 100000 | Using filesort |
+----+-------------+--------------+------+---------------+------+---------+------+--------+----------+

❌ type: ALL (Full Table Scan - 나쁨!)
❌ key: NULL (인덱스 미사용)
❌ rows: 100,000 (전체 테이블 스캔)
❌ Extra: Using filesort (추가 정렬 필요)
```

#### AFTER (인덱스 적용)

```
+----+-------------+--------------+------+------------------------+------------------------+---------+-------+------+-------------+
| id | select_type | table        | type | possible_keys          | key                    | key_len | ref   | rows | Extra       |
+----+-------------+--------------+------+------------------------+------------------------+---------+-------+------+-------------+
|  1 | SIMPLE      | chat_message | ref  | idx_chat_room_sent_at  | idx_chat_room_sent_at  | 4       | const | 100  | Using index |
+----+-------------+--------------+------+------------------------+------------------------+---------+-------+------+-------------+

✅ type: ref (Index Scan - 좋음!)
✅ key: idx_chat_room_sent_at (인덱스 사용!)
✅ rows: 100 (1000분의 1로 감소)
✅ Extra: Using index (커버링 인덱스 - 최고!)
```

---

## 💡 추가 성능 측정 팁

### 1. 반복 측정 (정확도 향상)

```sql
-- 각 쿼리를 3번씩 실행하고 평균 계산
-- 첫 번째 실행은 캐시 워밍업, 2-3번째 평균 사용

-- 1회차
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 20;

-- 2회차
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 20;

-- 3회차
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 20;

SHOW PROFILES;
-- 2회차와 3회차 평균 사용
```

---

### 2. 대용량 데이터 테스트

```sql
-- 데이터 양에 따른 성능 차이 확인

-- LIMIT 20 (일반적인 경우)
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 20;

-- LIMIT 100 (많은 메시지)
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 100;

-- LIMIT 1000 (대용량)
SELECT * FROM chat_message WHERE chat_room_id = 1 ORDER BY sent_at DESC LIMIT 1000;
```

---

### 3. MySQL 8.0 EXPLAIN ANALYZE (추천!)

```sql
-- MySQL 8.0.18+ 에서 사용 가능
-- 실제 실행 시간과 실행 계획을 동시에 확인

-- BEFORE (인덱스 없음)
EXPLAIN ANALYZE 
SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;

-- AFTER (인덱스 적용)
EXPLAIN ANALYZE 
SELECT * FROM chat_message 
WHERE chat_room_id = 1 
ORDER BY sent_at DESC 
LIMIT 20;
```

**결과 예시:**
```
-> Limit: 20 row(s)  (actual time=0.045..0.052 rows=20 loops=1)
    -> Index range scan on chat_message using idx_chat_room_sent_at  
       (actual time=0.044..0.050 rows=20 loops=1)

✅ actual time: 0.045초 (실제 실행 시간!)
```

---

## 🎯 성능 측정 체크리스트

### ✅ 측정 전

- [ ] `SET profiling = 1` 실행
- [ ] `RESET QUERY CACHE` 실행
- [ ] `FLUSH TABLES` 실행
- [ ] 동일한 조건으로 테스트 (같은 chat_room_id, user_id 사용)

### ✅ 측정 중

- [ ] 각 쿼리를 3번씩 실행
- [ ] `SHOW PROFILES`로 실행 시간 기록
- [ ] `EXPLAIN`으로 실행 계획 확인
- [ ] `EXPLAIN ANALYZE`로 실제 시간 확인 (MySQL 8.0.18+)

### ✅ 측정 후

- [ ] BEFORE와 AFTER 결과 비교표 작성
- [ ] 개선율 계산 및 기록
- [ ] EXPLAIN 결과 비교 (type, key, rows)
- [ ] 스크린샷 저장 (포트폴리오용)

---

## 📸 결과 저장

### Excel/Google Sheets 템플릿

```
테스트명 | BEFORE(초) | AFTER(초) | 개선율 | BEFORE_type | AFTER_type | BEFORE_rows | AFTER_rows | 비고
1. 채팅방 메시지 조회 | 0.500 | 0.050 | 90% | ALL | ref | 100000 | 100 | 가장 많이 사용
2. 읽지 않은 메시지 | 0.350 | 0.040 | 89% | ALL | ref | 50000 | 50 | 
3. 사용자 채팅방 목록 | 0.200 | 0.020 | 90% | ALL | ref | 10000 | 10 |
```

---

## 🎉 완료!

이제 인덱스 적용 전후의 성능을 정확하게 측정하고 비교할 수 있습니다!

**예상 결과:**
- ✅ 쿼리 실행 시간: **90% 감소**
- ✅ DB CPU 사용률: **87.5% 감소**
- ✅ 처리량 (TPS): **10배 증가**

**포트폴리오 활용:**
- EXPLAIN 결과 스크린샷
- SHOW PROFILES 결과 캡처
- 성능 비교표
- 시각화 그래프

---

**궁금한 점이 있으면 언제든지 물어보세요!** 🚀




