# JPA N+1 쿼리 최적화: 쿼리 수 95% 감소

## 문제 상황

이메일 받은편지함 조회 시:
- **실행 쿼리 수: 21개** (1개 메인 쿼리 + 20개 추가 쿼리)
- **DB 왕복 횟수: 21번** (네트워크 오버헤드)
- 메일 개수 증가 시: 선형 증가 (O(N))
  - 메일 20개 → 21개 쿼리
  - 메일 100개 → 101개 쿼리
- **응답 시간 증가:** 메일 수에 비례하여 지연

**사용자 경험:** 메일이 많을수록 로딩 시간 길어짐

---

## 원인 분석

### N+1 쿼리 패턴

**JPA Lazy Loading으로 인한 추가 쿼리 발생**

```java
// AS-IS: N+1 문제 발생 코드
@Query("SELECT r FROM EmailRecipient r " +
       "WHERE r.emailRecipientAddress = :address")
List<EmailRecipient> findByAddress(String address);

// 서비스 레이어
List<EmailRecipient> recipients = repository.findByAddress(email);
for (EmailRecipient r : recipients) {
    Email email = r.getEmail();  // ← 여기서 추가 쿼리 발생!
    // ...
}
```

**실행된 쿼리:**
```sql
-- 1. 메인 쿼리 (EmailRecipient 조회)
SELECT * FROM email_recipient WHERE email_recipient_address = '...'

-- 2-21. 각 EmailRecipient마다 Email 조회 (20번 반복)
SELECT * FROM email WHERE email_id = 1
SELECT * FROM email WHERE email_id = 2
SELECT * FROM email WHERE email_id = 3
...
SELECT * FROM email WHERE email_id = 20
```

**문제점:**
```
DB 왕복: 21번
네트워크 라운드트립: 21 × 2ms = 42ms
쿼리 실행: 21 × 1ms = 21ms
총 오버헤드: 63ms
```

---

## AS-IS vs TO-BE

### AS-IS (Lazy Loading)

| 지표 | 값 |
|------|-----|
| **쿼리 수** | 21개 (1 + N) |
| **DB 왕복** | 21번 |
| **추가 쿼리 시간** | 15-20ms |
| **메모리 효율** | 높음 (필요시만 로딩) |
| **네트워크 오버헤드** | ❌ 높음 (21번 왕복) |

**쿼리 로그:**
```
Hibernate: SELECT ... FROM email_recipient ... (1개)
Hibernate: SELECT ... FROM email WHERE email_id=? (20개 반복)
Hibernate: SELECT ... FROM email WHERE email_id=?
Hibernate: SELECT ... FROM email WHERE email_id=?
...
```

### TO-BE (Fetch Join)

| 지표 | 값 | 개선율 |
|------|-----|--------|
| **쿼리 수** | **1개** | **95% 감소** 🔥 |
| **DB 왕복** | **1번** | **95% 감소** |
| **추가 쿼리 시간** | **0ms** | **100% 제거** ⚡ |
| **메모리 효율** | 약간 감소 (즉시 로딩) | - |
| **네트워크 오버헤드** | ✅ 최소화 (1번 왕복) | **95% 감소** |

**쿼리 로그:**
```
Hibernate: 
SELECT r.*, e.* 
FROM email_recipient r 
LEFT JOIN email e ON r.email_id = e.email_id 
WHERE r.email_recipient_address = '...'
```

**단일 쿼리로 모든 데이터 조회!**

---

## 해결 방법

### 1. Fetch Join 적용

**Repository 수정:**

```java
// ❌ Before: N+1 발생
@Query("SELECT r FROM EmailRecipient r " +
       "WHERE r.emailRecipientAddress = :address")
List<EmailRecipient> findByAddress(String address);

// ✅ After: Fetch Join으로 한번에 조회
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email e " +  // ← 핵심!
       "WHERE r.emailRecipientAddress = :address " +
       "ORDER BY e.emailSentTime DESC")
List<EmailRecipient> findByAddressWithEmail(String address);
```

**Fetch Join 동작:**
```
1. EmailRecipient와 Email을 JOIN하여 한번에 조회
2. Hibernate가 결과를 객체 그래프로 매핑
3. r.getEmail() 호출 시 추가 쿼리 없음 (이미 로딩됨)
```

### 2. 실제 적용 코드

**EmailRecipientRepository.java:**

```java
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email e " +
       "WHERE r.emailRecipientAddress = :emailRecipientAddress " +
       "AND r.emailRecipientType IN :emailRecipientType " +
       "AND e.emailStatus = 'SENT' " +
       "AND r.deleted = false " +
       "ORDER BY e.emailSentTime DESC")
Page<EmailRecipient> findInboxExcludingTrash(
    @Param("emailRecipientAddress") String emailRecipientAddress,
    @Param("emailRecipientType") List<String> emailRecipientType,
    Pageable pageable
);
```

**특징:**
- `LEFT JOIN FETCH` → Email이 없어도 EmailRecipient 조회
- 여러 조건 복합 적용
- 정렬까지 한번에 처리

---

## 검증 결과

### SQL 로그 비교

**Before (N+1):**
```
2024-12-19 10:00:01 [SQL] SELECT * FROM email_recipient WHERE...
2024-12-19 10:00:01 [SQL] SELECT * FROM email WHERE email_id = 1
2024-12-19 10:00:01 [SQL] SELECT * FROM email WHERE email_id = 2
... (20번 반복)
```

**After (Fetch Join):**
```
2024-12-19 10:00:01 [SQL] 
SELECT r.*, e.* 
FROM email_recipient r 
LEFT JOIN email e ON r.email_id = e.email_id 
WHERE r.email_recipient_address = '...'
```

### 성능 측정

**쿼리 수 비교:**

| 메일 개수 | Before (쿼리 수) | After (쿼리 수) | 감소율 |
|----------|-----------------|----------------|--------|
| 20개 | 21개 (1 + 20) | **1개** | **95%** |
| 50개 | 51개 (1 + 50) | **1개** | **98%** |
| 100개 | 101개 (1 + 100) | **1개** | **99%** |

**응답 시간 비교:**

| 메일 개수 | Before | After | 개선율 |
|----------|--------|-------|--------|
| 20개 | 15-20ms | **2-3ms** | **85-88%** |
| 50개 | 40-50ms | **3-4ms** | **92-93%** |
| 100개 | 80-100ms | **5-6ms** | **94-95%** |

### 부하 테스트

**k6 Load Test (동시 45명, 2분):**

| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| DB 쿼리 수/분 | 5,354개 | **254개** | **95% 감소** |
| 평균 응답시간 | 18ms | **3ms** | **83% 단축** |
| DB CPU 사용률 | 45% | **15%** | **67% 감소** |
| 에러율 | 0% | 0% | 안정적 ✓ |

---

## 기술 스택

- **ORM:** Spring Data JPA, Hibernate
- **Query:** JPQL Fetch Join
- **Database:** MySQL 8.0
- **Testing:** Spring Boot Test, k6
- **Monitoring:** Hibernate SQL Logging, P6Spy

---

## 핵심 성과

```
🎯 쿼리 수 95% 감소 (21개 → 1개)
🎯 응답시간 85% 단축 (18ms → 3ms)
🎯 DB 부하 67% 감소 (CPU 45% → 15%)
🎯 확장성 향상 (100개 메일도 6ms 이내)
```

**비즈니스 임팩트:**
- DB 부하 감소로 동시 처리량 증가
- 메일 개수 증가에도 일정한 성능 유지
- 서버 비용 절감 (DB CPU 사용률 감소)

---

## 추가 최적화

### 1. 다른 N+1 패턴도 해결

**채팅 목록 조회:**
```java
@Query("SELECT r FROM ChatRoom r " +
       "LEFT JOIN FETCH r.lastMessage " +
       "LEFT JOIN FETCH r.participants " +
       "WHERE ...")
```

**성과:** 채팅 목록 쿼리 90% 감소

### 2. @EntityGraph 방식 (대안)

```java
@EntityGraph(attributePaths = {"email", "user"})
@Query("SELECT r FROM EmailRecipient r WHERE ...")
List<EmailRecipient> findWithGraph(...);
```

**장점:**
- 어노테이션 기반, 간결함
- 동적 Fetch 전략 적용 가능

---

## 기술적 학습

### N+1 문제 발생 원인

```
1. Lazy Loading (기본 전략)
   @ManyToOne(fetch = FetchType.LAZY)
   → 처음엔 ID만 가져옴, 실제 객체는 나중에

2. 반복문에서 연관 객체 접근
   for (EmailRecipient r : list) {
       r.getEmail().getTitle();  // ← 여기서 쿼리!
   }

3. 각 항목마다 개별 쿼리 실행
   → N개 항목 → N개 쿼리
```

### Fetch Join vs EntityGraph

| 비교 | Fetch Join | @EntityGraph |
|------|-----------|--------------|
| **문법** | JPQL | Annotation |
| **복잡도** | 중간 | 쉬움 |
| **성능** | 동일 | 동일 |
| **유연성** | 높음 | 보통 |
| **조건 추가** | 쉬움 | 어려움 |

**선택 기준:**
- 복잡한 조건: Fetch Join
- 간단한 조회: @EntityGraph

### 주의사항

**MultipleBagFetchException:**
```java
// ❌ 에러 발생
@Query("SELECT r FROM EmailRecipient r " +
       "LEFT JOIN FETCH r.email " +
       "LEFT JOIN FETCH r.attachments")  // 2개 OneToMany

// ✅ 해결: 단계별 조회 또는 @BatchSize
@BatchSize(size = 10)
@OneToMany
private List<Attachment> attachments;
```

**페이징 문제:**
```java
// ⚠️ Fetch Join + Pageable = 메모리 페이징
// 해결: DTO Projection 또는 @BatchSize
```

---

## 결론

### 기대 vs 실제

| 항목 | 목표 | 실제 | 평가 |
|------|------|------|------|
| 쿼리 수 | 90% 감소 | **95% 감소** | ✅ 초과 달성 |
| 응답시간 | 50% 단축 | **85% 단축** | ✅ 초과 달성 |
| DB 부하 | 30% 감소 | **67% 감소** | ✅ 초과 달성 |

### 적용 범위

**현재 적용:**
- ✅ 이메일 받은편지함
- ✅ 이메일 보낸편지함
- ✅ 채팅 목록
- ✅ 알림 목록

**향후 적용 예정:**
- 게시판 댓글
- 결재 문서 목록
- 일정 참석자 목록

---

**작성일:** 2025.12.19  
**프로젝트:** CoreConnect - 사내 그룹웨어  
**역할:** Backend Developer  
**관련 이슈:** #N+1-query-optimization

