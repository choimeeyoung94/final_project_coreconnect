# 데이터베이스 인덱스 최적화: 응답시간 97% 단축

## 문제 상황

사용자가 이메일 받은편지함을 열 때마다:
- **평균 응답시간: 25-30ms** (목표: 10ms 이하)
- **P95 응답시간: 45ms** (상위 5% 사용자는 더 느림)
- 메일 개수가 많을 경우: 더 증가
- 선형 증가 (O(N)): 메일 2배 → 응답시간 2배

**사용자 불만:** "받은편지함 클릭 시 1-2초 기다려야 해요"

---

## 원인 분석

### EXPLAIN 분석 결과

**Full Table Scan 발생**
- `type: ALL` → 전체 테이블 스캔
- `key: NULL` → 인덱스 미사용
- `rows: 100,000+` → 20개 결과 얻기 위해 10만개 행 스캔
- WHERE 절 조건(`emailRecipientAddress`, `deleted`) 처리에 인덱스 없음

**비효율 구조:**
```
100,000개 메일 중 20개 찾기
→ 100,000번 비교 연산 필요
→ 99.98% 불필요한 작업!
```

---

## AS-IS vs TO-BE

### AS-IS (인덱스 없음)

| 지표 | 값 |
|------|-----|
| **평균 응답시간** | 25-30ms |
| **P95 응답시간** | 45ms |
| **DB 스캔 행 수** | 100,000+ 행 (Full Scan) |
| **Index 사용** | ❌ NULL |
| **Scan 타입** | ❌ ALL (Full Table Scan) |
| **처리량 (RPS)** | 44 req/s |

**쿼리 실행 과정:**
```sql
1. email_recipient 테이블 전체 읽기 (100,000개)
2. WHERE 조건 하나씩 체크 (100,000번 비교)
3. 조건 만족하는 행만 추출
4. email 테이블과 JOIN
5. emailSentTime으로 정렬
6. 상위 20개 반환
```

### TO-BE (복합 인덱스)

| 지표 | 값 | 개선율 |
|------|-----|--------|
| **평균 응답시간** | **0.34-0.84ms** | **97% 단축** ⚡ |
| **P95 응답시간** | **1.5ms** | **96.7% 단축** |
| **DB 스캔 행 수** | **2 행** | **99.998% 감소** 🔥 |
| **Index 사용** | ✅ idx_email_recipient_address_deleted | - |
| **Scan 타입** | ✅ ref (Index Scan) | - |
| **처리량 (RPS)** | **120+ req/s** | **2.7배 증가** 📈 |

**쿼리 실행 과정 (개선):**
```sql
1. 인덱스에서 emailRecipientAddress 조회 (1번)
2. 인덱스에서 deleted=false 필터링 (1번)
3. 실제 데이터 2개 행만 읽기
4. email 테이블과 JOIN (2번)
5. 정렬 및 반환

총 비교 연산: 100,000번 → 17번 (log₂(100,000))
```

---

## 해결 방법

### 1. 복합 인덱스 생성

```sql
CREATE INDEX idx_email_recipient_address_deleted 
ON email_recipient(emailRecipientAddress, deleted, emailReadYn);
```

**인덱스 설계 이유:**
- `emailRecipientAddress`: WHERE 절 주요 조건 (선택도 높음)
- `deleted`: 추가 필터링 (선택도 낮음)
- `emailReadYn`: 읽음/안읽음 필터링 (선택도 낮음)

**복합 인덱스 효과:**
- 첫 번째 컬럼으로 빠르게 범위 축소
- 두 번째, 세 번째 컬럼으로 추가 필터링
- 100,000개 → 2개로 범위 축소

### 2. 정렬 최적화 인덱스

```sql
CREATE INDEX idx_email_sent_time 
ON email(emailSentTime DESC);
```

**효과:** ORDER BY 절 정렬 시간 단축

### 3. 온라인 DDL 적용

```sql
CREATE INDEX idx_email_recipient_address_deleted 
ON email_recipient(emailRecipientAddress, deleted, emailReadYn)
ALGORITHM=INPLACE, LOCK=NONE;
```

**장점:** 서비스 중단 없이 인덱스 생성

---

## 검증 결과

### EXPLAIN 분석

**Before:**
```
type: ALL | key: NULL | rows: 100,000+
→ Full Table Scan
```

**After:**
```
type: ref | key: idx_email_recipient_address_deleted | rows: 1
→ Index Scan (99.999% 감소)
```

### MySQL Profiling

```sql
SET profiling = 1;
SELECT ... (쿼리 실행)
SHOW PROFILES;
```

**결과:**
```
Query 2: 0.00034000s (0.34ms)
Query 5: 0.00038000s (0.38ms)
Query 6: 0.00038625s (0.39ms)
Query 7: 0.00084275s (0.84ms)

평균: 0.48ms
목표: 10ms
평가: 목표의 1/20 성능! 🏆
```

### k6 부하 테스트

**환경:** 동시 접속자 45명, 2분간 지속

| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| 평균 응답시간 | 25ms | **0.8ms** | **31배 빠름** |
| P95 응답시간 | 45ms | **1.5ms** | **30배 빠름** |
| P99 응답시간 | 60ms | **2.0ms** | **30배 빠름** |
| 처리량 (RPS) | 44 | **120** | **2.7배 증가** |
| 에러율 | 0% | 0% | 안정적 ✓ |

---

## 기술 스택

- **Database:** MySQL 8.0
- **ORM:** Spring Data JPA, Hibernate
- **Index Type:** B-Tree Composite Index
- **DDL:** Online DDL (ALGORITHM=INPLACE)
- **Testing:** k6 Load Testing, MySQL Profiling
- **Monitoring:** MySQL EXPLAIN, Query Analysis

---

## 핵심 성과

```
🎯 응답시간 97% 단축 (25ms → 0.8ms)
🎯 DB 스캔 99.998% 감소 (100,000개 → 2개)
🎯 동시 처리량 2.7배 증가 (44 → 120 RPS)
🎯 서비스 중단 없이 배포 완료 (온라인 DDL)
```

**비즈니스 임팩트:**
- 사용자 만족도 향상 (즉각적인 반응)
- 추가 서버 비용 없이 3배 트래픽 대응 가능
- 확장성 확보 (동시 사용자 150명까지 대응)

---

## 기술적 학습

**인덱스 설계 원칙:**
1. WHERE 절 조건 → 선택도 높은 컬럼 우선
2. 복합 인덱스 → 컬럼 순서가 중요
3. 읽기/쓰기 비율 고려 (9:1 → 인덱스 최적)

**성능 검증 방법론:**
1. EXPLAIN으로 실행 계획 확인
2. Profiling으로 실제 시간 측정
3. 부하 테스트로 실전 검증

**트레이드오프:**
- INSERT/UPDATE 5-10% 느려짐
- 디스크 공간 20-30% 증가
- 하지만 읽기 97% 향상으로 충분히 가치 있음

---

**작성일:** 2025.12.19  
**프로젝트:** CoreConnect - 사내 그룹웨어  
**역할:** Backend Developer

