# 데이터베이스 성능 최적화

## 📌 인덱스 최적화: 응답시간 97% 단축

**AS-IS:** Full Table Scan으로 100,000개 행 스캔 → 평균 응답시간 25-30ms, P95 45ms

**TO-BE:** 복합 인덱스 생성 (`emailRecipientAddress, deleted, emailReadYn`) → 2개 행만 스캔

**성과:** 응답시간 97% 단축 (25ms → 0.8ms), DB 스캔 99.998% 감소, 처리량 2.7배 증가 (44→120 RPS)

**기술:** MySQL B-Tree Index, EXPLAIN 분석, 온라인 DDL, k6 부하 테스트

---

## 📌 N+1 쿼리 해결: 쿼리 수 95% 감소

**AS-IS:** 메일 20개 조회 시 21개 쿼리 실행 (1개 메인 + 20개 Lazy Loading) → DB 부하 높음

**TO-BE:** JPA Fetch Join 적용 (`LEFT JOIN FETCH r.email`) → 1개 쿼리로 통합

**성과:** 쿼리 수 95% 감소 (21개 → 1개), 응답시간 85% 단축 (18ms → 3ms), DB CPU 67% 감소

**기술:** JPA Fetch Join, Hibernate Query Optimization, SQL 로그 분석


